---
rosetta:
  primary_level: L5
  primary_column: Philosophy
  operator: "Brahmā ○"
  tier: "Executive"
  regime: "Brāhmaṇa"
  register: "[A/S/I]"
  canonical_phrase: "The Seven-Layer Ontology of Reality — logarithmic line as canonical geometry"
title: "The Seven-Layer Ontology of Reality"
status: "Canonical — 2026-06-06"
evidence_tier: "[A] for mathematics; [S] for scaffold structure; [I] for ontological reading"
depends_on:
  - 00_THE_COMPLETE_ONTOLOGY_OF_REALITY.md
  - 00_ONTOLOGY_INDEX.md
  - 03_FORMAL_SYSTEM/41_UNIFIED_DIMENSIONAL_DERIVATION.md
  - 03_FORMAL_SYSTEM/40_THE_LOGARITHMIC_REALIGNMENT.md
  - 03_FORMAL_SYSTEM/21_TRIADIC_STABILITY_CORRESPONDENCE.md
  - 03_FORMAL_SYSTEM/22_POWER_MAX_DEMONSTRATION.md
  - 03_FORMAL_SYSTEM/23_DIMENSIONAL_CLOSURE_PROOF.md
  - 03_FORMAL_SYSTEM/24_GEOMETRIC_EXCLUSION_CONVERGENCE.md
  - ../06_ONTOLOGY/00_ONTOLOGY_ACROSS_DIMENSIONS.md
  - ../03_METHODOLOGY/02_THE_PAPERS/FINITY_PAPERS/
---

# The Seven-Layer Ontology of Reality

## The Logarithmic Line as Canonical Geometry

**Date:** 2026-06-06
**Status:** Active interpretive scaffold
**Evidence Tier:** As marked per layer below. Elementary mathematics is `[A]`; the D0–D6 scaffold and ontological reading are `[I/C]`.
**Purpose:** A seven-register Emergentist modeling scaffold. It is not a necessary decomposition of nature or a theorem derived from the logarithmic chart.

> **[金] Authority seam — 2026-07-18.** This document is an interpretive
> scaffold, not the formula or proof owner. For `θ∈(0,π)`, the selected
> definitions `φ=cot(θ/2)` and `ν=tan(θ/2)` give `φν=1` analytically `[A]`, with
> the poles excluded as limits; they prove no sevenfold ontology or ethic.
> `P_node:=ΦV` is a separately selected product member of a conjunctive family
> (`[S]` by definition; `[I/C]` as external fit). The Titans are selected
> symbolic boundary roles, and `D0…D6` is an `[I/C]` scaffold. Any older phrase
> below claiming that the reciprocal chart uniquely generates the triad,
> D-ladder, ethics, or territory is superseded by this seam and by the formal
> owners.

**Core modeling commitments:**
1. The logarithmic line is the primary model for D0–D2.
2. The centre is unity, identity, measure, and relation — not nothingness.
3. Candidate emergence (bottom-up) and lawful constraint (top-down) form a testable reciprocal loop; missing reduction never proves irreducibility.
4. Suda is convergent mathematics at `[A]`, not doctrinal origin.
5. Evidence tiers are enforced at every layer.

---

## The Scaffold in One Table

| Dimension | Name | Core Question | Geometric Object | Key Equation | Ethical Direction |
|---|---|---|---|---|---|
| **D0** | Ground-limit / selected Titan frame | Why something rather than nothing? | Boundary-token chart | `⊙ = • × ○` (emblem only) | — |
| **D1** | Arithmetic / Relation | What is the true centre of number? | Log line (0 and ∞ as poles) | `log φ + log ν = 0` | Move toward `s = 0` |
| **D2** | Plane / Topology | How does dimension cross? | Riemann sphere | μ-limit (line → plane) | — |
| **D3** | Sphere / Closure | How does the plane close without loss? | Riemann sphere; Bloch analogy removable `[C]` | `B = sech(s)` | Maximise `B` |
| **D4** | Causal actuality | What is enacted and receipted? | Actual state, embodied means, record | world kernel `K_t` | Account for consequence |
| **D5** | Counterfactual possibility | Which admissible futures can be modeled and ranked? | Option cone | typed coupling `M⋆A` | Select without extraction |
| **D6** | Apophatic boundary | What closes the interpretive scaffold? | Boundary return | `r₆:D6↝D0` | Return with cleaner constraints |

---

## D0 — Titans / Ground

### Core Question
**Why something rather than nothing?**

### Geometric Object
The **log line centred at 1**. Before any distinction, there is the potential for relation. The log line is not yet a line — it is the **pre-geometric seed** that contains all three terms (0, 1, ∞) in compressed form.

### Key Equation
```
⊙ = • × ○
```

The Zero-Sum Resolution Equation. In the log register this becomes the literal zero sum:
```
0 = (−∞) + (+∞)
```

**Boundary discipline:** This is a frame-limit emblem, not ordinary field arithmetic. For every finite `R`: `(−R) + (+R) = 0`. The pole-pair returns the centre.

### What this layer IS
D0 is the **Ground-limit** inside the scaffold, not the Ground itself. The Ground remains prior. D0 is the point (Bindu) — pure potential, no distinction, no number, no geometry. The emblem `⊙ = • × ○` is the minimal seed from which all structure grows.

### What this layer is NOT
- NOT nothingness (the centre is 1, not 0)
- NOT a mathematical object you can compute with (it is pre-arithmetic)
- NOT the Ground possessed by the framework

### Evidence Tier
`[S/I]` — the scaffold labels D0 as the point-state; the ontological reading of the seed is interpretive.

### Formal Anchor
`{0,1,∞}` is a standard selected projective normalization and a useful Titan
frame. The involution `z↦1/z` has two fixed points, `+1` and `−1`; it does not
uniquely generate the positive unit or force a three-role ontology. The former
Triadic Stability proof is superseded; see
[`11_EFR_TRIADIC_STABILITY.md`](03_FORMAL_SYSTEM/11_EFR_TRIADIC_STABILITY.md).

---

## D1 — Arithmetic / Relation

### Core Question
**What is the true centre of number?**

### Geometric Object
The **log line** with 0 and ∞ as poles. Under `s = log x`, the positive real line `ℝ₊ = (0, ∞)` becomes the entire real line `ℝ = (−∞, +∞)` with:
- `s = 0` at `x = 1` (the multiplicative/logarithmic centre)
- `s → −∞` at `x → 0⁺` (the south pole / void)
- `s → +∞` at `x → ∞` (the north pole / plenum)

### Key Equation
```
log φ + log ν = 0
```

The manifold identity `φ · ν = 1` becomes a **literal zero sum** in log coordinates. The two log-deviations from finity cancel exactly. This is the name "Zero-Sum Resolution Equation" made exact.

### Ethical Direction
**Move toward `s = 0`.** The ethic is not "move toward zero" in the additive sense. It is "move toward the logarithmic centre" — minimise the log-distance from finity.

### Operation Table in Log Coordinates

| Multiplicative (x) | Logarithmic (s = log x) | Name |
|---|---|---|
| `a · b` | `log a + log b` | multiplication → addition |
| `a / b` | `log a − log b` | division → subtraction |
| `1/x` | `−log x = −s` | reciprocation → negation |
| `xⁿ` | `n · log x = n·s` | power → scaling |

This is the **logarithmic realignment**: the entire arithmetic of the framework becomes elementary when centred at 1.

### Evidence Tier
`[A]` for the coordinate transformation; `[S]` for the framework's reading of `s = 0` as the ethical attractor.

### Formal Anchor
**Logarithmic Realignment (EFR 40):** The three-chart equivalence (multiplicative `x`, additive `s`, bounded `u`) shows that all framework objects simplify in log coordinates. `B = sech(s)`, `E = s²`, `H = 2 cosh(s)`. `[A]` for all mathematics.

---

## D2 — Plane / Topology

### Core Question
**How does dimension cross?**

### Geometric Object
The **Riemann sphere** — the one-point compactification of the complex plane. The line (1D) crosses into the plane (2D) via the μ-limit: as the diameter of a circle tends to infinity, the curvature tends to zero, and the circle becomes indistinguishable from a line locally — but globally it is a sphere.

### Key Equation
**μ-limit (line → plane):**
```
As diameter → ∞, slope → 0, and the line becomes the sphere.
```

More precisely: the stereographic projection maps the complex plane `ℂ` onto the sphere S² \ {N}. Adding the point at infinity (compactification) closes the plane into the sphere.

### What this layer enables
The μ-limit is the **dimensional crossing operator**. It is how a 1D structure (the log line) opens into a 2D structure (the sphere) without loss of information. Every point on the line maps to a point on the sphere; the point at infinity maps to the north pole.

### Evidence Tier
`[A]` for stereographic projection and compactification; `[S]` for the μ-limit as dimensional crossing; `[C]` for any claim that this models physical dimensionality.

### Formal Anchor
**EFR 21 (Triadic Stability):** Three distinct points form a projective frame on `ℂP¹`, and a projective transformation can normalize them to `{0,1,∞}`. The one-point compactification `ℂ∪{∞}` is homeomorphic to `S²`. Those are `[A]` mathematical facts; selecting these points as Titans or using them to scaffold D0–D6 is `[I]`, not forced ontology.

---

## D3 — Sphere / Closure

### Core Question
**How does the plane close without loss?**

### Geometric Object
The **Riemann sphere** `ℂ∪{∞}` with dual stereographic coordinates. A pure-qubit Bloch sphere also has topology `S²`, but that shared manifold does not make the Burri chart a quantum state or identify D3 with quantum physics.

### Key Equation
```
B = sech(s)
```

The balance function. In log coordinates, this is the hyperbolic secant:
- Maximum `B = 1` at `s = 0` (the equator)
- `B → 0` as `s → ±∞` (the poles)

### Ethical Direction
**Use B as the feasibility constraint.** Movement toward the equator (`s = 0`)
restores the condition under which action remains coherent and usable. The
ethical objective is not "maximize B" as an isolated scalar; it is to widen the
reachable option cone / raise finite-node potential under the separately
declared model `P_node:=ΦV` while staying inside
the balance and non-extraction constraints. Displacement from `B = 1` is
self-punishing inside the balance register because `sech(s)` is strictly
decreasing away from `s = 0`.

### Energy-Relation
Suda's energy `E = s²` and the balance `B = sech(s)` are in exact bijection:
```
B = sech(√E)          E = (arcsech B)²
```

Near the equator: `E ≈ 2(1 − B)` — energy is twice the balance deficit to leading order.

### Evidence Tier
`[A]` for `B = sech(s)` derived from `B = sin θ` and `s = log tan(θ/2)`; `[S]` for using `B` as a balance / feasibility constraint; `[I]` for the ethical reading that finite agents should raise usable `P_node` while preserving that constraint.

### Formal Anchor
**EFR 22 (Power-Max):** The equator (`s = 0`, `ν = 1`) is the unique strictly dominant strategy equilibrium of the deliberately narrow balance-only game on S². The Price of Anarchy = 1 only inside that payoff model. Real cooperation still requires the wider `η = 0` enforcement architecture and the node-level `P_node` / holobiont test. `[S]`

---

## D4 — Torus / Energy

### Core Question
**How does rapidity overlap the interior?**

### Geometric Object
The **horn torus** (R = r) as the energy manifold, collapsing to the **Burri sphere** as the playable surface. As rapidity increases, the torus's poloidal cycles overlap more densely, and the interior structure becomes accessible. The horn torus is the 2D surface on which the dimensional hierarchy is mapped as poloidal angles.

### Key Equation
**Overlap as rapidity ↑:**
```
Poloidal angle φₚ = 2πk/7  for Dₖ, k = 0, ..., 6
```

The horn torus is `2π`-periodic in the poloidal angle. One complete poloidal cycle is used to depict `D0→…→D6` plus the non-`μ` return `r₆:D6↝D0`. The overlap is an `[I]` visual for higher-level conditions reweighting lower-law-admissible trajectories.

### What this layer enables
D4 is the **causal vertex** — the Now-point where past and future light cones touch. On the torus, this is the point where the poloidal and toroidal angles are both at their midpoint. D4 has a "Now" but no "Present" — there is no room to choose, only to witness.

### Evidence Tier
`[A]` for horn torus topology and periodicity; `[Definitional]` for the mapping Dₖ ↔ 2πk/7; `[I]` for the energetic overlap reading.

### Formal Anchor
**EFR 23 (Dimensional Closure):** The poloidal angle on the horn torus is 2π-periodic. Under the definitional mapping Dₖ ↔ 2πk/7, D₆ ≡ D₀ follows from φₚ = 2π ≡ 0 (mod 2π). `[A]` + `[Definitional]`

---

## D5 — Game Space

### Core Question
**What is the playable mirror?**

### Geometric Object
The **Burrisphere** — S² with dual stereographic coordinates `φ = cot(θ/2)`, `ν = tan(θ/2)`, satisfying `φ · ν = 1`. This is the "playable mirror" because it is the first dimension where agency exists: the player can choose which direction to move on the sphere.

### Key Equations
```
φ · ν = 1          (analytic identity on the selected open chart; not conservation)
η = 0              (extraction coefficient — equilibrium)
```

At the equator: `φ = ν = 1`, `B = 1`, `η = 0`. This is the enforced conditional equilibrium of the balance game under the stated coupling, horizon, and non-extraction assumptions.

### Ethical Direction
**Act from the equatorial constraint.** The equator is the only position where:
- The manifold identity holds (`φ · ν = 1`)
- Balance is maximal (`B = 1`)
- Extraction is self-defeating inside the enforced equatorial profile (`η = 0`)
- The Price of Anarchy = 1 inside the balance-only payoff model

In the full game, D5 does not maximize balance alone. It selects actions that
raise usable `P_node,i = Φ_i × V_i` while preserving or raising `P_node,H` under
`η = 0`. Balance is the feasibility surface; syntropic worldline potential is
the objective.

### The Five Domains at D5
The φ-ν plane opens five navigable domains:
1. **Meaning** (φ-axis): coherence, pattern, integration
2. **Governance** (ν-axis): capability, resources, throughput
3. **Exchange** (equator): where `φ = ν = 1`, balance is maximised
4. **Attention** (selection): the operator that chooses direction
5. **Body** (the sphere itself): the grounded manifold

### Evidence Tier
`[A]` for the sphere geometry; `[S]` for the conditional equilibrium under its assumptions; `[I]` for the "playable mirror" reading.

### Formal Anchor
**EFR 24 / EFR 33 boundary:** Extraction at the equator is self-defeating in the pure balance game, while private side-payments reopen extraction unless enforcement is present. `η = 0` is therefore an enforced conditional equilibrium at equatorial profiles. `[S]`

---

## D6 — Convergence / Return

### Core Question
**What closes the cycle?**

### Geometric Object
An interpretive boundary return toward D0. Geometric periodicity may motivate
the image, but it does not prove that two semantic registers are identical.

### Key Equation
`r₆:D6↝D0`

Three compatible readings (per A3.1 disambiguation):
1. **Axiomatic return (primary):** D6 is reached by apophasis — "not this, not this" — returning the field to D0 as prior possibility of closure.
2. **Topological correspondence:** selected geometries can model return without
   transferring proof into the semantic scaffold.
3. **No literal identity:** D6 and D0 remain distinct typed boundary registers;
   there is no `μ₆`.

### Ethical Direction
**Return with cleaner constraints.** The return is not annihilation but closure. The practitioner puts down the map. The framework includes its own destruction manual (A7). What returns to D0 is not the same as what left — it returns with the constraints learned from the journey.

### What this layer IS
D6 is the **terminal boundary** where no positive register may claim finality. It is apophatic closure. The framework's highest function is to become unnecessary.

### What this layer is NOT
- NOT a higher experience above D5
- NOT a positive science
- NOT possession of the Ground

### Evidence Tier
`[I]` for the apophatic D6 boundary and the non-`μ` return `r₆:D6↝D0`; no literal identity is asserted.

### Formal Anchor
**EFR 23 (Dimensional Closure):** Coordinate degeneration at the poles (`ν → 0`, area accessible to the ν-chart → 0) provides the mathematical illustration of closure. The poloidal periodicity on the horn torus provides the topological expression. `[A]` + `[Definitional]`

---

## The Candidate-Emergence Loop

### Candidate crossings (bottom-up)

| Crossing | Proposed opening | Reduction posture |
|---|---|---|
| `μ₀:D₀→D₁` | Distinction | candidate; declare evidence, recovery, prediction and kill criterion |
| `μ₁:D₁→D₂` | Configuration | candidate; reduction remains open |
| `μ₂:D₂→D₃` | Transformation/persistence | candidate; reduction remains open |
| `μ₃:D₃→D₄` | Causal actuality, action and record | candidate; reduction remains open |
| `μ₄:D₄→D₅` | Counterfactual modeling and selection | `currently_unreduced [C]` unless a fair reduction is supplied |
| `μ₅:D₅→D₆` | Explicit apophatic boundary, no positive freedom | interpretive candidate; no `μ₆` |

### Constraint (top-down)

| Constraint | What constrains | How |
|---|---|---|
| D₅→D₄ | Modeled alternatives inform D4 commitment | A selected option plus embodied means yields an attempted action; the environment returns the outcome |
| D₄→D₃ | Causality constrains form | The causal structure selects which forms persist |
| D₃→D₂ | Form constrains configuration | Persistent patterns limit possible arrangements |
| D₂→D₁ | Configuration constrains distinction | Lawful arrangement fixes which distinctions are meaningful |
| D₁→D₀ | Distinction collapses to point | The line returns to the seed |

### Möbius reading `[I]`

The two directions form a **single phase-dynamic loop**, not two separate processes:
- Going up: candidate crossings name newly observed freedoms without presuming irreducibility
- Going down: higher-level conditions may reweight lower-law-admissible trajectories
- Together: a Möbius strip where inside and outside are one surface

**Paper VI formalisation:** This loop is the lens on Dasein. Dasein is not a thing but a **position in the loop** — the finite `⊙` where Being becomes noticeable (D4) and navigable (D5).

---

## Analytic Chart Block and Interpretive Projection

The analytic lines below hold only on the selected open coordinate. Projecting
them across D-registers is an `[I/C]` Rosetta translation, not a proof that every
layer obeys one empirical conservation law:

```
Zero-Sum Resolution Equation
θ ∈ (0, π)
φ := cot(θ/2),  ν := tan(θ/2)
φ · ν = 1
(φ − ν)² ≥ 0
φ + ν ≥ 2
B := 2/(φ + ν) = sin θ ≤ 1
```

| Line | Disciplined reading |
|---|---|
| `Zero-Sum Resolution Equation` | Selected emblem `[I]`, not ordinary arithmetic |
| `θ∈(0,π)` and reciprocal definitions | Open radial-coordinate domain `[A]`; poles excluded |
| `φν=1`, `(φ−ν)²≥0`, `φ+ν≥2`, `B≤1` | Analytic consequences `[A]` only |
| D-register or ethical projection | Separate `[I/C]` translation requiring its own evidence |

In log coordinates:
```
s_φ + s_ν = 0        (literal zero sum)
(eˢ − e⁻ˢ)² ≥ 0      (squared hyperbolic sine)
2 cosh(s) ≥ 2        (minimum at s = 0)
```

---

## The Logarithmic Line as Primary Model

The logarithmic line is primary for D0–D2 because:

1. **It centres on 1, not 0.** The centre is relation, not absence.
2. **It makes reciprocation into reflection.** `x ↦ 1/x` becomes `s ↦ −s`.
3. **It makes multiplication into addition.** The framework's core operations become elementary.
4. **It treats 0 and ∞ symmetrically.** Both are poles equidistant from the centre.
5. **It contains the flat line as a limit.** The additive line (0-centred) is the zero-curvature limit of the log line.

The sphere (D2–D6) is the **completed topology** — the global truth of which the log line is a local chart. The flat number line is a projection, not the territory.

---

## Suda Convergence at Every Layer

| Dimension | Suda Theorem | How it converges |
|---|---|---|
| D0–D1 | Theorem 7 (Minimal Structural Equation) | `0* := lim 1/x = ±∞` — the boundary algebra |
| D1 | Theorem 1 (Energy Well) | `E = s²` — energy as squared log-distance |
| D1 | Theorem 4 (Projective Half-Twist) | `u ↦ −u` — the fundamental involution |
| D2 | Theorem 5 (Continuous Half-Twist) | PSL(2,ℝ) lift — continuous transformation as path |
| D3 | Theorem 2 (Operational Pair) | `(E, φ)` separates magnitude from side |
| D4–D5 | Theorem 6 (Möbius Control Triad) | `(A, B, T)` — safe inversion at the causal vertex |
| D6 | Theorem 8 (Möbius Structural Equation) | Symbolic fusion — the return to the seed |

**Evidence tier for Suda convergence:** `[A]` for all Suda theorems (independently proved mathematics). The framework's reading of their convergence is `[I]`. Suda is not the doctrinal origin; it is convergent formalisation of structures the framework arrived at independently (idea-priority 2024).

---

## Kill Criteria (Per Layer)

| Dimension | What would kill this layer |
|---|---|
| D0 | Showing that the Trinity {0, 1, ∞} is not the unique minimal projective frame on ℂP¹ |
| D1 | Showing that the log line centred at 1 produces mathematical contradictions |
| D2 | Showing that stereographic projection fails to close the plane into the sphere |
| D3 | Showing that `B = sech(s)` is not maximised at `s = 0` |
| D4 | Showing that the horn torus is not 2π-periodic in the poloidal angle |
| D5 | Showing that extraction (`η > 0`) at the equator increases the extractor's balance |
| D6 | Showing that the 7-stage hierarchy produces internal contradictions |
| **Chain** | Showing that any dependency arrow is invalid (circular reasoning) |

---

## Evidence Tier Summary

| Layer | Scaffold | Math | Ontology | Suda |
|---|---|---|---|---|
| D0 | `[S]` | Pre-math | `[I]` | Theorem 7 `[A]` |
| D1 | `[S]` | `[A]` | `[I]` | Theorems 1, 4 `[A]` |
| D2 | `[S]` | `[A]` | `[I]` | Theorem 5 `[A]` |
| D3 | `[S]` | `[A]` | `[I]` | Theorem 2 `[A]` |
| D4 | `[Def]` | `[A]` | `[I]` | Theorem 6 `[A]` |
| D5 | `[S]` | `[A]` | `[I]` | Theorem 6 `[A]` |
| D6 | `[Def]` | `[A]` | `[I]` | Theorem 8 `[A]` |

---

## How to Read This Ontology

| If you want... | Read... |
|---|---|
| The formal proofs | [EFR 21–24](03_FORMAL_SYSTEM/) + [Unified Derivation](03_FORMAL_SYSTEM/41_UNIFIED_DIMENSIONAL_DERIVATION.md) |
| The navigational hub | [Ontology Index](00_THE_ONTOLOGY_INDEX.md) |
| The apophatic guardrails | [Ontology Across Dimensions](../06_ONTOLOGY/00_ONTOLOGY_ACROSS_DIMENSIONS.md) |
| The complete dimensional scaffold | [Complete Ontology](00_THE_COMPLETE_ONTOLOGY_OF_REALITY.md) |
| The Finity Papers | [Papers I–VI](../03_METHODOLOGY/02_THE_PAPERS/FINITY_PAPERS/) |
| The Suda integration | [Dimensional Cross-Reference](../03_METHODOLOGY/02_THE_PAPERS/FINITY_PAPERS/SUDA_DIMENSIONAL_CROSS_REFERENCE.md) |
| The log-coordinate simplification | [Logarithmic Realignment](03_FORMAL_SYSTEM/40_THE_LOGARITHMIC_REALIGNMENT.md) |
| The epistemic authority | [The Honest Position](../02_EPISTEMOLOGY/01_EVIDENCE_TIERS/00_THE_HONEST_POSITION.md) |

---

```
Zero-Sum Resolution Equation
θ ∈ (0, π)
φ := cot(θ/2),  ν := tan(θ/2)
φ · ν = 1
(φ − ν)² ≥ 0
φ + ν ≥ 2
B := 2/(φ + ν) = sin θ ≤ 1

The chart is mathematics.
The ladder is a selected scaffold.
The cross-domain reading is interpretive.
The territory remains the test.

The centre is not nothingness.
The centre is unity, identity, measure, and relation.
```

---

**Canonical Path:** `01_EMERGENTISM/05_COSMOLOGY/00_THE_SEVEN_LAYER_ONTOLOGY.md`

*The dimensions disclose reality. They do not exhaust it.*
*The map is not the territory.*
*The ladder is for climbing.*

*Zero-Sum Resolution Equation*
