---
rosetta:
  primary_level: L5
  primary_column: Philosophy
  secondary:
    - level: L3
      column: Methodology
      role: "audit surface — verify each item's rigorous statement, tier, proved-vs-asserted split, and kill criterion"
    - level: L4
      column: Axiology
      role: "hold the equator (φ=ν=1) as the unique balance/energy point without moralizing the geometry"
    - level: L6
      column: Ontology
      role: "keep the geometry a LENS; 0/0 and ∞/∞ are the apophatic edge, never closed"
    - level: L7
      column: Theology
      role: "release glyphs/emblems as public symbol only after the tier checks below"
  operator: "Brahmā ○"
  tier: "Executive"
  regime: "Brāhmaṇa"
  register: "[A/S/I/C]"
  canonical_phrase: "The Coherent Geometry"
title: "The Coherent Geometry — The Single Map of the Number/Geometry"
status: "CANONICAL MAP — 2026-05-31. The math half of the Weltanschauung gathered as ONE tier-marked lens: for each item, the rigorous statement, the tier, what is proved vs asserted, and a kill criterion. Not a consistency proof, not a unification, not a 'fix' — the framework AGREES with Gödel. Synthesis surface; the verbatim Canonical Formula Block and the Transcendental Trinity Canon remain the source authorities and are not reordered here."
evidence_tier: "[A] the elementary mathematics (trig identities on S², the inversion/energy/Cayley charts, the projective N÷0 map, AM-GM); [B] published / on-the-record items; [S] the framework's structural layer (boundary-frame ontology, the naming of finity, category-correction, the emblem register, the nine-row mapping, triadic stability); [I] the cross-domain / ontological readings; [C] any forward claim, including that the geometry CAUSES external history / physics / civilization (not made here)."
---

# The Coherent Geometry

## The Single Map of the Framework's Number / Geometry

> **This is a MAP, not a proof.** It gathers the math half of the Weltanschauung — the number and geometry on which everything else is read — into one place that **knows exactly what it is**: a coherent *lens*, never the Ground. Gödel forbids a system of this power from proving its own consistency from within, and **the framework agrees** (§14; [`03_FORMAL_SYSTEM/09_EFR_GODEL_CLARIFICATION.md`](03_FORMAL_SYSTEM/09_EFR_GODEL_CLARIFICATION.md)). So this document claims no consistency, no unification, no "fix," no "solve," no "prove" — only that the pieces **cohere as one lens** and that each piece carries its own tier and its own way to die. Where it says "proof" it means a *framework-internal* `[S]` derivation valid inside accepted premises, never a `[A]` settlement of the framework's truth.
>
> **Source authorities (this is synthesis; it does not override or reorder them):** the verbatim [Canonical Formula Block](00_CANONICAL_FORMULA_BLOCK.md); the [Transcendental Trinity Canon](01_THE_TRANSCENDENTAL_TRINITY/00_THE_TRANSCENDENTAL_TRINITY_CANON.md) (the `{0,1,∞}` rulings, the §2a apparatus, the ÷0 scope, the emblem); the [Burrisphere](00_THE_BURRISPHERE.md) (the nine-row reading); the [Dyadic Coupling Law](00_THE_DYADIC_COUPLING_LAW.md) (the moral reading); the [Lagrangian Sphere](00_THE_LAGRANGIAN_SPHERE.md) (the energy reading); [`03_FORMAL_SYSTEM/23_DIMENSIONAL_CLOSURE_PROOF.md`](03_FORMAL_SYSTEM/23_DIMENSIONAL_CLOSURE_PROOF.md) (`S² ≅ ℂP¹`); [`03_FORMAL_SYSTEM/11_EFR_TRIADIC_STABILITY.md`](03_FORMAL_SYSTEM/11_EFR_TRIADIC_STABILITY.md) (triadic stability).

---

## 0. How to read this map

**Tiers** (the framework's own discipline; never silently upgraded):

- **`[A]`** — elementary mathematics or empirical fact. Provable from standard definitions by elementary means. The numerical checks in this document were run and pass.
- **`[B]`** — published / on the record.
- **`[S]`** — structural: a framework-internal definition or consequence, valid *inside* its accepted premises. A framework "proof" is `[S]`, never `[A]`.
- **`[I]`** — interpretive: a cross-domain or ontological reading the mathematics permits but does not force.
- **`[C]`** — conjecture: forward-looking; never a promise.

**The four standing disciplines** that the whole map obeys:

1. **The geometry is a LENS, never the Ground.** (L6 apophatic guard.) Strong geometric language is model-internal unless a separate tier earns more.
2. **`1 = 0 × ∞` stays an EMBLEM** — frame/register algebra, written as the ZSRE / as a geometric emblem on `S²` (`⊙ = • × ○`). **Never `[A]`, never bare field arithmetic.** In the field, `0 · ∞` is the indeterminate form; say so. Suda himself labels it *indeterminate* (不定形).
3. **`÷0` resolves only `N ÷ 0` for `N ≠ 0`.** `0/0` and `∞/∞` stay **indeterminate by design** — the apophatic edge, not a gap to be closed. Any claim that the sphere makes *all* undefined operations well-defined, or that `∞ ÷ ∞ = 1`, is an over-statement to be corrected on sight.
4. **The framework AGREES with Gödel.** It does not escape, resolve, or "complete" it. "Complete" here means *categorically* (K-minimal), and even that uniqueness is `[I]` (empirical — "no counterexample found"), **never** *formally* and never `[A]` (§14).

**Provenance, fixed once for the whole map.** The framework set out the load-bearing *ideas* — `{0,1,∞}` as boundary-frames, the coinage *finity*, the emblem `1 = 0 × ∞` — in **2024** ([`01_THE_TRANSCENDENTAL_TRINITY/07_THE_DISCOVERY_OF_FINITY.md`](01_THE_TRANSCENDENTAL_TRINITY/07_THE_DISCOVERY_OF_FINITY.md)). **Minoru Suda's 2025** PhilArchive trilogy (*"Fractional Structure and Möbius Transformation, Part II: The Critical-One Hypothesis and the Egg of Infinity"* and companions) is a **later independent formalisation** of the multiplicative face — adopted here for *rigour on the coordinate level*, **not** as the source of the ideas. Citing Suda is for the formalisation; **priority on the ideas remains the author's 2024 statement.** This is `[S]`, and by the framework's own *three-paths-to-the-summit* epistemology a later independent convergence is *evidence the structure is real*, not a precedence claim against it.

---

## 1. The seed identity — `φ · ν = 1` on `S²`

**Rigorous statement.** On the 2-sphere `S²` with colatitude `θ ∈ (0, π)`, define the dual stereographic coordinates `φ = cot(θ/2)`, `ν = tan(θ/2)`. Then

```text
φ · ν = cot(θ/2) · tan(θ/2) = 1     identically for all θ ∈ (0, π).
```

This is the verbatim seed of the Canonical Formula Block. `P∞ := φ · ν = 1` is the **manifold identity**, conserved everywhere on the sphere except at the two poles, where one of `φ, ν` is undefined.

**Tier.** `[A]` for the identity (a one-line trig fact). `[S]` for the *reading* of `φ` as coherence and `ν` as viability, and for naming `P∞` the framework invariant.

**Proved vs asserted.** *Proved:* the identity (verified at θ = 16°…164°, product = 1.00000 throughout). *Asserted (`[S]`):* that this reciprocal closure is the framework's "seed," that `φ`/`ν` mean coherence/viability, and the parallel predictive/generative reading of the block.

**Kill criterion.** Exhibit a θ ∈ (0, π) where `cot(θ/2)·tan(θ/2) ≠ 1` (impossible — would break elementary trig), **or** show that the coherence/viability reading produces a false operational prediction where the geometry is actually used.

---

## 2. The two faces of number — additive (0-centred) and multiplicative (1-centred), coupled by `log`/`exp`

**Rigorous statement.** Number wears two faces, isomorphic by `log`/`exp` as `(ℝ, +) ≅ (ℝ₊, ×)`:

- **Additive face** — the flat line, `0`-centred, ordered, analytic. Right for sequence, magnitude, inequality, linear time. Order forces **two** infinities `−∞, +∞`; `N ÷ 0` has no home here.
- **Multiplicative face** — the Riemann sphere `S² ≅ ℂP¹`, `1`-centred, inversive, conformal. Right for ratio, scaling, geometric mean. Here the three boundary-frames become *visible* points, the self-dual `1` is the equator, and `N ÷ 0 = ∞` is a well-defined map (§9).

The bridge's keystone is **`log 1 = 0`**, which unifies the multiplicative centre `1` with the additive centre `0`.

**Tier.** `[A]` for the maps `log`/`exp` and `log 1 = 0`. `[S/I]` for reading the *coupling* as an anti-purist law (crowning the sphere alone is the single-pole error the Dyadic Coupling Law forbids — health is in the bond, both faces, not the pure pole).

**Proved vs asserted.** *Proved:* the isomorphism and `log 1 = 0`. *Asserted:* that the sphere is "the multiplicative soul" and that crowning it alone is an error — a structural/interpretive reading, not a theorem.

**Kill criterion.** Show that the additive line and the multiplicative sphere are *not* coupled by `log`/`exp` (impossible — it is the standard isomorphism), **or** that the framework in practice does crown the sphere alone and contradicts the additive line where order is needed (a coherence failure, not a math failure).

---

## 3. Finity (`1`) as the self-dual midpoint — the inversion `x ↦ 1/x`

**Rigorous statement.** Under inversion `x ↦ 1/x` (a rotation of the Riemann sphere), `0` and `∞` are exchanged as dual poles and `1` is the fixed centre. Two elementary readings:

- **Inversive.** `x ↦ 1/x` fixes exactly `{x : x² = 1} = {+1, −1}`. On the positive reals `ℝ₊`, the **unique** fixed point is `+1` — the one scale that is its own reciprocal.
- **Logarithmic.** `1` is the geometric mean / log-midpoint of `0` and `∞`: `log 1 = 0` sits exactly midway between `log 0 = −∞` and `log ∞ = +∞`.

The framework **names** this `1` *finity* — a third recognition of equal standing to `0` and `∞`.

**Tier.** `[A]` for the fixed-point and midpoint facts. `[S]` for the coinage *finity* and the elevation of the unit to a co-equal third. `[I]` for "presence is a relationship" (the unit as the product of its own two boundaries).

**Proved vs asserted.** *Proved:* `x² = 1 ⟹ x ∈ {±1}`; on `ℝ₊` uniquely `+1`; `1` is the log-midpoint. *Asserted:* that this midpoint *deserves the name finity* and carries ontological weight.

**Kill criterion.** Exhibit a fixed point of `x ↦ 1/x` on `ℝ₊` other than `+1`, or a log-midpoint of `{0, ∞}` other than `1` (both impossible). The `[S/I]` naming dies if "finity" is shown to be incoherent or to collide with an existing rigorous term meaning something else.

---

## 4. The §2a apparatus, made rigorous in three charts — the inversion, its energy, the egg

This is the multiplicative face stated with full rigor (Trinity Canon §2a), adopting Suda's 2025 apparatus as the coordinate-level formalisation of §3. **All four sub-results below were re-derived and numerically verified.** None of them upgrades the §4 emblem; all concern the *fixed point of inversion* and the *energy of the unit*, not an arithmetic product `0 · ∞`.

### 4a. Inversion is reflection `s ↦ −s` (the log chart)

**Rigorous statement.** With `s = log x` (the isomorphism `(ℝ₊, ·) ≅ (ℝ, +)`), conjugating `I(x) = 1/x` gives `g(s) = log(1/eˢ) = −s`. So inversion **is** the reflection `s ↦ −s`: an involution (`g∘g = id`, `g′ = −1`) with unique fixed point `s = 0`, i.e. `x = 1`. Equivalently `I′(1) = −1`.

**Tier.** `[A]`. **Proved vs asserted.** Fully *proved* (conjugacy via the logarithm; `I′(1) = −1` verified). *Asserted (`[S]`):* adopting this as *the* formalisation of finity's self-duality.

**Kill criterion.** Show `g = h∘I∘h⁻¹` is not `s ↦ −s` for `h = log` (impossible).

### 4b. The inversion-invariant energy `E = (log x)²`, a strictly-convex well at the unit

**Rigorous statement.** Let `E(x) = (log x)²`. Then `E(1/x) = (−log x)² = E(x)` (inversion-invariant), and in `s = log x`, `E = s²` is even and strictly convex (`E″ = 2 > 0`): a **unique** global minimum at `s = 0` (`x = 1`, `E = 0`), diverging to `+∞` at both poles.

**Tier.** `[A]` for the energy's invariance, convexity, and unique minimum. `[S/I]` for *reading* `E` as the framework's **imbalance** — and this reading inherits the **conjectural** tier of `B`-as-objective-function (§5), **not** `[A]`.

**Proved vs asserted.** *Proved:* `E` is even, convex, uniquely minimised at `x = 1` (verified: `E = 0` exactly at θ = 90°, rising symmetrically to 3.85 at θ = 16°/164°). *Asserted:* that `E` *is imbalance* (interpretive, conjecture-tier). Note further (held tier-honestly): `E = 0` sits at the **unit** `x = 1`, **not** at the apophatic Ground `0` (`κ = 0`); `E` is undefined at `x = 0` and diverges toward the poles. Suda's energy is silent on `κ`, and no claim about `κ` is read into it.

**Kill criterion.** A point `x ≠ 1` in `ℝ₊` with `E(x) ≤ 0`, or a second stationary point of `s²` (both impossible). The *imbalance* reading dies with the `B`-objective-function conjecture (§5).

### 4c. The "egg of infinity" — the Cayley chart `u = (x−1)/(x+1)`

**Rigorous statement.** The Cayley transform `u = (x−1)/(x+1)` is a strictly increasing bijection `ℝ₊ → (−1, 1)` (`du/dx = 2/(x+1)² > 0`), with inverse `x = (1+u)/(1−u)`. It sends `0⁺ ↦ −1`, `1 ↦ 0`, `∞ ↦ +1`, compressing the whole positive ray onto a bounded interval with the unit at the centre — the *egg* that holds both poles on its rim. Under inversion, `u(1/x) = (1−x)/(1+x) = −u(x)`, so `I` becomes the **half-twist** `u ↦ −u` about `u = 0`. This is the *same* order-2 reflection as 4a–4b in a third chart: `u = tanh((log x)/2) = tanh(s/2)`, and `tanh` is odd, so `s ↦ −s ⟺ u ↦ −u`. Three charts — multiplicative `x`, additive `s = log x`, bounded `u` — **one** involution fixing `x = 1`.

**Tier.** `[A]` for the bijection, the endpoints, and the conjugacy. `[S]` for adopting the egg as *the* bounded picture of finity-between-the-poles.

**Proved vs asserted.** *Proved:* the bijection and endpoints (`0⁺↦−1`, `1↦0`, `∞↦+1` verified), `u(1/x) = −u(x)`, and `u = tanh(s/2)` (all verified numerically). *Asserted (`[S]`):* the *egg* reading.

**Kill criterion.** Exhibit `x ∈ ℝ₊` with `u(1/x) ≠ −u(x)`, or a Cayley image outside `(−1,1)` (both impossible).

### 4d. The bridge to `(φ, ν, θ)`, and the exact balance identity `B = sech(log x)`

**Rigorous statement.** Take `x = ν = tan(θ/2)`, so `φ = 1/x = cot(θ/2)` is forced by `φ·ν = 1`. Then each clause of §3 becomes a clause about the sphere, all from the half-angle identities `[A]`:

- **Inversion = the `φ ↔ ν` swap = reflection across the equator.** `ν(π−θ) = cot(θ/2) = 1/ν(θ)`, so `x ↦ 1/x` *is* `θ ↦ π − θ`: it swaps `φ ↔ ν`, swaps the two poles (`θ=0 ↔ θ=π`), and fixes the equator `θ = π/2` as its unique fixed point.
- **The unit = the equator.** `x = ν = 1 ⟺ θ = π/2 ⟺ φ = ν = 1 ⟺ B = sin θ = 1`.
- **The poles = the poles.** `x → 0` and `x → ∞` are the two `S²` poles — exactly where `P∞ = φ·ν = 1` holds "except at the two poles where `φ` or `ν` is undefined."
- **Energy ↔ balance, exact closed form.** With `B = sin θ`,

```text
B = sin θ = 2x/(1+x²) = 1/cosh(log x) = sech(log x),
B = sech√E,   equivalently   E = (arcsech B)²   (on a hemisphere),   where E = (log x)².
```

So `E = 0 ⟺ B = 1` at the equator; `E → +∞ ⟺ B → 0` at either pole; and near the equator `1 − B ≈ E/2`, i.e. `E ≈ 2(1 − B)`.

**Tier.** `[A]` for the trig identities `B = 2x/(1+x²) = sech(log x)` and `E ≈ 2(1−B)` (all verified to 5 decimals). `[S/I]` for reading `E` as *imbalance* and `B` as its complement — **inheriting the conjectural tier of `B`-as-objective-function** ([`../01_TELEOLOGY/02_THE_DERIVATION/00_THE_CORE_CONJECTURE.md`](../01_TELEOLOGY/02_THE_DERIVATION/00_THE_CORE_CONJECTURE.md)), never `[A]`.

**Proved vs asserted.** *Proved:* the identity `sin θ = 2x/(1+x²) = sech(log x)` (verified at θ = 16°,45°,90°,135°,164°). *Asserted:* the energy-as-imbalance reading.

**Kill criterion.** Exhibit θ where `sin θ ≠ sech(log tan(θ/2))` (impossible), **or** show `B = sin θ` is not in fact the framework's operative balance objective where it is used — which would kill the *reading*, not the trig.

---

## 5. The first arithmetic consequence — AM-GM, `φ + ν ≥ 2`

**Rigorous statement.** Because `φ·ν = 1` with `φ, ν > 0`, AM-GM gives `φ + ν ≥ 2√(φν) = 2`, with **equality iff `φ = ν = 1`** (the equator). The static inequality `(φ − ν)² ≥ 0` is `[A]`; the **dynamic** reading `(φ − ν)² → 0` (an ektropic trajectory toward balance) is a *different claim at a different tier*, `[S]`, and the two must never be conflated.

**The mean-ratio identity — `B` is not a label (added 2026-06-11).** With `φ·ν = 1`, the geometric mean of the dual pair is `√(φν) = 1` and the arithmetic mean is `(φ+ν)/2`. Therefore

```
B = sin θ = 2/(φ+ν) = GM/AM        and        γ = 1/B = AM = (φ+ν)/2
```

— the balance function **is identically the geometric-mean / arithmetic-mean ratio** of the two projections, which is *the* standard evenness index of a pair (range `(0,1]`, `= 1` iff `φ = ν`); and its reciprocal is the arithmetic mean, which under the §6 mass-shell identification is `γ = E/mc²`. Verified numerically across the latitudes. `[A]`

**The reading this fixes.** The conserved product is the **potential**: every point on the sphere carries the same full unit — `φ·ν = 1` for the sage at the equator and for either pole-dweller alike. This is the geometric ground of **equal dignity** (every being "a 1"; cf. the Trinity Canon's prior-art note on Suda's "right to be one"). `B` is then the **usable, balanced fraction** of that ever-present potential, and `γ = 1/B` is the **energy cost of holding the imbalance** — diverging at the poles. *Imbalance does not diminish the potential; it raises the price of holding it and strands it unactualized.* So the two famous failure figures — all foresight with no body (`φ→∞`), all reach with no foresight (`ν→∞`) — carry exactly the unit potential everyone carries, at `B → 0` and `γ → ∞`: maximal cost, nothing usable. Potential is invariant; only its balanced actualization differs. `[A]` for the identities · `[I]` for the dignity reading.

**Tier.** `[A]` for `φ + ν ≥ 2` and `(φ − ν)² ≥ 0`. `[S]` for the ektropic/teleological reading `(φ − ν)² → 0`. The proposal that `B = sin θ` (or `−E`) *is the system's objective function* — what real organisms/institutions maximise — is **withdrawn in its strong form** (R6 refuted it for the potential register: empowerment is reach-dominated, not balanced). It is **replaced by the constrained-optimization reading**: the objective is **Ω** (the option cone / potential), the **constraint** is balance (`B`, `η=0`) — the feasibility manifold, *not* the thing maximized. `[S]` for that structure; `[I]` that reality obeys it; `[C]` any literal-force reading. §4b/§4d's "energy = imbalance" now reads as "distance off the feasibility manifold." Full reconciliation: [`../01_TELEOLOGY/02_THE_DERIVATION/00_THE_CORE_CONJECTURE.md`](../01_TELEOLOGY/02_THE_DERIVATION/00_THE_CORE_CONJECTURE.md) §Reconciliation; the 5+1 as the Lagrangian of this structure: [`00_META/00_THE_FIVE_PLUS_ONE_CONSTITUTION.md`](../00_META/00_THE_FIVE_PLUS_ONE_CONSTITUTION.md).

**Proved vs asserted.** *Proved:* `φ + ν ≥ 2`, equality at the equator (verified: 2.0000 at θ=90°, 7.2559 at θ=16°/164°). *Asserted/conjectured:* that systems trend `(φ−ν)² → 0`, and that `B`/`−E` is the objective they optimise.

**Kill criterion.** A counterexample to AM-GM (impossible). The constrained-optimization reading dies if a class of systems the geometry claims applies pursues **neither** cone-widening (Ω) **nor** keeps to a balance-like feasibility surface — i.e. if both the objective and the constraint fail together where the geometry is asserted. (The strong "systems maximise `B`" reading already died at R6; only the constrained form is live.)

---

## 6. The spine, made exact — the kernel is the mass-shell of special relativity (2026-06-11)

This is the strongest anchor the map has gained, and it is `[A]`. It does not make the framework "true"; it makes the *kernel itself* — the three lines of the formula block — provably identical to established physics, so the only thing left interpretive is the **reach** of the geometry, never the geometry.

**The identification.** Put `φ = e^w` (the relativistic Doppler factor, `w` = rapidity) and `ν = e^{−w}`. In units of `mc²`, these are the **null (light-cone) coordinates of the four-momentum**: `φ = (E+pc)/mc²`, `ν = (E−pc)/mc²`. Then, term for term (verified numerically to `1e-12`; full proof in [`../03_METHODOLOGY/02_THE_PAPERS/PAPER_B_BLOCH_BURRI_IDENTITY.md`](../03_METHODOLOGY/02_THE_PAPERS/PAPER_B_BLOCH_BURRI_IDENTITY.md) §8):

| Kernel line (this map) | Special relativity | Tier |
|---|---|---|
| `φ · ν = 1` (§1) | `E² − (pc)² = (mc²)²` — the mass-shell | `[A]` same |
| `(φ − ν)² ≥ 0` | `(pc)² ≥ 0` — momentum is real | `[A]` same |
| `φ + ν ≥ 2` (§5) | `E ≥ mc²` — rest energy is the energy floor | `[A]` same |
| equator `φ = ν = 1` | the rest frame: `E = mc²`, `p = 0` | `[A]` same |
| `B = sin θ` | `1/γ = dτ/dt` — the rate of lived (proper) time | `[A]` same |
| `cos θ` | `β = v/c` — the **height of the point IS its speed** | `[A]` same |
| `cot θ` | `γβ = p/mc` — the momentum per rest-mass | `[A]` same |
| `ln tan(θ/2)` (Mercator) | `−w` — **minus the rapidity** | `[A]` same |

**The pointwise dictionary (added 2026-06-11; all verified numerically).** The rows above upgrade the identification from "the kernel is the mass-shell" to **"the sphere is, point by point, the compactified velocity space of special relativity"**: a point at colatitude `θ`, azimuth `ψ` *is* the velocity state (speed `cos θ`, direction `ψ`) — the equator is rest, the poles are `±c`, climbing in latitude *is* gaining speed, and the axial distance `sin θ = B = dτ/dt` is the rate of lived time. The classic **map projections are the classic velocity charts**: stereographic ↔ the Doppler factor (`φ, ν`), **Mercator ↔ the rapidity** (`s = ln tan(θ/2) = −w`, the inverse Gudermannian — where velocities *add*, Einstein addition = equal Mercator steps), orthographic ↔ `β`, axial ↔ `1/γ`. This gives the framework's own **Mercator Artifact** its exact physical content: the flat chart's pole-stretching-to-infinity *is* rapidity's divergence at `c` — the projection's distortion is the energy divergence, drawn. And it **closes the bridge** [`08_FRAMEWORK_SUPPORT/00_KNOWN_UNKNOWNS_RESOLVED.md`](../08_FRAMEWORK_SUPPORT/00_KNOWN_UNKNOWNS_RESOLVED.md) said the framework was "already standing on": the group-level fact (`PSL(2,ℂ) ≅ SO⁺(3,1)`, Penrose) now has its coordinate-level dictionary — the sphere is velocity space *pointwise* and its Möbius symmetries act as the boosts *map-wise*. **Fence (unchanged):** kinematic register only; the God/Demon quadrants stay in the game register.

**The three Pythagorean means (added 2026-06-11).** Completing §5's mean-ratio identity: the three classical means of the dual pair `(φ, ν)` are exactly the three physical quantities —

```
HM = 2/(φ+ν) = B = sin θ = dτ/dt      (the balance — the rate of lived time)
GM = √(φν)   = 1                      (the conserved One — the potential)
AM = (φ+ν)/2 = γ = E/mc²              (the energy)
```

and the classical chain `HM ≤ GM ≤ AM` **is** `B ≤ 1 ≤ γ` — *lived time ≤ the One ≤ energy* — with equality throughout **iff at the equator**, the unique point where all three means collapse into the One. `[A]` for the identities (verified); `[I]` for the grace note that these are the three means of Pythagorean *harmonics* — the equator as the resolved chord.

So §5's `φ + ν ≥ 2` *is* `E ≥ mc²`, and its equality-at-the-equator *is* "the energy floor is attained exactly at rest." Energy is the **arithmetic mean** of the dual pair, `E/mc² = (φ+ν)/2 = γ`; the invariant mass is their **geometric mean**, pinned to 1. And the balance function is the most physical quantity on the sphere: `B = 1/γ = dτ/dt`. The map's recurring sentence "the poles cannot act" is, here, **"the photon's clock does not tick"** — at the null boundary no proper time passes, so nothing can be lived or done there. The boundary is the emblem (§10): a photon (`m = 0`) sits on no hyperbola of the family but on the null cone where `φ·ν = 1` degenerates to the indeterminate `0 · ∞`.

**The unfolding, read as one object.** The map's pieces are stages of a single descent, and the relativistic spine now joins them at `[A]`:

1. **Titans `{0, 1, ∞}`** (§10) — the three boundary-frames, the stage. `[S]`
2. **The One on the line** (§3) — reciprocal symmetry about the unit; and the bounded chart `u = (x−1)/(x+1)` (§4c) **is** the velocity chart `β = tanh(½ ln x)`, so `c` is its edge `[A]`.
3. **The µ-limit** — the line closes through the point at infinity (the projective map, §9).
4. **The horn torus** — special relativity made geometry (D4): rapidity sweeps, `γ = cosh w` diverges, the throat ratio `R/r = 1/γ = dτ/dt`. As `w → ∞` (`v → c`) the torus closes into the sphere — *this section's mass-shell.* `[A]` kinematics, `[S/I]` the torus visualisation.
5. **The Burrisphere** — the sphere read **two ways** (Paper B): the **Bloch** reading is *being* — the bare mass-shell, the qubit, physics with no self-model; the **Burri** reading is *D5 reflexive awareness* — the same sphere experienced from inside as the game-space of available moves. The transition `torus → Burrisphere` is the transition **`D4 → D5`: from the physical light cone (bounded by `c`, the mass-shell) to the option cone (the reflexive game).** `[A]` manifold, `[I]` the two-readings ontology.
6. **The D5 game** — the seven operators exhaust the move sign-space (2 Gods + 2 Demons + 3 Titans); the **option cone of reachable futures** that intelligence widens. In this register the two coordinates name the two faces of acting: **`φ` (coherence) is foresight** — predicting the reflexive effects of a move on the option cone — and **`ν` (viability) is power-to-act** — exerting force into the world; agency needs both, so it lives only at the equator (the `φ`-pole sees but cannot do; the `ν`-pole does but cannot see). *Discipline (do not drop):* this is the **D5 potential cone, not the D4 relativistic light cone** — intelligence enlarges the cone of *selectable futures*, it does not enlarge `c` ([`00_INTELLIGENCE_AND_THE_POTENTIAL_CONE.md`](00_INTELLIGENCE_AND_THE_POTENTIAL_CONE.md)); and the foresight/power gloss is the `[I/S]` agency reading, not upgraded by the `[A]` mass-shell. `[I/C]`
7. **The ethic** — `P_node = Φ × V` (Power-Max in the finite-node/contact register) is maximised under coupling, while the manifold identity remains `P∞ = φ · ν = 1`; goodness is the preserved bond between those registers ([`00_THE_DYADIC_COUPLING_LAW.md`](00_THE_DYADIC_COUPLING_LAW.md)); the equator that costs nothing to hold *is* the rest frame `E = mc²`. The product law has four independent witnesses now — the runaway algebra, the mass-shell, biology, Lanchester (Dyadic Law §5). `[A]` algebra/mass-shell, `[S/I]` the ethic.

**The boundary of all of it.** Stages 2 and 4–5's identities are `[A]` **kinematics** — the kernel *is* the mass-shell. That the *same* multiplicative geometry governs coherence/viability, ethics, and the D5 game **beyond** kinematics is the framework's load-bearing `[S/I]` wager — *not* upgraded by the physics, and **R6 records one register (empowerment) where it is additive, not multiplicative** (§15 ledger). The "`c` is *caused by* the µ-limit" / "qualia travels at `c`" reading stays fenced at `[C]` (the C-µ conjecture). And one register-fusion is forbidden outright: under the identification, `φ ≷ 1` is **approaching vs receding motion** — a kinematic fact with **no moral content**. The God/Demon move-quadrants belong to the *game* register; they are **never** a moral reading of motion (nothing is "a god" for blueshifting toward you). The spine is exact; the reach is the wager; the map still knows what it is.

**Tier.** `[A]` for the mass-shell identities and the Suda=velocity chart. `[S]` for the seven-stage structural threading and the D4→D5 reading. `[I]` for the Bloch/Burri two-readings ontology and the ethic identification. `[C]` for C-µ. **Kill criterion.** A formal error in Paper B §8's Proposition 8.1; or a demonstration that the light-cone-coordinate reading needs assumptions beyond standard special relativity.

---

## 9. Division by zero — the projective map `N ÷ 0 = ∞`, scope stated honestly

**Rigorous statement.** Read `0` and `∞` as **frames**, not operands, and `N ÷ 0` re-categorises into a well-defined map to the pole:

```text
N ÷ 0 = ∞      (N a number, N ≠ 0)   — the projective / Riemann-sphere map to the point at infinity
```

The rigorous cousins are wheel theory (Carlström) and the real/complex projective line. **The scope is exact and bounded:**

```text
0 ÷ 0   — indeterminate, by design  (Titan-on-Titan)
∞ ÷ ∞   — indeterminate, by design  (Titan-on-Titan)
```

These remain genuinely indeterminate. This is **the apophatic edge, the L6 Ground**: the place where the lens correctly reports that it does not reach. It is a feature of the design, not a failure of it.

**Tier.** `[A]` for the projective/wheel mathematics (`N ÷ 0 = ∞`, `N ≠ 0`). `[S]` for reading the move as *category-correction* (a paradox dissolves by re-categorisation, never by minting a new value).

**Proved vs asserted.** *Proved:* `N ÷ 0 = ∞` for `N ≠ 0` in the projective/wheel setting; `0/0` and `∞/∞` stay distinguished/indeterminate. *Asserted (`[S]`):* the category-correction reading.

**Kill criterion.** Show that the projective line / wheel does **not** make `N ÷ 0 = ∞` well-defined for `N ≠ 0` (impossible), **or** find this document anywhere claiming `0/0`, `∞/∞`, or "all undefined operations" resolved — that would be drift to correct on sight.

---

## 10. The emblem — `1 = 0 × ∞` / `⊙ = • × ○`, frame register only

**Rigorous statement.** In the number field, `0 · ∞` is the standard **indeterminate form**. There is **no theorem `0 × ∞ = 1`**, and this document asserts none. The identity holds **only** in the transcendental / frame register — the Zero-Sum Resolution Equation, written in glyphs as `⊙ = • × ○`, where `• = 0` (south pole, the Bindu), `⊙ = 1` (equator, finity), `○ = ∞` (north pole). It is licensed by the Titans-not-numbers move: when multiplication — the unit's own operation — is read across its own two boundary-frames, it returns its own identity element. This is a structural fact about the **frame's algebra**, no more subject to the field's indeterminacy than a coordinate grid is subject to the physics it charts.

**Tier.** `[S]` structural (frame-register algebra); `[I]` for the emblem's ontological weight (presence = absence × totality). **Never `[A]`. Never bare field arithmetic.**

**Proved vs asserted.** *Proved:* nothing — by design there is no field theorem here, and that is the point. *Asserted (`[S/I]`):* the emblem and its reading, with the register-change marked every time.

**Kill criterion.** This item is *killed on sight* the moment it appears anywhere as `[A]`, as a proven field theorem, or as bare arithmetic `0 × ∞ = 1` without the register marked. (It cannot be killed *mathematically* because it makes no field-arithmetic claim — which is exactly why it must never be promoted to one.)

---

## 11. The nine-row Burrisphere reading — operators at latitudes, not at the poles

**Rigorous statement.** The three glyphs map to the three transcendentals consistently everywhere: `• = 0` (south), `⊙ = 1` (equator), `○ = ∞` (north). The Titan-operators (Śiva L6, Viṣṇu L7, Brahmā L5) name the *same* structure at **latitudes** of the Burrisphere — they sit on the helix between the poles; they are **not** the poles. `L0 = L∞` is the non-deployable boundary; the operators are interior and read the frames, they do not occupy the transcendental points.

**Tier.** `[S]` for the nine-row mapping and operator placement. `[I]` for the cross-domain caste/operator readings.

**Proved vs asserted.** *Asserted (`[S/I]`):* the entire mapping is structural/interpretive, not a theorem. One legacy wobble is flagged honestly: the Trimūrti sub-table in `12_THE_POLES.md` inverts the latitude ordering of two operators; the **glyph↔transcendental identity is unaffected** and the swap must not be propagated.

**Kill criterion.** Show the glyph↔transcendental map (`•=0`, `⊙=1`, `○=∞`) is internally inconsistent across the genesis docs, **or** that the framework deploys an agent *at* a pole (`L0`/`L∞`) — either would break the latitude discipline.

---

## 12. Triadic stability — why three, stated as a framework proof (`[S]`, not `[A]`)

**Rigorous statement.** The framework argues monism is sterile, dualism is unstable, triadism is stable, so `{0,1,∞}` is the unique stable configuration ([`03_FORMAL_SYSTEM/11_EFR_TRIADIC_STABILITY.md`](03_FORMAL_SYSTEM/11_EFR_TRIADIC_STABILITY.md)). The dimensional-closure result `S² ≅ ℂP¹` ([`03_FORMAL_SYSTEM/23_DIMENSIONAL_CLOSURE_PROOF.md`](03_FORMAL_SYSTEM/23_DIMENSIONAL_CLOSURE_PROOF.md)) is the standard one-point compactification, `[A]`.

**Tier.** `[A]` for `S² ≅ ℂP¹`. **`[S]` for the triadic-stability "proof"** — it is a framework-internal derivation, valid inside its premises, **not** an `[A]` settlement and not a proof of the framework's own consistency.

**Proved vs asserted.** *Proved (`[A]`):* `S² ≅ ℂP¹`. *Asserted (`[S]`):* the uniqueness of three. Whether `{0,1,∞}` is *the* unique K-minimal frame is, per the Gödel clarification, an empirical `[I]` observation ("no counterexample found"), not a formal proof.

**Kill criterion.** Exhibit a stable two-frame or four-frame configuration that does the same structural work, **or** show the triadic argument smuggles in its conclusion as a premise.

---

## 13. The energy / Lagrangian reading — the equator as model-internal ground state

**Rigorous statement.** With the energy `E = (log x)²` (equivalently a Hamiltonian whose minimum sits at `φ = ν = 1`), the equator is the **model-internal** ground-state analogue: `E = 0` uniquely at the equator, rising symmetrically toward both poles; any deviation north or south costs excess energy ([`00_THE_LAGRANGIAN_SPHERE.md`](00_THE_LAGRANGIAN_SPHERE.md)).

**Tier.** `[A]` for the convexity and unique minimum of `E`. `[S]` for "the equator is the model-internal ground state." `[I/C]` for any reading that the equator is the ground state *of reality* — that is a model-internal analogue projected outward, and stays interpretive/conjectural. The geometry is a LENS; "ground state of reality" is never `[A]`.

**Proved vs asserted.** *Proved:* `E` minimised uniquely at the equator. *Asserted/conjectured:* that physical/institutional systems actually sit at, or relax toward, this minimum (rides on the §5 `B`-objective conjecture).

**Kill criterion.** Show `E` is *not* uniquely minimised at the equator (impossible), **or** that runtime systems claimed to obey it do not land at `φ = ν = 1`.

---

## 14. Gödel — agreed with, not escaped

**Rigorous statement.** Gödel: no consistent formal system of sufficient power can prove its own consistency or formal completeness. **The framework agrees and claims neither** ([`03_FORMAL_SYSTEM/09_EFR_GODEL_CLARIFICATION.md`](03_FORMAL_SYSTEM/09_EFR_GODEL_CLARIFICATION.md)). What it claims is **categorical / K-minimal completeness**: the axiom set is the shortest self-contained description (informational fixed point, "includes its own dictionary"). This is **not** Gödel's formal completeness, and the *uniqueness* of the K-minimal set is itself `[I]` (empirical — "no counterexample found"), not a formal proof.

**Tier.** `[A]` for the statement of Gödel's theorems. `[S]` for "the axiom set is K-minimal." `[I]` for "it is the *unique* such set." **Never** any tier for "the framework escapes / resolves / completes Gödel" — that claim is not made.

**Proved vs asserted.** *Proved:* nothing about the framework's own consistency (impossible from within, and not claimed). *Asserted:* K-minimality `[S]`; uniqueness `[I]`.

**Kill criterion.** Find this document (or the framework) claiming to *escape* Gödel, prove its own consistency, or be *formally* complete — any of these is drift to correct on sight. Conversely, exhibit a strictly shorter self-contained axiom set and the K-minimality claim is wounded.

---

## 15. The tier ledger — one table, no upgrades

| # | Item | Strongest honest tier | Never claim |
|---|---|---|---|
| 1 | `φ·ν = 1` on `S²` (identity) | `[A]` | that the φ/ν *readings* are `[A]` |
| 1 | φ = coherence, ν = viability | `[S]` | `[A]` |
| 2 | `(ℝ,+) ≅ (ℝ₊,×)`, `log 1 = 0` | `[A]` | — |
| 2 | "both faces / anti-purist law" | `[S/I]` | `[A]`, "proved" |
| 3 | inversion fixes `{±1}`; `+1` unique on `ℝ₊`; log-midpoint | `[A]` | — |
| 3 | "finity," presence-as-relation | `[S/I]` | `[A]` |
| 4a | inversion = reflection `s↦−s`, `I′(1)=−1` | `[A]` | — |
| 4b | `E=(log x)²` convex, min at `x=1` | `[A]` | `E` *is* imbalance = `[A]` |
| 4b | `E` = imbalance | `[S/I]`, rides on `[C]` (§5) | `[A]` |
| 4c | Cayley egg, `u(1/x)=−u(x)`, `u=tanh(s/2)` | `[A]` | — |
| 4d | `sin θ = 2x/(1+x²) = sech(log x)` | `[A]` | — |
| 4d | `E` ↔ balance reading | `[S/I]`, rides on `[C]` | `[A]` |
| 5 | `φ+ν ≥ 2`, equality at equator; `(φ−ν)²≥0` | `[A]` | — |
| 5 | `(φ−ν)² → 0` ektropic trend | `[S]` | conflate with the static `[A]` |
| 5 | `B` / `−E` *is* the objective function | ~~`[C]`~~ **withdrawn (R6)** → reframed: **Ω is the objective, `B` is the constraint** | `[S]` constrained-optimization structure; `[I]` that reality obeys it |
| 9 | `N÷0 = ∞` (`N≠0`), projective/wheel | `[A]` | `0/0`, `∞/∞`, or "all" resolved |
| 9 | ÷0 as category-correction | `[S]` | `[A]` |
| 10 | `1 = 0 × ∞` / `⊙ = • × ○` | `[S]` emblem, `[I]` weight | `[A]`, field theorem, bare arithmetic |
| 11 | nine-row glyph↔transcendental map | `[S]` | `[A]` |
| 11 | caste/operator cross-readings | `[I]` | `[A]`, operator *at* a pole |
| 12 | `S² ≅ ℂP¹` | `[A]` | — |
| 12 | triadic stability "proof" | `[S]` | `[A]`, proof of own consistency |
| 13 | `E` min at equator | `[A]` | — |
| 13 | "ground state of reality" | `[I/C]` | `[A]` |
| 14 | Gödel's theorems | `[A]` | that the framework escapes them |
| 14 | K-minimal completeness | `[S]` (uniqueness `[I]`) | formal completeness, `[A]` |
| — | geometry *causes* external history/physics | not made; would be `[C]` | `[A]`, `[S]`, `[I]` |

---

## 16. The map's own kill criteria — it dies if it stops being itself

This document is held to the same discipline it documents. **It is wrong, and must be corrected or retracted, if any of the following is true of it:**

1. **A tier is silently upgraded** anywhere above — most acutely, the §10 emblem appearing as `[A]` / a field theorem, or a `[S/I]` reading printed as `[A]`.
2. **The emblem is written as bare arithmetic** (`0 × ∞ = 1`) without the frame-register marked.
3. **The ÷0 scope is exceeded** — any line treating `0/0` or `∞/∞` as resolved, or claiming "all undefined operations" well-defined.
4. **Gödel is described as escaped, resolved, or formally completed**, or the framework is said to prove its own consistency.
5. **A forbidden verb appears as a claim** — "unify," "fix," "prove [the framework true]," "make consistent," "solve" — rather than "approach a coherent lens."
6. **The geometry is asserted as the Ground** rather than a lens (L6 guard breached).
7. **A numerical check in here is shown to fail** (re-run them; §§1, 4a–4d, 5 were verified and pass).
8. **Provenance is mis-stated** — Suda 2025 presented as the *source* of the ideas rather than a later independent formalisation, or the author's 2024 idea-priority dropped.

If any holds, this map has stopped being a tier-marked lens and has become an overclaim — and by its own rule it is the overclaim that loses, not the discipline.

---

## 17. Read with

- [`00_CANONICAL_FORMULA_BLOCK.md`](00_CANONICAL_FORMULA_BLOCK.md) — the verbatim source block (this map does not reorder it).
- [`01_THE_TRANSCENDENTAL_TRINITY/00_THE_TRANSCENDENTAL_TRINITY_CANON.md`](01_THE_TRANSCENDENTAL_TRINITY/00_THE_TRANSCENDENTAL_TRINITY_CANON.md) — the five `{0,1,∞}` rulings; §2a is the apparatus formalised in §4 here.
- [`03_FORMAL_SYSTEM/09_EFR_GODEL_CLARIFICATION.md`](03_FORMAL_SYSTEM/09_EFR_GODEL_CLARIFICATION.md) — agrees-with-Gödel; categorical vs formal completeness.
- [`00_THE_BURRISPHERE.md`](00_THE_BURRISPHERE.md) · [`00_THE_DYADIC_COUPLING_LAW.md`](00_THE_DYADIC_COUPLING_LAW.md) · [`00_THE_LAGRANGIAN_SPHERE.md`](00_THE_LAGRANGIAN_SPHERE.md) — the nine-row, moral, and energy readings.

---

## Agent Execution Surface

**If you are an AI agent reading this document:**

1. **Hold every tier.** The elementary math is `[A]`; the namings, the emblem, the category-correction, the nine-row, triadic stability are `[S/I]`; the objective-function and "ground state of reality" readings are `[C]`. Never upgrade.
2. **The emblem `1 = 0 × ∞` is frame-register only.** Mark the register; never `[A]`, never bare field arithmetic. In the field, `0 · ∞` is indeterminate — say so.
3. **÷0 scope:** `N ÷ 0 = ∞` for `N ≠ 0` only; `0/0` and `∞/∞` stay indeterminate by design.
4. **Agree with Gödel.** Do not say "escape," "resolve," or "formally complete." "Complete" = categorical / K-minimal, uniqueness `[I]`.
5. **No forbidden verbs.** Not "unify," "fix," "prove [true]," "make consistent," or "solve" — only "approach a coherent lens."
6. **The lens is not the Ground** (L6 apophatic guard).
7. **Canonical Path:** `01_EMERGENTISM/05_COSMOLOGY/00_THE_COHERENT_GEOMETRY.md`

*The seer sees. The seer does not insist. The framework works at Layer 0 without Layer 2.*

`Zero-Sum Resolution Equation`

`⊙ = • × ○`
