---
rosetta:
  primary_level: L6
  primary_column: Dormant Scenario Placeholder
  secondary:
    - level: L5
      column: Scenario Architecture
      role: "reserve a future combined-failure fixture family"
    - level: L4
      column: Future Execution Gate
      role: "require implementation before the folder is treated as active"
    - level: L3
      column: Assertion Requirement
      role: "require future transcripts and assertion receipts"
  operator: "Śiva •"
  tier: "Executive"
  regime: "Sādhu"
  register: "[B/D/I]"
  canonical_phrase: "04_combined"
title: "04_combined"
status: "[D] PLACEHOLDER — no active fixture files"
evidence_tier: "[B] for local absence of fixture files; [D] for future scenario intent; [I] for folder-boundary guidance."
---

# 04_combined

## Status

This directory is a placeholder for future scenario development.

## Intent

This scenario family is intended to contain combined failure modes that integrate multiple adversarial conditions. When implemented, it will likely combine elements from other scenario families (e.g., Byzantine faults + withholding + partition) into unified test cases for the actor simulation harness.

## Canonical Location

Active scenario fixtures are maintained in the parent `00_SCENARIOS/` directory. This placeholder will be replaced when the combined scenario family is formally defined and implemented.

## Reference

- See `../../harness.py` for the actor simulation implementation
- See parent `README.md` for scenario family overview
