# Runtime dependencies for active Python validation scripts in this directory.
# Keep parser semantics pinned: predeploy_check.py validates security-sensitive
# CSS URL tokenization with tinycss2 and fails closed on a version mismatch.
tinycss2==1.5.1
