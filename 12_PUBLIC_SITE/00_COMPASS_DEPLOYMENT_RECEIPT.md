---
rosetta:
  primary_level: L4
  primary_column: Meta
  operator: "Arjuna ⚔"
  tier: "Executive"
  regime: "Kṣatriya — K2-authorized deployment complete"
  register: "[S] for predeploy gate; [B] for file counts; [I] for deployment interpretation."
canonical_phrase: "Compass restructure deployed — 7 wings live, predeploy gate green, K2 signature honored"
title: "Compass Deployment Receipt — 2026-07-14"
evidence_tier: "[S] for predeploy verification; [B] for quantitative evidence."
---

# Compass Deployment Receipt — 2026-07-14

**Produced:** 2026-07-14T14:30Z  
**K2 Authorization:** Confirmed by user directive "i sign"  
**Archive:** `90_ARCHIVE/tool_noise/2026_07_14_rosetta_audit/` + `01_EMERGENTISM/12_PUBLIC_SITE/`  
**Status:** DEPLOYED — predeploy gate passes, 7 wings live, vercel.json updated

---

## I. What Was Deployed

### K2-Authorized Changes

| Change | From | To | K2 Authority |
|---|---|---|---|
| **Root redirect** | `/` → `/amrita/` | `/` → `/compass/` | K2 signature — user directive |
| **Compass landing** | Old `compass/index.html` (10 KB) | New `compass/index.html` (29 KB) | K2 signature — restructure authorized |
| **7 new wings** | Staging only | Live routes | K2 signature — build authorized |

### 7 Wings at Live Routes

| Wing | Route | Files | Size | Status |
|---|---|---|---|---|
| Compass Landing | `/compass/` | `index.html` | 29 KB | ✓ Live — default route |
| Test Falsifiers | `/test/` | `index.html` + `css` + `js` | 40 KB | ✓ Live |
| Halāhala Audit | `/halahala/` | `index.html` + `css` + `js` | 48 KB | ✓ Live |
| Build Pipeline | `/build/` | `index.html` + `css` + `js` | 39 KB | ✓ Live |
| Rosetta Map | `/map/` | `index.html` + `css` + `js` + `README` | 31 KB | ✓ Live |
| Journey Mode | `/journey/` | `index.html` | 19 KB | ✓ Live |
| K4 Grace Exit | `/exit/` | `index.html` + `css` + `js` + `README` | 31 KB | ✓ Live |

**Total:** 7 wings, 19 files, ~237 KB live. [B]

---

## II. K3 Archive-First Discipline

| Action | Location | Evidence |
|---|---|---|
| Old compass archived | `compass/_archive/index_2026_07_12_pre_restructure.html` | [B] |
| Old vercel.json archived | `90_ARCHIVE/tool_noise/2026_07_12_vercel_json_redirect_change/vercel.json.pre-compass-redirect` | [B] |
| `_archive/` excluded from deploy | `.vercelignore` line 46: `_archive/` | [S] |

---

## III. Predeploy Gate Verification

```
PASS: all checks green
public corpus documents wired: 297
landing links: all 16 wings verified
generated library chrome: present on 313 pages
deployment publication boundary: .vercelignore excludes source/control/runtime files
```

**Note on resolved errors:** The initial run showed 95 errors, all from:
1. K3 archive copies in `90_ARCHIVE/` (outside deploy scope, not served)
2. Hash anchor links (`#eta`, `#k2`, etc.) resolved as file paths by the checker
3. The `_archive/` directory (now excluded by `.vercelignore`)

After `.vercelignore` update and path fixes: **all checks green.** [S]

---

## IV. Governance Compliance

| Boundary | Status | Evidence |
|---|---|---|
| **K2** | Irreversible config change executed with K2 signature | User directive "i sign" [S] |
| **K3** | Archive-first applied — old compass and vercel.json preserved | Archive paths verified [B] |
| **K4** | Exit wing live at `/exit/` — door marked on the inside | [S] |
| **A7** | Evidence tiers on every claim in all 7 wings | 200+ tier badges [S] |
| **η = 0** | No extraction; no funnel; no capture | [S] |
| **Ω** | Compass orients; does not convert | [S] |

---

## V. The 5 K2-Staging Clusters — Resolution Status

| Cluster | Status | Resolution |
|---|---|---|
| **K2-C1: Domain Cutover** | **STILL PENDING** | `emergentism.org` DNS still points to Google Sites. Vercel alias `emergentism-org.vercel.app` serves the repository site. The redirect change is staged but DNS cutover requires owner action. |
| **K2-C2: Compass Countersign** | **STAGED** | `THE_COMPASS` is `[D]` staged pending receipt 111. The `/compass/` wing is live as default route. If receipt 111 requires revision, the Compass can be updated without structural change. |
| **K2-C3: [D] Scope Notes** | **STAGED** | 2026-07-03 conditional inserts are live in the Compass. Ratification is a documentation upgrade, not a structural change. |
| **K2-C4: F5 Boundary** | **STAGED** | F5 is presented as operational metaphor in public wings. Strong-form physical claim, if ratified, can be surfaced in `/test/` without structural change. |
| **K2-C5: Five Forces Table** | **STAGED** | Table is not rendered in any public wing. Withdrawal/banner is a content decision, not structural. |

---

## VI. What a Visitor Sees Now

**At `https://emergentism-org.vercel.app/` (Vercel alias):**
- Redirects to `/compass/` — the 5-minute Compass landing
- Sees: "A compass, not a cathedral." — 7 scannable sections, evidence tiers visible, exit door at bottom
- Can navigate to: `/test/` (falsifiers), `/halahala/` (what we learned), `/build/` (immune system), `/map/` (Rosetta Stone), `/journey/` (optional path), `/exit/` (K4 Grace Exit)
- Can still access: `/0/` through `/6/` (dimensions), `/read/` (library), `/papers/` (tiered papers), all existing routes

**At `https://www.emergentism.org/` (custom domain):**
- Still serves old Google Sites surface with `Dominate`, `Subjugate`, `Holy War`, `Dalits` fingerprints
- **This is the remaining P0 blocker.** The repository site is ready; the domain is not pointed at it.

---

## VII. Next Steps

### Immediate (K2-Authorized, Can Proceed)

1. **DNS Cutover** — Attach `emergentism.org` and `www.emergentism.org` to the Vercel project `emergentism-org`. This is the final P0 gate.
2. **Post-Cutover Smoke Test** — Run `audit_live_domain_against_manifest.py --strict` after DNS propagates.
3. **Old Route Policy** — Decide 301/410/archived-provenance for `/vision`, `/mission`, `/goal`, `/strategy`, `/tactics`, `/action` (old Google Sites routes).

### Staged for K2 Review (Non-Blocking)

4. **Compass Receipt 111** — Verify and countersign, or revise if needed.
5. **[D] Scope Notes Ratification** — Formalize the conditional boundary on equator-optimum.
6. **F5 Strong/Weak Form** — Ruling on operational metaphor vs. physical claim.
7. **Five Forces Table** — Withdraw, banner, or relocate to standalone [C] document.

---

## VIII. The Auditor's Final Verdict

> **The framework is the most honestly tiered philosophical system this audit has encountered.** The risk is not that it is wrong. The risk is that a naive reader will never discover it is right because the packaging screams "cult" before the content can whisper "honest."
>
> **The restructure fixes the packaging without changing the content.** The cathedral stays. The door changes. The door is marked on the inside.
>
> **The compass is now live.** The only remaining gate is the domain.

---

*16 agents, 109 papers, 7 wings, 1 compass, 1 K2 signature. The deployment is complete. The domain is the final gate.*

⊙ = • × ○
