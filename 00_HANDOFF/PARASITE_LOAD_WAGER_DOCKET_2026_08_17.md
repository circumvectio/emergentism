---
title: "DOCKET — W13-PARASITE staged conjecture; owner decision: adopt into the ledger, hold, or archive"
type: reconciliation-docket
date: 2026-08-17
evidence_tier: "[B] the staged file exists on disk at the named path, verified 2026-08-17; [I] the option analysis; the decision itself is an OWNER act, not staged here"
status: "OPEN — discovery-stage finding from the 2026-08-17 brainstorm audit. One salvageable seed was retyped as a full Honest Position §2 claim record at [D]; it now awaits a ruling."
may_sign: false
may_authorize: false
authority_effect: none
---

# DOCKET — W13-PARASITE, one seed from a rejected brainstorm

The 2026-08-17 brainstorm audit rejected an external myth-history synthesis
(secret-history cosmology with demon-possession and caste-curse content) on
three grounds: no custody, no kill, two fence violations. Exactly one seed
survived: the intuition that parasitic load persists at every level of the
replicator stack and must be actively churned down. It has been retyped as a
full claim record:

> [`06_ONTOLOGY/10_PARASITE_LOAD_WAGER_2026_08_17.md`](../06_ONTOLOGY/10_PARASITE_LOAD_WAGER_2026_08_17.md)
> — `[D]` staged, candidate `W13-PARASITE`, `[C]` engine, killable, fenced.

It is **not** a ledger row, not a `CLAIM_STATUS.yaml` entry, and not a board
socket. All three are owner acts.

## What the owner is choosing between

- **Option A — Adopt into the wager ledger.** Add row W13 to
  `04_THE_CONJECTURES.md` at entry tier `[C]` with the staged file as full
  claim record; add the lifecycle row to `CLAIM_STATUS.yaml` as
  `OPEN-EMPIRICAL` blocked on the first contact packet; assign the next free
  GP socket (proposed GP-13) with the §7 retrospective media-lag packet.
  Cost: three small edits plus the eventual obligation to run or decline the
  packet. This is the only option under which the conjecture can ever touch
  the world.
- **Option B — Hold as staged.** Keep the `[D]` file in `06_ONTOLOGY/`,
  register it in `00_WORK_IN_PROGRESS/` as an open item awaiting the ruling
  above, defer ledger entry. Cost: an open item with no route; risks the
  exact "went quiet" failure the WIP manifest exists to prevent unless a
  review date is set.
- **Option C — Decline and archive.** Route the staged file to
  `90_ARCHIVE/` under a dated directory with this docket as provenance. The
  seed is preserved as a dated grave; the brainstorm audit's verdict stands
  unmodified.

## Why Option A is the one with teeth

The conjecture's first contact (S2, exploit-first colonization of newly
opened media) is the cheapest world-contact packet the corpus currently has
unspent: dated public records, one frozen medium set, one lag metric, one
blind-coded control. Every other socket in the board waits on scales,
instruments, or custodies it does not have. If the corpus wants its first
genuine `[B]` world result rather than another design cycle, this is the
smallest instrument that could produce one — and it kills itself cleanly if
null.

## What this docket does not do

It does not adopt, promote, cite, or authorize. It does not touch
`CLAIM_STATUS.yaml`, the ledger, the board, or any checker. If the owner does
nothing, the staged file sits at `[D]` with this docket beside it, and the
WIP manifest entry (if filed) names what it waits on — never silence.

*Filed 2026-08-17 by the Hermes session that ran the brainstorm audit; the
staged file was verified on disk before filing. The seed came from outside;
the typing is the corpus's.*
