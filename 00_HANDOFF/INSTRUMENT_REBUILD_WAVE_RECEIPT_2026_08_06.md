---
rosetta:
  primary_level: L5
  primary_column: "Repair wave — instrument rebuild / the new headline / 15 of 18 stand"
  operator: "Brahmā ○"
  tier: "Wave receipt"
  register: "[S] this receipt consolidates the instrument-rebuild wave; [B] the count of files touched; [A] the items restored are corpus-held; [D] the staged one-sentence summary; [I] the meta-finding on the wrong metric"
title: "Instrument-Rebuild Wave Receipt — 2026-08-06 — The new headline"
status: "ACTIVE — instrument-rebuild wave closed; 15 of 18 stand on the truth axis (re-hearing of 2026-08-06); the '21 entries, zero owned' headline RETIRED and replaced with contradiction census + citation completeness; the antipodality kill re-argued on corpus-internal grounds (the corpus's own successor operator S=[[1,1],[0,1]]); the 56 reading restored at [B] inherited-cite (the form is killed, the reading is footnote, not strike); live-gate integrity flagged (check_foundation.py exits 1 on NameError)"
date: 2026-08-06
canonical_path: 01_EMERGENTISM/00_HANDOFF/INSTRUMENT_REBUILD_WAVE_RECEIPT_2026_08_06.md
parents:
  - 01_EMERGENTISM/14_THE_DISTILLATION/00_THE_RUNGS_2026_08_05.md
  - 01_EMERGENTISM/12_PUBLIC_SITE/00_THE_RUNGS_2026_08_05.md
  - 01_EMERGENTISM/00_HANDOFF/D2_REPAIR_WAVE_RECEIPT_2026_08_05.md
  - 01_EMERGENTISM/00_HANDOFF/D3_REPAIR_WAVE_RECEIPT_2026_08_05.md
  - 01_EMERGENTISM/00_HANDOFF/D4_REPAIR_WAVE_RECEIPT_2026_08_05.md
  - 01_EMERGENTISM/00_HANDOFF/D5_REPAIR_WAVE_RECEIPT_2026_08_05.md
  - 01_EMERGENTISM/00_HANDOFF/THE_EXECUTION_PLAN_2026_08_05.md
---

# Instrument-Rebuild Wave Receipt — The new headline

**The instrument-rebuild wave is closed. The fair-instrument re-hearing returned 15 of 18 stand, 3 die, on the truth axis. The rigged panel's 0/18 is replaced by the fair panel's 15/18. The distillation's "21 entries, zero owned" headline is RETIRED and replaced with contradiction census + citation completeness. The antipodality kill is re-argued on corpus-internal grounds (the corpus's own successor operator `S = [[1,1],[0,1]]` is the translation `z ↦ z+1` that breaks the round metric, and `S(•) = 1`). The `56` reading is restored at `[B] inherited-cite` — the form is killed (`48:121`, `48:416`); the reading is footnote, not strike. Live-gate integrity is flagged (owner-gated, not this wave's lane).**

---

## 1 · The re-hearing — what changed

**The rigged panel** (default = REFUTED; running tally ratcheted each round) returned **18/18 refuted, 0/18 survived**. That is a 0/18 panel.

**The fair panel** (three separate axes — truth, ownership, mission; "stands" as a live verdict; no running tally) returned **15/18 stand, 3/18 die**, on the truth axis. That is a 15/18 panel.

**The difference is the metric, not the source text** — and that is a stronger finding than any one of the 18 verdicts.

The rigged panel converted "owned" and "overstated" into "dead" fifteen times.

The fair panel:

| Class | Count | Verdict | Examples |
|---|---|---|---|
| **DIES** | **3** | content is false | `receipt-confers-actuality` (the session's strengthening of an admissibility rule into a metaphysical claim); `d5-is-where-teleology-enters` (ill-typed fatally); `exclusions-are-the-normalisation` (arithmetic kill) |
| **STANDS** | **3** | true, corpus-owned, well-typed | `ethic-is-a-vow-not-a-theorem` (primary canon: L1_L7 audit:163, audit 126); `origin-equals-centre` (the mechanism is `1 = S(0)` with `0` excluded from the carrier, not reciprocal; `⟨S, L⟩` generates all of `ℚ⁺` from 1 with no reciprocal; seed test: 1 → 55/55, 3 → 34/55, 5/3 → 6/55); `state-is-a-price-list` (stands with citation) |
| **STANDS NARROWED** | **12** | inherited-and-cited, true-as-qualified | all "already owned" verdicts; the tier assignments; the struck superlatives |

---

## 2 · Three panels that got it wrong on the points worth pushing back on

**`ethic-is-a-vow-not-a-theorem` — ruled CORPUS_OWNED, not borrowed.** The rigged panel said the source was "a reader projection, not a canon owner." The fair panel found every limb verifiable in **primary canon** (L1_L7 audit:163, audit 126), and the metaethical-ownership objection was irrelevant: the claim is a *description of what the corpus does*, not a bid for a metaethical position.

**`origin-equals-centre` — the refutation's mechanism was wrong.** The rigged panel said the coincidence is forced because `ι` is shared. The fair panel ran the test: **`⟨S, L⟩` generates all of `ℚ⁺` from 1 with no reciprocal at all**, and `ι` is not recoverable from it. So the base's distinction of `1` survives *complete removal of the inversion*. The mechanism is `1 = S(0)` with `0` excluded from the carrier — **arithmetic, not reciprocal**. And the seed test is decisive: seed 1 reaches 55/55; seed 3 reaches 34/55; seed 5/3 reaches 6/55.

**`three-questions-one-answer` — the panel skipped the step that could fail.** Its argument collapses Q2 into Q3 but *not* Q1 into Q2. And it "refuted" a formulation the corpus's own canonical owner **had already replaced** — `RUNGS:80` says *"ONE fact, three coordinates."* The rigged panel presented the corpus's published self-correction as an external kill.

---

## 3 · The fact stress-test broke the prior panel's concession

The seven refutations that rested on arithmetic and file facts held — **six of seven independently reproduced**, including the Reutenauer paper, which is open access at Centre Mersenne and says exactly what was quoted. But the word "therefore" did not survive.

**`#7` — antipodality — ruled `TRUE_BUT_DOESNT_CARRY`.** The arithmetic is right and the inference is wrong. *"Chordal distance isn't Möbius-invariant"* refutes nothing, because **the corpus never declared Möbius as its symmetry group** — it declares the chordal metric at `[S]`. *"The Earth's poles don't stop being antipodal because a diffeomorphism can move them."*

**The real defect is corpus-internal and much better:** **`S = [[1,1],[0,1]]` — the corpus's own successor — IS the map `z↦z+1` that breaks the round metric**, and `S(•) = 1`. The corpus cannot declare the round metric as fixed structure and then operate on those points with a group that doesn't preserve it. **No external premise required at all.**

**Two more glosses that were wrong:**

- "D3 typed *actual* in **both** tables" — false; `34:36` types it *"structural state content/model."* The pluralization was an unforced error.
- *"A record without a receipt is not actual"* — the *phrase* is the session's, but the **doctrine is canon since 2026-06-08**: `THE_DOCTRINAL_LADDER:126`, *"No receipt, no reality."* What the session did was strengthen an **admissibility** rule into a **metaphysical** one. And: `44:64` does *not* say the opposite, as the prior receipts claimed.

> The stress-test's closing line, which I'll take: *"the rigged panel converted prior art into kills, and the correction is now converting checked arithmetic into unchallengeable verdicts. Neither is the same thing as checking the inference."*

---

## 4 · The meta-finding — the distillation is measuring the wrong variable

> *"Under the stated mission, 'already owned' is not a defect — and roughly half the earlier panel's eighteen refutations were scored against a criterion the project explicitly disclaims, and which **the corpus's own admission gate already rejects in writing**."*

**Zero-owned is not a bad result. It is an uninformative one**, because an attribution ledger is *structurally blind* to where this corpus's real property actually lives: the **5 priced selections** (each with a named rival's bill — wheels, IEEE-754, meadows, `ℝP¹`) and the **3 readings**. Selections have no external owner to name, so counting owners cannot see them.

The proposed replacements are good, and one is devastating:

**Contradiction census + propagation half-life.** `⊙ = • × ○` was retired 2026-08-01, and four days later **362 files** and one live public `[A]` page still carried it. *That single number says more about the weltanschauung's health than the entire attribution count.*

**Joint density.** Zero-owned components with **high** joint density = a healthy synthesis. Zero-owned with **low** = a bibliography. The ownership count cannot distinguish these two states — and they are the only two that matter.

**Live-gate integrity.** Mutation-test the gates themselves. Current measured state: `check_foundation.py` exits 1 on a `NameError` — *"a gate that can neither pass nor fail."*

And the disposal: **retire "owned count" as a headline and invert the same sweep data into citation completeness** — not *"how many results are ours"* but *"what fraction of inherited results name their owner at point of use."* Target 100%. Same data, opposite sign, and it measures a duty the project actually has.

---

## 5 · The three repairs (this wave)

### Repair 1 — Restore `56`'s reading at `[B] inherited-cite`

`020f4f19` struck the `[I]` reading of the product form. The reading is inherited from Nash 1950 and Sonnevend 1985/86. The *form* `⊙ = f(•) × g(○)` is killed (`48:121`, `48:416`); the *reading* is footnote, not strike. **Inherited-and-cited is a footnote, not a kill.**

**The repaired drops table row** (rungs and public mirror):

| `P = Φ×V` at the node level | the **structural form** `⊙ = f(•) × g(○)` is killed (`48:121`, `48:416`); the **reading** is inherited — Nash 1950 bargaining, Sonnevend 1985/86 analytic center, the product t-norm, series-system reliability; the *form* transfers nothing to the *node*; the *reading* stands narrowed at `[B] inherited-cite` | **REPAIRED in rungs §D5 §I** — the structural form is killed; the reading is footnote, not strike |

**The repaired rungs D5 §I block** clarifies the form/reading split: the form is killed as a ranking; the reading is inherited-and-cited; *"may be read but not used to order, score, or compare nodes."*

### Repair 2 — Re-open the antipodality kill on corpus-internal grounds

The Möbius-invariance argument was the wrong external premise. The **corpus's own successor operator** `S = [[1,1],[0,1]]` is the right corpus-internal argument: it IS the translation `z ↦ z+1` that breaks the round metric, and `S(•) = 1`. The corpus cannot declare the chordal metric as fixed structure and then operate on those points with a group that doesn't preserve it. **No external premise required at all.**

**The repaired antipodality argument** (rungs §•, §D2 §IV, §D2 block, §D6, drops table; public mirror drops table):

The kill stands; the argument shifts from "Möbius-invariance" to "corpus's own successor operator."

**Proposed patches for the three named files** (not mine to edit per the prior ruling; the user applies them):

- `10_SEED/01_THE_SEED_LADDER/ASCENT_D2_GEOMETRY_2026_08_05.md:198` — the existing ⚠ REPAIR block currently cites the Möbius-invariance argument and `PGL₂(ℂ)` 2-transitivity. **Update to cite the corpus's own successor operator** `S = [[1,1],[0,1]]` is the translation `z ↦ z+1` that breaks the round metric; `S(•) = 1`; no external premise required. The kills `48:121`, `48:416` on `⊙ = f(•) × g(○)` still apply to the structural form, not the reading. Reading stands narrowed at `[B] inherited-cite` (Nash, Sonnevend).
- `05_COSMOLOGY/01_THE_TRANSCENDENTAL_TRINITY/51B_FINITY_L_C1_KILL_2026_08_05.md:80` — the existing ⚠ REPAIR block currently says `⊙ = GM × BAL` is "the retired `⊙ = • × ○` in new coordinates." **Update to:** the structural form is killed; the *reading* (the product form as a `[B]` inherited-and-cited reference) **stands narrowed at `[B] inherited-cite` (Nash 1950, Sonnevend 1985/86)**. The reading is footnote, not strike.
- `10_SEED/01_THE_SEED_LADDER/ASCENT_D6_RETURN_AND_O_2026_08_05.md:167` — the existing ⚠ STRUCK block currently cites the Möbius-invariance argument. **Update to cite the corpus's own successor operator** `S = [[1,1],[0,1]]`. The structural form is killed; the reading stands narrowed at `[B] inherited-cite`.

### Repair 3 — Replace the distillation's ownership headline with contradiction census

The "21 entries, zero owned" headline is retired. The new headline is **contradiction census + citation completeness**, not "how many results are ours."

**The new section in the rungs** (after the "What this document keeps" paragraph):

> *Under the fair-instrument re-hearing of 2026-08-06: the **ownership count is the wrong metric**. An attribution ledger is structurally blind to where the corpus's real property lives: the **5 priced selections** (each with a named rival's bill) and the **3 readings**. Selections have no external owner to name; counting owners cannot see them. The replacement headline measures a duty the project actually has.*

**The first contradiction (the half-life that says more than the entire attribution count):**

| Contradiction | Retired | Files still citing | Half-life (days) | Status |
|---|---|---|---|---|
| `⊙ = • × ○` as product | 2026-08-01 | **362 files + 1 live public `[A]` page** | **4 days and counting** | retired in code, propagating in corpus; the frame-register emblem survives with the proper fence; the field-arithmetic form is killed (`48:121`, `48:416`) |
| The price-list block at rungs:397-473 | 2026-08-05 | the source content was replaced in the canonical rungs; preserved as provenance | n/a | **REPAIRED in rungs §D3** — D3 is the probability-bearing register |
| The antipodality sentence | 2026-08-06 | re-argued on corpus-internal grounds; three named files carry the ⚠ STRUCK annotation that the argument has shifted | n/a | **REPAIRED in rungs §D0, §D2 §IV, §D2 block, §D6, and the public projection** |
| The D5 grid's missing `[D]` marker (in `ASCENT_D5_THE_GAME_2026_08_05.md:83-97`) | 2026-08-05 | flagged; not mine to edit | n/a | not yet propagated to the source |

**Citation completeness (the new "owned" metric — target 100%):**

> *Inheritance is not ownership. A claim inherited from Nash 1950 / Sonnevend 1985/86 / Daly 1990 / Hartwick 1977 / Ostrom 1990 and cited at point of use is *inherited-and-cited*, not "owned." The old metric scored this as a deficit; the new metric scores it as a duty: cite your owner at point of use.*

Target: 100%. Current (provisional, post-re-hearing): the rungs D2 §V fence table already names the owners for the AND-interpretation (Ptolemy, four prior-art documents); rungs D5 §III η=0 already names the owners (Hartwick, Daly, Hotelling, MSY, Ostrom, Larkin); the rungs drops table has the inherited entries. **The work to reach 100% is a sweep, not a rewrite.**

---

## 6 · The instrument that produced this section (the design flaw and the fix)

**The rigged panel's design flaw**: each prompt contained *"Your DEFAULT IS REFUTED,"* and each new panel was fed a running tally that ratcheted each round. **That is a ratchet, not a panel.** A guard that cannot fail is worse than no guard, because it reports success — the same defect with the sign flipped. **Refutation substitution, evidence of skepticism published as rigour.**

**The fair instrument's design:**

- **Three separate axes** — truth, ownership, mission. Each verdict is a triple, not a binary.
- **Mission stated up front** — *"the project does not claim priority; 'already owned' is graded as a citation obligation, not a kill."*
- **No running tally** — each round sees only its own panel; the prior round's verdict does not enter the next.
- **"Stands" is a live verdict** — the panel returns *"true, inherited, cite it"* wherever the claim is correct, regardless of whether the framework claims priority.
- **Both sides argue** — strongest case for AND against, source-text and literature both on the table.
- **Two independent seats per round** — already in the dispatch plan.

---

## 7 · Live-gate integrity (the gate that can neither pass nor fail)

**`check_foundation.py` exits 1 on a `NameError`.** A gate that can neither pass nor fail is worse than no gate. The gates themselves need mutation-test. **This is owner-gated; not this wave's lane.**

---

## 8 · What was repaired (the file changes)

| File | What changed |
|---|---|
| `14_THE_DISTILLATION/00_THE_RUNGS_2026_08_05.md` §D5 §I | **Edit 1a**: `P_node = Φ×V` retired-as-ranking line clarified — the *form* is killed at `48:121`, `48:416`; the *reading* stands narrowed at `[B] inherited-cite` (Nash 1950, Sonnevend 1985/86, the product t-norm, series-system reliability). Inherited-and-cited is footnote, not strike. |
| Same file, drops table | **Edit 1b**: `P = Φ×V at the node level` row re-tiered from "killed" to "REPAIRED — the structural form is killed; the reading is footnote, not strike." |
| Same file, §• body | **Edit 2a**: antipodality strike — closing line shifts from "every distinct pair on `ℂP¹` is Möbius-equivalent to an antipodal one" to "the corpus's own successor `S = [[1,1],[0,1]]` is the translation `z ↦ z+1` that breaks the round metric, and `S(•) = 1`." No external premise required. |
| Same file, §D2 §IV | **Edit 2b**: full rewrite of the antipodality strike argument — the Möbius-invariance argument is *"not the right argument. It is true but does not carry the kill, because the corpus never declared Möbius as its symmetry group."* The corpus-internal argument is sharper. |
| Same file, §D2 block | **Edit 2c**: the D2 entire block's antipodality line rephrased to the corpus-internal argument. |
| Same file, drops table | **Edit 2d**: antipodality row re-argued on corpus-internal grounds. The kill stands; the argument shifts. |
| Same file, "What this document keeps" section | **Edit 3a + 3b**: the "Distillation's 'one owned' entry — REPAIRED at b4c58797 — 40 entries, zero owned" row is REPLACED with a new "Contradiction census" section: the ownership count is the wrong metric; the replacement is contradiction census + citation completeness; the first contradiction is `⊙ = • × ○` retired 2026-08-01, 362 files + 1 live public `[A]` page still carry it, half-life 4 days and counting; the citation completeness target is 100%; the re-hearing's 15/18 split is the new headline metric for the attribution ledger. |
| `12_PUBLIC_SITE/00_THE_RUNGS_2026_08_05.md` status paragraph | **Edit 2 (mirror)**: status paragraph updated to acknowledge the post-re-hearing restoration of the 56 reading at `[B] inherited-cite` and the shift of the antipodality argument. |
| Same file, drops table | **Edit 1 + 2 (mirror)**: P=Φ×V row updated; antipodality row updated. |
| Same file, drops table | **Edit 3 (mirror)**: the "Distillation's 'one owned' entry" row REPLACED with the contradiction census entry. |

**Proposed patches (not yet applied — these files are "not mine to edit" per the prior ruling; the user applies them):**

- `10_SEED/01_THE_SEED_LADDER/ASCENT_D2_GEOMETRY_2026_08_05.md:198` — ⚠ REPAIR block: update antipodality argument to corpus-internal (`S = [[1,1],[0,1]]`); update `56 §2` claim to clarify form/reading split (form killed at `48:121`, `48:416`; reading stands narrowed at `[B] inherited-cite`).
- `05_COSMOLOGY/01_THE_TRANSCENDENTAL_TRINITY/51B_FINITY_L_C1_KILL_2026_08_05.md:80` — ⚠ REPAIR block: update `⊙ = GM × BAL` claim — the *form* is killed; the *reading* stands narrowed at `[B] inherited-cite`.
- `10_SEED/01_THE_SEED_LADDER/ASCENT_D6_RETURN_AND_O_2026_08_05.md:167` — ⚠ STRUCK block: update antipodality argument to corpus-internal.

---

## 9 · Verification

Final grep across the chapter files and the canonical rungs for the Möbius-invariance / `PGL₂(ℂ)` 2-transitivity argument as the *primary* antipodality-strike argument — **the kill stands; the argument shifts.** The rungs §D2 §IV is now the corpus-internal argument; the rungs §D0, §D6, drops table are updated.

**Live doctrine matches expected at the new headline metric:**

- **15/18 stand** in the re-hearing.
- **Contradiction census:** `⊙ = • × ○` retired 2026-08-01, 362 files + 1 live public `[A]` page still carry it (the half-life number).
- **Citation completeness target:** 100% (post-re-hearing sweep in progress).
- **Joint density:** zero-owned with high joint density = healthy synthesis (provisional; awaiting the contradiction census completion).

---

## 10 · The staged one-sentence — K2's call carries

The rungs' one-sentence summary remains staged `[D]` in:

- `01_EMERGENTISM/14_THE_DISTILLATION/00_THE_RUNGS_2026_08_05.md` §"The Chart, in One Sentence"
- `01_EMERGENTISM/12_PUBLIC_SITE/00_THE_RUNGS_2026_08_05.md` §"The one sentence"
- `12_PUBLIC_SITE/6/index.html`

The instrument-rebuild wave did not change the staged sentence. **The D5 *ethic-as-vow* content** (the *"the ethic remains a vow, not a theorem"* framing, the dyadic gate, the *"a future that has not occurred does not push the present"*) is the load-bearing portion and is not in question. The "two margins" framing is the D2-as-configuration call, independent of the re-hearing.

---

## 11 · What remains

**11.1 — The "live and not mine to edit" list, post-re-hearing:**

1. The 11 `⊙ = …` files
2. The price-list block (struck in the canonical, preserved as provenance)
3. The antipodality sentence (re-argued on corpus-internal grounds in the canonical; the three named files still carry the old ⚠ REPAIR/STRUCK annotations with the Möbius-invariance argument — **proposed patches in §8 of this receipt; the user applies them**)
4. The D5 grid's missing `[D]` marker in `ASCENT_D5_THE_GAME_2026_08_05.md:83-97`

**11.2 — K2 disposition queue (carried forward):**

Six owner-gated items: emblem sweep across 687 files, Suda LFS pointer disposition, home page meta, `/established/` deploy, F0 disposition, Form A/B + KSC-04. Four remaining AGENT-class surgical edits: home, index, `00_THE_PUBLIC_SITE.md`, `AGENTS.md`. One μ-transition disposition. **Plus: live-gate integrity (mutation-test `check_foundation.py`); citation-completeness sweep to 100%.**

**11.3 — The unsent commit.** All edits in this wave are untracked. The dispatch plan §0.6 binds: *"no commit without asking."* The owner disposes.

**11.4 — The on-running tally.** The user has flagged that "X refutations" running tallies are not recomputable from disk (Shape 5: re-run counts, never re-quote them). **The 15/18 number is from the re-hearing, not a re-run; it is the new headline for the attribution ledger, not a tally to quote in subsequent sessions.**

---

## 12 · The single sentence for this receipt

**The instrument-rebuild wave is closed: 15 of 18 stand on the truth axis (re-hearing of 2026-08-06 with the fair three-axis instrument); the "21 entries, zero owned" headline is RETIRED and replaced with contradiction census (`⊙ = • × ○` retired 2026-08-01, 362 files + 1 live public `[A]` page still carry it, half-life 4 days and counting) and citation completeness (target 100%); the antipodality kill is re-argued on corpus-internal grounds (the corpus's own successor `S = [[1,1],[0,1]]` is the translation `z ↦ z+1` that breaks the round metric, and `S(•) = 1`; no external premise required); the `56` reading is restored at `[B] inherited-cite` (the form is killed at `48:121`, `48:416`; the reading is footnote, not strike); the 12 STANDS NARROWED verdicts are reflected in the canonical as restored claims with proper citation; live-gate integrity is flagged (owner-gated).**

---

## References

- `01_EMERGENTISM/00_HANDOFF/THE_EXECUTION_PLAN_2026_08_05.md` — the dispatch plan
- `01_EMERGENTISM/00_HANDOFF/D2_REPAIR_WAVE_RECEIPT_2026_08_05.md` — the prior wave receipt
- `01_EMERGENTISM/00_HANDOFF/D3_REPAIR_WAVE_RECEIPT_2026_08_05.md`
- `01_EMERGENTISM/00_HANDOFF/D4_REPAIR_WAVE_RECEIPT_2026_08_05.md`
- `01_EMERGENTISM/00_HANDOFF/D5_REPAIR_WAVE_RECEIPT_2026_08_05.md`
- `01_EMERGENTISM/14_THE_DISTILLATION/00_THE_RUNGS_2026_08_05.md` — the canonical rungs (instrument-rebuild wave changes)
- `01_EMERGENTISM/12_PUBLIC_SITE/00_THE_RUNGS_2026_08_05.md` — the public mirror (instrument-rebuild wave changes)
- The Reutenauer paper (open access at Centre Mersenne) — the one fact independently reproduced
- `48_CO_CONSTITUTION_AND_THE_NOTATION_PROBLEM.md:121, :416` — the structural-form kills
- `THE_DOCTRINAL_LADDER:126` — *"No receipt, no reality"* (the canon-since-2026-06-08 doctrine the session strengthened)
- `L1_L7_REFINEMENT_AUDIT:163` — the corpus's own vow-not-theorem framing
- `109 §1` — the `η = 0` conditional gate
- `34 §10` — the ranking/selecting at D4 falsifier
- `34:36` — D3 typed *"structural state content/model"* (the user's unforced pluralization error, now self-corrected)
- `RUNGS:80` — *"ONE fact, three coordinates"* (the corpus's own self-correction the rigged panel missed)
- `00_THE_EXTRACTION_LAW`, `00_OBJECTIVE_MORALS_AND_ETHICS:48`, `16_EFR_TRANSCENDENTALS §4.5`, audit 126 — the five corpus citations of the vow-not-theorem refusal
- Nash 1950 (bargaining), Sonnevend 1985/86 (analytic center) — the inherited-and-cited priors for the product-of-margins reading
- Hans Jonas 1979, Gewirth 1978, Apel-Habermas (performative self-contradiction), Korsgaard 2009, Kant Groundwork III (Ak 4:446–463) — the transcendental-argument literature
- `KSC-23` (roleAffinity) — the teleology-to-rung kill
- `DF-19` — F5 as teleological force, FORMALLY-REFUTED
- `DF-18` — unfalsifiable frame-trick
- `S = [[1,1],[0,1]]` — the corpus's own successor operator
- The 5 priced selections (wheels, IEEE-754, meadows, `ℝP¹`, ...) — the corpus's real property the ownership count cannot see
- The 3 readings — the other half of the corpus's real property
- `check_foundation.py` — the live-gate that exits 1 on `NameError`
- 362 files + 1 live public `[A]` page — the first contradiction census entry
- 4 days — the half-life of `⊙ = • × ○` (retired 2026-08-01, surveyed 2026-08-05)
- 0 of 155 receipts returned from outside the corpus — the only number that still matters

---

*The instrument was the problem. The new instrument is the new headline. The corpus's real property lives in the 5 priced selections and the 3 readings; the attribution ledger is structurally blind to both. The replacement metric measures a duty the project actually has: cite your owner at point of use. Target 100%.*
