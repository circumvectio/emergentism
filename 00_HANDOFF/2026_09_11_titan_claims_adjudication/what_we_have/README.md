---
type: established-form
title: "What we have — the four seats and the emblem, at the tier each actually holds"
date: 2026-09-11
status: "[D] STAGED — UNSIGNED. Creates no doctrine, promotes nothing, signs nothing. The owner disposes."
evidence_tier: "[B] the 21-agent pass and every command re-run at HEAD a842d41b; [A] the inherited mathematics as attributed; [S] the framework consequences; [I] the readings"
owner: "Yves R. Burri — K2. No semantic authority is created here."
may_sign: false
may_authorize: false
authority_effect: none
method: "5 seats × (write → two adversarial lenses → revise conceding in the text) + one re-check pass that ran a command against all 34 [A] lines. 21 agents, 0 errors."
---

# What we have

`•  ⊙  ○`

The adjudication established what is *not* ours. This establishes what stands —
per seat, at the tier it actually holds, with an owner named for every line and a
command that was **actually run** against every `[A]`.

## The one-paragraph answer

**We have a typed boundary vocabulary, a body of correctly-attributed inherited
mathematics, and a working immune system.** We do not have a theorem of our own,
an ontology, an ethics, or a fact about the world — and every place where the
corpus reached for one is recorded here as a grave, a staging, or a debt.

## The numbers

| | |
|---|---|
| `[A]` lines across five seats | **34** |
| re-checked by a command that was run | **34** |
| survived at `[A]` | **29** |
| **demoted** | **5** |
| corpus quotations located with `/usr/bin/grep` before use | 437 |
| governing fences named | 86 |
| items recorded as **not had** | 104 |
| adversarial findings conceded *in the text* | 133 |
| attacks rejected with a stated reason | 18 |

**Every one of the five demotions has the same shape:** a sentence bundling a
re-runnable proof with clauses no command can bear on, where the unverifiable
clauses borrow the proof's certainty. That is this corpus's own named defect,
found operating inside its own established lines.

## The five seats

| | file | what it holds |
|---|---|---|
| `•` | [01_GROUND.md](01_GROUND.md) | The ground seat. Five `[A]` lines — **none of which is a fact about the seat**: two dated measurements of our own instruments, three inherited analytic facts. The seat itself is `[I]`. |
| `⊙` | [02_REALM.md](02_REALM.md) | The realm. Eight `[A]` lines. The seat reversal is **staged and undisposed**; *which set `⊙` names is unruled* — `ℕ⁺` and `ℝ₊` are distinct at `[A]` and the corpus has never chosen. |
| `1` | [03_UNIT.md](03_UNIT.md) | The unit. Six `[A]` lines, four separate results that must never be fused. **"No throne"** — the word is withdrawn. |
| `○` | [04_HORIZON.md](04_HORIZON.md) | The horizon. Seven `[A]` lines. Absorption is Cantor's, is inherited, and is **analytic** — its kill cannot fire, which is evidence of analyticity, not of survival. |
| `• ⊙ ○` | [05_EMBLEM.md](05_EMBLEM.md) | The emblem whole, the selection and its published price, and **what we do not have**, stated so it cannot be softened. |
| | [00_RECHECK.md](00_RECHECK.md) | All 34 `[A]` lines, the command run against each, the result, and the five demotions. |

## What the pass corrected in its own authors

Recorded here rather than repaired silently, because the correction is the result.

1. **"The equator is not a throne."** Already on disk at
   `13_BOOKS/the_reciprocal/08_the-sphere-and-the-balance.md`. The word "throne"
   entered this work from the session, not the corpus, and performs exactly the
   fusion the four-way fence forbids. **Withdrawn.**
2. **The firewall is a linter, not a type checker.** `titan_arithmetic_matches`
   matches a Titan glyph adjacent to an arithmetic operator. *"It has a seat,
   therefore it accounts"* contains neither and **passes green**. The earlier
   claim that the typing makes that fusion "a type error a gate can reject" is
   **withdrawn**; what stands is a refusable *notational* discipline.
3. **A live `[A]` error — and this session propagated it before catching it.**
   *"Seeded off-centre, the construction reaches `ℚ⁺ \ {1}`"* is **false as
   universally stated.** Independently re-verified here by exhaustive
   breadth-first search over 400,000 reachable values: it holds for seeds `2` and
   `1/2`, and **seeded at `3` the construction never reaches `2`**. The sound half
   — the unit is reachable only from itself — survives and is what the fence now
   carries. It still stands at `[A]` on disk at
   `14_THE_DISTILLATION/00_THE_RUNGS_2026_08_05.md`,
   `ASCENT_D1_ARITHMETIC_2026_08_05.md` (which prints *"Seed it at `3` and it
   reaches 19"* directly above its own contradicting conclusion), and
   `FAIR_RE_ADJUDICATION_RESULTS_2026_08_06.md` row 17, stamped *"**STANDS** …
   Correct, registered `[A]`."* **Open gate failure at those owners.** It also
   entered `47_THE_EMERGENCE_OF_FINITY.md` §5 in yesterday's four-way fence, cited
   from the bad owner without being tested — **repaired 2026-09-11**, and recorded
   here rather than quietly fixed.
4. **KSC-28's chart-locality kill has no enforcing gate.** Verified this pass:
   `/usr/bin/grep -rln 'omits its chart' --include='*.py' 09_TOOLS/` exits 1.
   `check_foundation.py` does carry a `chart-local` **presence** check — it
   confirms the phrase appears in named documents — but presence of the rule is
   not enforcement of its kill (*"an arithmetic claim omits its chart"*, located
   verbatim in the registry). The rule is real; nothing tests conformance to it.
5. **The census instrument under-reported three times.** Formula literal → 10
   files; `"Dedekind 1888"` → 12; the seat pass found 2 more, including an
   internal contradiction the first sweep itself created. **Actual: 14.** A census
   built from one string is not a census.
6. **The `Set` fence is now the most frequent `NoCoercion` line in the corpus**
   (10 + 1 comma-free), not the missing one — because R4 landed hours earlier. An
   earlier draft of the ground seat printed *"×2 — both in the ledger"*; false of
   this tree, corrected in place. **A measurement is true of a date.**

## What this page does not do

It promotes nothing. `00_ESTABLISHED/README.md` is explicit — *"this folder is not
a promotion path"* and *"a claim that is merely true is not admitted"* — and
nothing here has been admitted there. No register row was opened (schema-blocked;
the owner rules on the bucket). No ruling was signed. `check_foundation.py` is red
for ten pre-existing reasons unrelated to this work, so **no fence on this page has
its enforcement demonstrated.** World contact remains zero.

`•  ⊙  ○` — *the seats are the questions the frame must settle, not entities within it.*
