---
type: session-handoff
title: "Public Wisdom projection — authorized takeover and integration"
date: 2026-09-06
status: "INTEGRATION REVIEWED; POST-COMMIT CUSTODY REPLAY REQUIRED"
evidence_tier: "[B] bounded local checks; [S] publication and authority separation"
may_sign: false
may_authorize: false
---

# Public Wisdom takeover

The owner's “Yes” authorized taking over the pending Public Wisdom revision,
reconciling its failures, and completing the requested commit and push. It did
not authorize deployment. This supplements, rather than rewrites, the
[earlier recap](../2026_09_06_recap_and_push/README.md).

## Custody and scope

Checkout: `01_EMERGENTISM`; branch: `theory/parasite-load-2026-08-17`;
base: `c33b293898ad4b49238604edda57da2775325069`.

Fresh L1 inventory matched the previously reviewed 77-path candidate exactly:
65 tracked dirty paths and 12 release additions, with no staged paths or
observed intervening package changes. This is not proof of exclusive access.
L2 retained the dependent site package as one reversible integration; L3
reviewed the defects and release boundary; L4 alone edited the files.

[PATHSET.json](PATHSET.json) binds the resulting 80 implementation paths:
the authorized 77 plus the node-ranking checker, its test, and the
contact-limited census test. The receipt files are separate metadata. Staging
uses explicit paths and checked hashes, not a harvest of the worktree.

Five local `.hermes` plans and the earlier browser captures remain untouched
and excluded. New browser captures remain local under the existing output
lane. They are not required public assets. No reset, stash, broad deletion,
ignore-rule masking, branch change, or historical-route revival was used.
The prior design contract survives byte-for-byte in the declared archive:
SHA-256 `a92bbcd377102be866400fc5af47784bf064324f98c363b21eee6bb3b3409f23`.

## Repairs

- Adopt the pending three Wisdom pages, shared theme/navigation, design-v2
  contract, deterministic builders, machine exports, and lifecycle census.
- Keep application coverage, adoption, and record maturity separate in RAG;
  derive record state and export source IDs, repository commits, paths and
  hashes. Source bodies and private evidence are not copied into public data.
- Preserve the frozen source packet, its Provisional state, zero independent
  outcomes and zero product adoptions. Public visibility does not upgrade it.
- Refresh the reviewed Kernel binding and replace obsolete Burrisphere hero
  literals with the current one-sphere/two-reciprocal-charts explanation.
  Retain the mathematical and evidence fences; test accessible controls rather
  than obsolete pixel coordinates.
- Hide appearance controls until their handlers are installed. System theme
  remains usable without JavaScript or storage. Give Wisdom headings an
  explicit responsive type scale; preserve a full first action at 320×568.
- Recognize exactly one complete conditional formula-name warning in the
  node-ranking checker. Hard-wrapped equivalents pass; adjacent assertions,
  active brand rankings, and weakened conditions fail. The economic doctrine
  itself was not edited or validated.
- Reconcile literal census test expectations with the already-receipted
  September 1 census; do not relax mutation checks or erase owner-held debts.

The purported missing Reciprocal source was a relocation. Strict replay uses
the existing `EMERGENTISM_PRIMARY_CHECKOUT_ROOT` configuration, finding the
declared sibling source by exact content digest:
SHA-256: `86b59d4f3e4ad8ec64e85fb1b075ac986953b3c28339eda1046459789696a1f9`.
No metadata-only waiver, source rewrite, or hash substitution was used.

## Local evidence before the implementation commit

- 70 focused site unit tests pass: Public Wisdom, Gestalt, living map,
  decision transaction, Burrisphere instrument and operators.
- All seven node-product tests pass, including the live active-corpus scan
  and adversarial warning controls.
- All 103 contact-limited mutation tests and 13 Public Wisdom source tests
  pass; the census remains exact and the source packet remains unchanged.
- Core shell, dimensions, book, PWA, design constitution, semantic parity,
  and all nine default site-artifact checks pass. Artifact checks were replayed
  after regeneration; the service worker was fingerprinted last.
- Full predeploy completed with 42 failures, all in the explicit HEAD-custody
  section for the uncommitted package; the other 17 sections passed. A new
  full run after committing is required, not presumed.
- Chromium checked four routes (`/`, `/wisdom/`, its `@1` dossier, and its
  record) at 1440×1000, 768×1024, 390×844 and 320×568, plus all four at 320×568
  without JavaScript: 20 passing cases, no page errors, no external requests,
  no document overflow, one h1, and visible 48px Exit access.
- Native no-JavaScript menus, desktop theme cycling, keyboard activation,
  Escape focus return, storage-denied theme cycling and live system-theme
  changes were checked. The latter test needed to await the media-change
  event; an immediate read was not a reliable assertion. Reduced-motion
  contexts were used. This is local QA, not accessibility certification.
- `git diff --check` passes. Later commit and push identities belong in the
  subsequent validation receipt, not a prediction in this one.

Rosetta's pre-existing frozen-A3 logging HOLD is preserved. No writer was
retried, reset, amended, or bypassed. Rosetta exposed actual export defects;
agent agreement supplies neither truth nor authority.

The previous repo-wide gate run reported other source/purity/WIP/receipt
debts and did not complete. This bounded integration does not claim that
entire gate is green. The full public predeploy gate is a separate check.
Future multi-record/successor Wisdom releases must update the human-page
state and routing contracts; today's fixed singleton v1 does not grant that
expansion. No model call, deploy command, DNS change, product adoption,
scientific validation, or independent outcome is part of this work.
