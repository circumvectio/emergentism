---
title: "Disposition — leftover Titan dirt, L5 proposal, staged vision, predeploy"
date: 2026-08-13
status: "DISPOSITION — staged files stay [D]; no W13; no deploy"
---

# Disposition of what was left alone

## Titan Pass `01_PRESERVE.md`

Two headings marked `{no-anchor}`. That is the Emission/Titan
citation exemption: the heading *names* a checker as data, it does
not quote an owner sentence. Kept. Not folklore.

## L5 proposal and staged vision

Both remain **unsigned [D]**. Committed as custody so they are not
orphans. Not adopted. Not W13.

Host correction (2026-08-13), then chair adjudication the same day:

- **(a) is not one [I] claim.** It splits: aim `[S]`, mechanism `[C]`
  (`W10-SPARK`, OPEN-EMPIRICAL), amrita presupposition unpaid.
- **(b) is not `[C]`.** It is `W0-COMPLETE` NOT-WELL-POSED: equivocation
  on *complete*. Successor `[I]`: any model that mistakes fluency for
  emergence, or map for Ground, has not yet sat this problem.

Live seats: `VIS-00` / `OC-00` on `/record/problems`, and the two
CLAIM_STATUS rows. Staged vision + L5 proposal remain unsigned `[D]`.

## Predeploy

- tinycss2 1.5.1 is on Homebrew Python 3.11; the Xcode 3.9 interpreter
  cannot install it. Run the gate with `python3.11`.
- Withheld hashes refreshed by `build_withholding_boundary.py` after
  the attic-banner apply.
- Public book rebuilt.
- Sitemap file URLs (`spark.md`, `llms.txt`, `*.json`) removed: the
  contact-limited contract allows trailing-slash HTML only. The files
  remain linked from `/spark/` and `llms.txt`.
- Reciprocal live Skyzai path added as a same-hash alias in
  `EXTERNAL_SOURCE_CONTRACTS`. Old path kept. Six Lenses pin still
  unpaid.

World contact 0. G10 unpaid. No host cutover.
