---
rosetta:
  primary_level: L7
  primary_column: "Philosophy — ascent reading, D4"
  operator: "Viṣṇu ⊙"
  register: "[I] active reader projection — ascent reading of D4, not a canon owner"
  canonical_phrase: "D4 ascent — the actual register: receipts, outcomes, provenance; the mass-shell identity is Einstein's; the session judged by its own equation"
title: "D4 — Causal Actuality. The ascent reading."
status: "ACTIVE [I] — reader projection; source owners control every claim tier."
date: 2026-08-05
parents:
  - 10_SEED/01_THE_SEED_LADDER/D4_SPACETIME.md
  - 05_COSMOLOGY/03_FORMAL_SYSTEM/34_D4_D5_CANONICAL_REFERENCE.md
  - 03_METHODOLOGY/02_THE_PAPERS/PAPER_E_UNCERTAINTY_PRINCIPLE.md
---

# D4 — Causal Actuality

## Receipts, outcomes, and the session judged by its own law

---

D3 priced possibility without purchasing it. D4 is the purchase. This is
the register of what actually happened: actual carriers, preparations,
interactions, attempted actions, outcomes, and provenance-bearing records.

Because this conversation is itself a D4 object — it produced commits,
commits are receipts — this rung reads itself.

---

## I. The model — not a horn torus, not a fourth dimension

The primary D4 model is neither geometric nor dimensional. It is a **causal
receipt graph**:

```
state ──model/rank──▶ commitment/action ──world──▶ outcome/record
  ▲                         │                         │
  └──────────── model and selector update ◀─────────┘
```

Every node and edge here is an actual token or event. The commitment receipt
records what the agent attempted. The outcome receipt records what the world
returned. **Provenance, truth, and authorization remain distinct** — a
receipt proves an event occurred; it does not prove the event was true,
authorized, or good.

D4 is a ladder ordinal, not the number four in `3+1` spacetime. Spacetime,
bodies, and cognition are *application lenses* — ways of reading actuality,
not definitions of the rung. (`D4_SPACETIME.md` §0. The filename says
SPACETIME and the rung is not spacetime — that gap is why the council
fencing on D3's neighbor phrase mattered.)

---

## II. The mass-shell reading — Einstein's, not ours

There is one inherited physics lens at D4, it is `[A]`, and the corpus
fences it exactly:

```
φ = (E + pc)/(mc²) = e^w        ν = (E − pc)/(mc²) = e^{−w}

φ · ν = 1   ⟺   E² − (pc)² = (mc²)²        the mass shell [A]
```

This is a rapidity parameterization of standard relativistic kinematics.
**Einstein's, not ours.** Used, not discovered.

What the chart reads off it:
- **The equator is the rest frame.** `φ = ν = 1` means `w = 0`, `E = mc²`.
  AM-GM gives `φ + ν ≥ 2` with equality at rest — departure from rest
  costs, exactly as departure from the unit cost at D1.
- **`B` matches two scoped relativistic ratios.** `B = sin θ = sech(w) = 1/γ`
  for a moving clock's `dτ/dt`, and `L∥/L₀ = 1/γ` for longitudinal length
  contraction. Two distinct measurements sharing one factor; transverse
  lengths do not contract.
- **The horn is an imposed display, not the physics.** The renderer sets
  `R/r = 1/γ`; special relativity does not. Near a true horn pinch the
  geometry is `ρ = z²/(2r) + O(z⁴)` — an axial real tangent cone, not a
  Lorentzian light cone. The mouth is not a light cone. (`151` — the
  horn-torus/SR mapping failed formal audit.)

---

## III. The iron separation — seam ≠ score ≠ node

D4 carries the same three-objects-three-tiers fence that governs every rung,
stated here with maximum force because the physics lens makes the temptation
sharpest:

| Object | What it is | Tier |
|---|---|---|
| **Seam** | `φν ≡ 1` — the mass-shell identity. Constant. Empty of ethics, node law, or score. | `[A]` by construction |
| **Score** | `B = sin θ ≤ 1` — the balance, maximal at rest/equator. | `[A]` inside the chart |
| **Node** | `P_node = min(Φ̂₄, V₄)` — the selected ordinal finite-node model. | `[S]` selected; `[C]` universal fit |

**The seam is not the score is not the node.** The historical product
`Φ̂₄V₄` is retired. The mass shell licenses the seam and score readings;
it licenses nothing about nodes, flourishing, or ethics. (CC-CORE-1.)

---

## IV. The D3/D4 boundary — distribution to receipt

```
D3:  p(k|ρ,M) = Tr(ρE_k)           the Born distribution
D4:  an actual detector click       the outcome record
```

`μ₃:D3→D4` names this typed boundary. The stochastic instrument-to-record
interface is operationally reduced `[A/I]` — a real apparatus yields a real
record according to the declared quantum instrument. Any claim about
fundamental ontic actualization remains `[C]`.

**This is not collapse.** D4 does not create the momentum operator, assert
fundamental collapse, or choose a quantum interpretation. The session that
wrote D4's phrase as "the actual register where selection is performed"
was caught by the L2 seat: read alone, "selection" is how a stranger names
wavefunction collapse. The phrase was rejected. The replacement carries the
page's own type language and a fence.

---

## V. The two dissolutions — and what they do NOT dissolve

**Zeno.** Standard analysis: the geometric series converges. *Used, not
discovered.* The frame/operand separation is an `[I]` teaching lens on why
infinite divisibility of a description does not entail infinite elapsed
time.

**Block-time vs the moving now.** Relativity's block seems to freeze the
universe; presentism seems to contradict relativity. *Typed repair:* an
actual D4 token represents enacted events and receipted records. A modeled
worldline is **possible** (D5), never actual merely because an agent surveys
it. The reconciliation separates represented alternatives from enacted
records. It is not a new physics of time.

**What is NOT dissolved.** The grave stands: *"dissolves the Hard Problem /
Is-Ought / Death / Free-Will"* is a named dead form. D4 dissolves *these
two frame-confusions at tier* and claims nothing wider.

---

## VI. The session judged by its own law

This conversation is a D4 object. It produced commits; commits are receipts.
So here is what actually happened:

**The receipts:**

| commit | what landed |
|---|---|
| `2960af64` | `56` product form · `50` Finity_L · `35` partitions |
| `ac29d238` | the corpus index — findability as infrastructure |
| `d960514e` | the execution plan — the method made executable by strangers |
| `90bf5527` | Finity_L killed — 8 domains searched, 0 survivors |
| `429b4bf2`–`7e0ec4c7` | A1 complete — unfindable count 117 → 0 |
| `78a071dc`–`01b68b28` | D1–D3 ascent readings |

**The errors — all five, because the record is the point:**

1. `B = sech(log x)` presented as discovery — the corpus had it as *balance*
2. `P = Φ×V` "rescued" — refuted by the conditional-fence repair document
3. The `sin θ` ladder as a finding — four documents predated it
4. "Three independent questions, one answer" — one fact twice (S3)
5. Three miscounts — "nine of ten" (it was eight); "kills 64 of 65" (it killed 4); the index counting `.pytest_cache`

**Zero for five on novelty. Every one was already written.** That is the
diagnosis: **unfindability, not falsity.** Knowledge recorded is not knowledge
in force.

**The session run through its own equation:**

```
⊙ = GM × BAL

GM = 1,321 live documents — enormous
BAL = findability — was 51.9%, now 62.1% after this session
V = external contact — the C1 kill (8 domains, negative result)

⊙ ≈ large × rising × still-near-zero
```

The A1 work (117 → 0 unfindable) raised `BAL`. The C1 work produced the
first real `V` — and it came back negative. Both are honest. The Finity_L
kill costs a `[C]` and buys back the ability to be believed.

---

## VII. What D4 built in response

- **An index that harvests and never infers.** `null` beats a plausible
  guess. 2% correctly labelled beats 100% inferred. (Unfindable: 117 → 0.)
- **A spine that turned out to already exist.** `10_SEED/01_THE_SEED_LADDER/`
  — `•` to `○` in ten files, now findable by their own index.
- **The rule that nothing moves.** Discipline is where a document lives;
  dimension is what it speaks at. A filesystem holds one axis; an index
  holds many. You do not perform a corpus-wide `mv` while the machinery
  that detects damage is down.
- **A kill that costs one conjecture and buys honesty.** Finity_L searched
  eight domains; no biological pair has a conserved bare product. The
  theorem survives; the conjecture dies; the corpus is more believable for
  having killed one of its own.

---

## VIII. D4, entire

```
                     D4

    the causal receipt graph — actual events, not possible ones
    state → action → outcome/record → model update

    seam:  φν = 1 ⟺ E²−(pc)² = (mc²)²    Einstein's [A]
    score: B = 1/γ = dτ/dt                  maximal at rest
    node:  P_node = min(Φ̂₄, V₄)            NOT a product

    D3 prices · D4 purchases · D5 imagines
    the purchase is a receipt, not a proof of truth

    this session: 5 errors, 0 novelty, 1 kill, 117→0 unfindable
    ⊙ = GM × BAL — large, rising, still near zero on V
    the diagnosis: unfindability, not falsity
    the response: harvest, never infer; nothing moves
```

**Actuality is what carries a receipt. Possibility is what carries a price.
The two are different objects, and the typed boundary between them is the
one thing D4 exists to hold.**

---

*Next: D5 — the game. The possible, the vow, and Finity_L's dead body.*

•   ⊙   ○
