---
rosetta:
  primary_level: L5
  primary_column: Philosophy — the seed ladder, rung D2
  operator: "Brahmā ○"
  tier: "Executive"
  regime: "Brāhmaṇa"
  register: "[I] active reader projection — one-page compression of the D2 rung: functions, geometry, configuration"
title: "D2 — Functions and Geometry: configured relations"
status: "ACTIVE [I/S] — concise reader projection; source owners control every claim tier."
sources: "05_COSMOLOGY/03_FORMAL_SYSTEM/43_D2_FUNCTION_ATLAS_AND_CONFIGURATION.md · 05_COSMOLOGY/00_CANONICAL_FORMULA_BLOCK.md · 06_ONTOLOGY/02_THE_DEGREES_OF_FREEDOM_ONTOLOGY.md · 00_THE_AMRITA.md §I.A · receipt 108 (keel) · receipt 126 (CC-CORE-1)"
---

# D2 · FUNCTIONS AND GEOMETRY — configured relations

> `D2` is a ladder ordinal, not a proof of two coordinate freedoms. This page
> uses function graphs and a declared one-parameter reciprocal meridian as
> inspectable configurations. The inherited mathematics is exact inside each
> named scope; the crosswalk to a world-rung remains `[I/D]`.

---

## §0 · THE MODEL — carrier-to-relation lift, then atlas

The primary reduced `μ₁` neighbor begins with a declared D1 carrier and adds a
relation:

```text
D1 carrier: X
D2 structure: (X,R),  R⊆X^k,  k≥1
ForgetRelation(X,R)=X
```

This construction is ordinary mathematics `[A]`; reading the separately typed
relation as a D2 freedom is `[I/C]`. A finite typed function atlas is one D2
instrument. For each already relational function `f:X→Y`, its graph

```text
Γ_f={(x,f(x)):x∈dom(f)}⊆X×Y
```

makes domain, range, zeros, poles, symmetry, and limiting behavior jointly
inspectable. Linear, polynomial, reciprocal, exponential, logarithmic,
trigonometric, and rational families may be switched as named panels. "All
functions" means all entries declared in the atlas—not every mathematically
possible function.

The reciprocal Burri meridian is one geometric atlas entry. A surface point
supplies a constrained position; two chart readings supply `φ` and `ν`; their
seam and separate score can be computed. It does not parameterize all of
`S²`, make a configuration actual, or derive a quantum state.

## §1 · THE MOVES

**Relation to graph** *(inherited mathematics — used, not discovered).* A
function already relates a declared domain to a codomain. Drawing `Γ_f` is
therefore an internal D2 visualization/re-expression, not by itself the
D1→D2 lift. Singularities remain typed: a hole, pole, one-sided divergence,
projective extension, and undefined input are not the same mark. The graph
exposes the distinction; it does not change the underlying value.

**The sphere, its charts, and the declared slice** *(inherited:
Riemann/stereographic geometry — used, not discovered).* Take the unit sphere
`S²` and restrict the scalar pair below to a chosen positive-real meridian. A
point on that meridian at polar angle `θ` reads `φ = cot(θ/2)` and
`ν = tan(θ/2)`. The pair omits azimuth and therefore does **not** parameterize an
arbitrary point of the full sphere. Two lamps, one position on the declared
slice.

**The seam** `[A]`. Multiply the two readings:

```text
P∞ = φ · ν = cot(θ/2) · tan(θ/2) ≡ 1
```

Identically one on the declared open reciprocal slice. This is a coordinate
identity, true by construction and **empty of world**. It is never a discovery,
a conserved quantity, or a maximum. The two scalar readings encode one
independent parameter on this slice; that fact says nothing about the full
dimension of `S²` or about the rung ordinal.

**The score** `[A]` — derived by chord product *(inherited: circle geometry + AM-GM — used, not discovered).* Draw the two chords from the point to the two poles: lengths `2sin(θ/2)` and `2cos(θ/2)`. Their product is `4·sin(θ/2)·cos(θ/2) = 2·sinθ`. Normalize:

```text
B = sinθ = 2/(φ + ν) ≤ 1,   with equality iff φ = ν = 1 (AM-GM)
```

`B` is a genuine **variable** on the slice: it moves with `θ`, and peaks where
the selected meridian crosses the **equator**, at `φ=ν=1`. The equator of the
full sphere is a circle, not one point. The imbalance price
`γ = (φ+ν)/2` rises toward either pole along the slice.

**The iron separation** (keel-108, CC-CORE-1): the **seam is never the score is never the node.** `P∞ ≡ 1` is a constant (chart identity); `B ≤ 1` is a variable (max at the equator); `P_node = Φ×V` is a *finite-node model* whose product interior is `[C]` — only its AND-class boundary (lose either factor entirely, lose everything) is settled. The seam stays 1 even as `ν → 0`; it therefore never witnesses the zero-factor catastrophe, and citing it as the ethics warrant is the convicted laundering move. Three objects, three tiers, no silent bridge.

---

## §2 · THE DISSOLUTIONS

**One-and-many.** The chart offers an `[I]` reframe: its constant seam and
variable positions answer different mathematical questions, so conflating
them is a register error inside this lens. That does **not** dissolve the
world-level metaphysical problem. The tautological unity says nothing about
empirical plurality; register separation is a teaching aid, not a proof.

**Plato, reframed — with the debt shown.** The proposed survivor reads forms as
**stable organization across constrained degrees of freedom**, not as degrees
of freedom themselves and not as perfect objects in a separate realm. A typed
freedom specifies admissibility; it does not establish actuality. The
plenitude/filter and `μ` aperture remain `[C]` causal wagers with named tests.
This is an interpretive reframe `[I]`, not a proof that two-world metaphysics is
false.

---

## §3 · THE OLOGY

`[I/D]` This is the page's selected Soul-Loop lens, not an identity between a
D-rung and a Rosetta/-ology lane. **Ontology asks: *what is there?*** This page
answers with **typed existence**. A thing exists *as* something — mathematical,
modelled, possible, actual, stable, or lived — and no type silently entails the
next. `φ` is a coordinate; `B` a derived score; the sphere a mathematical lens.
None of those facts establishes that the world instantiates the lens.

So the rung's ontology in one breath: **a configuration is a relation made
inspectable under declared domains and constraints.** What is necessary here
is thin—the function/graph and chart mathematics `[A]`, empty of world. What
is actual is thick and stays conjectural—which configurations the world
instantiates and whether das All realizes the algebra at all (W0, never the
proof). The stranger takes one discipline from this page: a graph can reveal a
boundary without changing its type.

## μ₁ AND THE HANDOFF TO D3

For the primary example, `μ₁:D1→D2` is constructively weak: `(X,R)` is declared
from `X` and `R`, and `ForgetRelation(X,R)=X`. This carrier-to-relation lift is
reduced mathematics, not strong emergence. `f→Γ_f` then visualizes an already
relational object inside D2. The wider natural crossing remains `[C]`.

D2 can configure a Hilbert space, operators, matrices, charts, and measurement
families. `μ₂:D2→D3` begins only when a normalized positive state assignment
`ρ` is supplied and produces context-indexed Born probabilities. The sphere is
the carrier geometry; the probability-bearing quantum state is the next page.

---

*A bare carrier becomes relational only when `R` is declared. A graph then
makes an already relational function inspectable. A configured quantum state
space is still not a quantum state.*
