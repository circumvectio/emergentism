---
title: "122 — Compass-restructure convergence: adopt the concurrent 8-wing build as canonical, feed it the corrected foundation (handoff, not edit — the other session is live) — STAGED, pending K2"
date: 2026-07-12
status: "[S] convergence decision (K2 in-chat 2026-07-12: 'Converge on theirs'). The concurrent _STAGING_COMPASS_RESTRUCTURE/ (7 wings, 'A Compass, Not a Cathedral') is adopted as the canonical site restructure. This session's duplicate 12_PUBLIC_SITE/lens/ is RETIRED. Because the concurrent session is actively writing (mtime 13:59, live), the corrected foundation is delivered as a HANDOFF spec here, NOT by editing their in-flight files."
evidence_tier: "[S] the convergence + the corrections (grounded in receipt 121's disconfirming pass); [B] the file-path evidence"
verdict_extends: "receipt 121 (the corrected foundation) · receipt 120 (launch telos) · keel-108"
owner: "K2 chose convergence. The concurrent build's session owns its files; this session does not edit them (race avoidance per the concurrent-committer discipline)."
parents:
  - ./121_THE_COMPASS_COMPRESSED_THREE_GENERATORS_AND_ARTIFACT_PENDING_K2.md
  - ../../12_PUBLIC_SITE/_STAGING_COMPASS_RESTRUCTURE/compass_landing/index.html
---

# 122 · Convergence handoff — one restructure, on the corrected foundation

## 1. The decision

Two parallel compass rebuilds existed: this session's compressed single-page
`12_PUBLIC_SITE/lens/` and a concurrent session's full 7-wing
`_STAGING_COMPASS_RESTRUCTURE/` (compass_landing · journey_mode ·
map_rosetta_wing · halahala_wing · test_falsifiers_wing · exit_k4_wing ·
build_wing; titled *"A Compass, Not a Cathedral"*). K2 ruled **converge on the
concurrent build.** It is the fuller structure, well-governed (staging
receipts, `magnum-opus-orientation` skill), and strongly embodies the framework
(51 tier markers, 15 halāhala refs, 9 falsifiers, a K4 exit wing).

**This session's `lens/` is retired** (removed — untracked, mine). The published
artifact (`claude.ai/code/artifact/b9d1e976…`) and receipt 121 preserve its
content; nothing is lost.

## 2. Why this is a handoff, not an edit

At the moment of convergence the concurrent session was **live-writing**
(build_wing/build.css mtime 13:59:23, plus many uncommitted tracked corpus
files). Per the concurrent-committer discipline (never checkout/edit in the
shared worktree while another writer is active), this session **does not touch**
`_STAGING_COMPASS_RESTRUCTURE/` or the concurrent M-files. The corrections below
are a spec for that session (or a later reconciliation once it settles) to
apply.

## 3. The corrections the concurrent build owes (from receipt 121's disconfirming pass)

The concurrent landing is mostly fence-clean already (no "governs"; H3 handles
convergence-as-proof as REFRAMED; real per-claim falsifiers). Owed folds:

1. **Drop "equality = optimum" — `compass_landing/index.html:415`.** The line
   "systems that balance complementary quantities at equality are at an
   extremum" is the "equator = optimum" claim the disconfirming pass **dropped**:
   spontaneous symmetry breaking puts emergent order at an *extremal* order
   parameter, not at balance. Reword: *the AND-class forbids only the zero pole
   of a necessary factor; it does not locate the optimum at balance.*
2. **Register-index the death claim.** Engine register `[S]`: a factor of
   P_node=Φ×V at its pole ⟹ P→0, always. Frame register `[A]`: apparent
   "emergence at a pole" is a P∞ conjugate divergence (criticality: ξ→∞) —
   non-residable, dimension-gated. Never state a flat "a pole is always death /
   never emergence."
3. **σ-scope G1.** "Both factors off their poles" holds only for *complementary*
   couplings (σ≤1); substitutes (σ>1: redundancy, failover) survive a factor at
   its pole. State the AND/OR check as a precondition, not a universal law.
4. **Keel tier on any Φ×V "engine" claim.** AND-class (need-both) is `[S]`; the
   multiplicative product is `[C]` unwon (GFS Wave-1 NOT SUPPORTED). Never label
   the product a proven law.
5. **The open negative must appear (G3 requires it non-empty):** the shared
   frame's correlated blind spot (PRISM≥2 never covers the common metric); G2
   untested; the value↔extraction/gauge-redundancy weld is carried not re-won;
   and the honest self-referential one — *if the site's own coherence and poison
   are substitutable, its AND-gate scopes out.*

## 4. Deploy-gate state (informational, not acted on)

The predeploy gate is currently RED (66 errors): 65 are the concurrent build's
in-flight broken cross-links between wings (relative paths to sibling wings not
yet wired), and 1 was this session's now-retired `lens/` orphan. Recommended
resolution when the concurrent build settles: add
`_STAGING_COMPASS_RESTRUCTURE/` to `.vercelignore` (it is staging, underscore-
prefixed, not for deploy until promoted) and fix the inter-wing hrefs. **Not
done here** — `.vercelignore` and the wings are the concurrent session's live
surface; acting now would race it.

## 5. Disposition

`[S]` convergence recorded. This session steps back from the site build per K2's
ruling; the concurrent 7-wing restructure is canonical and owes the §3
corrections before promotion. Deploy remains K2-gated; the gate must go green
(staging excluded or wings wired) first.
