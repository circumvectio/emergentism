---
title: "Receipt 186 — the torus fails cosmology worse than it failed SR; and the third mode lands"
date: 2026-07-30
status: "OUTCOME — item 7 tested and closed; items 3 and 6 written"
evidence_tier: "[A] the fit and the critical-point count; [S] the dispositions"
---

# Receipt 186

## §1 · Item 7 — the horn torus as a COSMOLOGICAL emblem: NO, and worse than before

The prior audit tested SR only. The owner observed, correctly, that *"the cone widens
exponentially in the future"* is **false of SR and true of an expanding universe** — a real
feature the corpus does not contain and that audit never examined. The test was run.

**The best-fit expansion rate is exactly zero.**

```text
a = HT      0        0.25     0.5      1        2        4        8       20       40
RMS      0.075438  0.079151 0.089294 0.120888 0.196002 0.322412 0.452131 0.548820 0.581079
max|res| 0.105257  0.127771 0.152201 0.205081 0.315700 0.505857 0.718540 0.897475 0.958197
```

Monotone increasing throughout. **Every positive Hubble rate makes the emblem fit worse — so
the family introduced to rescue it selects, as its best member, the straight Minkowski cone
the rescue existed to escape.** And even that best case leaves `max|res| = 0.1053`, **10.5% of
the full normalised range.**

**The kill is topological and lands before any residual.** `ρ` restricted to the meridian is a
smooth function on a circle, so it has **at least two critical points** — exactly two: the
pinch and the outer equator. A comoving cone has `χ′(t) = c/a(t) > 0` at every finite `t`:
**exactly zero.** `2 ≠ 0` is invariant under every reparametrisation of time and every
rescaling of reach. The prior audit's `T4`/`T5` died on a residual; this dies on a count.

**Named as precisely as the prior kill.** Where `151` said *"1 − β linear vs √(1−β²)
concave"*, this says: **sinusoid vs exponential — `W = +ω⁴` against `W ≤ 0`**, where
`W ≡ χ″² − χ‴χ′` is `+ω⁴` for any circle and `≤ 0` for any admissible cosmology.

**And the divergence is unbounded, where the prior one was not.** The ratio diverges as
`4/(π²x)` toward the apex: `4.09×` at `x=0.10`, `20.3×` at `0.02`, **`405×` at `0.001`.**

## §2 · What the owner got right — three credits, and one is against the audit `[A]`

**The physics.** In de Sitter, proper distance reached from `t=0` is `d(t) = (e^{Ht}−1)/H`:
`1.72` at `Ht=1`, `147` at `Ht=5`, `4.85×10⁸` at `Ht=20`. Genuine exponential widening. Not
in the corpus.

**The structural fork, guessed correctly.** A future event horizon exists iff `∫^∞ dt/a`
converges — for `a ∝ tⁿ`, iff `n > 1`. De Sitter and ΛCDM have one; matter and radiation do
not. ΛCDM integrated fresh (`Ω_m=0.3, Ω_Λ=0.7, H₀=70`): age **13.467 Gyr**, particle horizon
**46.17 Gly**, total comoving reach ever **62.10 Gly**, of which **15.93 Gly is still ahead**.
All literature-standard. *"The cone reaches a limit"* is correct physics.

**And a correction to receipt 151, which is owed.** That audit tested SR and concluded about
SR, but `OB-1`'s indifference to *which* spacetime is depicted was **asserted there, not
exercised**. It is exercised now — `OB-1` does hold for FLRW identically — but nobody had
checked, and phrasing "every SR reading is refuted" so that it reads as "every reading is
refuted" **was an over-reach of framing.** Recorded.

## §3 · The correction that must travel with the credit `[A]`

**Bounded ≠ saturating**, and the hypothesis conflated them. The torus attains its maximum at
**finite** parameter, with **zero** derivative there, and then **decreases**. De Sitter
approaches an asymptote **never attained**, with `χ′ > 0` at every finite `t`.

And the exponential statement is true only of **proper** distance, which is **unbounded** — so
the one thing the owner got physically right is stated in exactly the register a bounded
surface cannot depict.

**Disposition:** the SHOWS-only emblem survives, unchanged — *"where worldlines end."* The
citable geometry remains Minkowski's mass-shell hyperboloid, where rapidity **is** arc length.

## §4 · Items 3 and 6, written

**Item 3 — the third mode.** Doc 52 §5.1 replaced. Irrationals are **words in the same two
operations that never halt**: `√2 = [1;2,2,2,…] = S ι SS ι SS ι …`. And the direction defect
is stated first rather than hidden: `val` is **left-to-right**, which on an infinite word is a
**dynamical orbit** that does not converge — `SιSιSι…` gives `2, ½, 3/2, ⅔, 5/3, …`, two
accumulation points `φ` and `1/φ`. A period-`k` word gives exactly `k`. The continued fraction
builds the **other way**, and on finite words the two readings are conjugate by reversal:
`val_R(w) = val_L(w̄)`, **zero mismatches over all 8191 words of length ≤ 12** — so `G1` is
direction-blind and only the infinite case needs the declaration.

**Item 6 — doc 54, and it carries a category correction.** Stereographic projection does
**not** exhaust 3-space: it is radius-blind, `2↔2`. Rotations are isometries, so `SO(3)`
exhausts **directions**, never lengths. The honest form is **`2 + 1 = 3`** — the sphere gives
two degrees of freedom, **the radius gives the third, and the radius is not on the sphere.**
Without that sentence it would have shipped as dimension-laundering.

•   ⊙   ○ — *the rescue chose the thing it was built to escape.*
