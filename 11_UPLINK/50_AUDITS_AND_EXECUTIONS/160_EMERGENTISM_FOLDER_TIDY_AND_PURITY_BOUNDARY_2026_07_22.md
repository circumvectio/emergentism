---
title: "Receipt 160 — Emergentism folder tidy and purity boundary"
date: 2026-07-22
status: "EXECUTED — bounded filesystem, routing, and authority cleanup"
evidence_tier: "[B] repository facts; [S] routing and purity checks"
owner: 01_EMERGENTISM
---

# Receipt 160 — Emergentism folder tidy and purity boundary

## Scope

The user instructed the agent to tidy the Emergentism folder after the loose
arrival reconciliation. The pass preserved the already committed root tidy and
the concurrently completed Receipt 158 migration. It did not rewrite either
commit, doctrine history, public deployment, or external application repos.

## Physical cleanup

Only ignored and reconstructible working-copy outputs were removed:

- Lean build and dependency cache under `formal_reap/.lake/`;
- `book-pwa/node_modules/` and `.next/`;
- generated Vercel output, while retaining project linkage and local environment
  metadata;
- Python, Ruff, pytest, Finder, and empty-directory debris;
- five verified-empty directories accidentally created at the Magnum Opus root
  by the preceding tidy.

Lockfiles, manifests, sources, archives, credentials, and project linkage were
preserved. The local repository footprint fell from roughly 8.9 GB to 145 MB.

## Authority correction

The preceding tidy plan and receipt incorrectly used a private financial and
contractual signature label for ordinary repository approval. The current
surfaces now say **owner approval**. The Uplink Door likewise uses neutral
`decision packets`. The original wording remains visible in Git history but
creates no current editorial, repository, or AI-work authority.

## Pure-worldview boundary

The staged `00_META/00_THE_FINAL_RULES_2026_07_22.md` combined the standalone
worldview with external product and governance rule systems. It was moved
intact to:

`90_ARCHIVE/2026_07_22_mixed_final_rules_boundary/`

The original path is now a provenance-only tombstone. The reusable naming
discipline was extracted into the smaller, doctrine-neutral owner:

`00_META/00_PLAIN_LANGUAGE_NAMING_LAW.md`

Receipt 158's useful outputs remain active: the eta conversion map, the
changed-prose naming lint, and the four path-repair stubs. Their active prose
now cites Receipt 158 or pure Emergentist owners instead of the mixed staging
compilation. Negative-check tooling is explicitly typed as such by the purity
checker rather than mistaken for leaked authority.

## Verification

- `check_emergentism_purity.py`: PASS across 778 active files before register
  regeneration;
- `check_links.py`: complete;
- naming lint: clean across the eight new or repaired active semantic surfaces;
- the full compiler semantic suite: 92 tests passed; its stricter link test
  exposed and then verified repairs to ten incorrectly rooted forwarding links
  left by the preceding root move;
- deterministic registers: clean at 3,167 files and 756 folders;
- `git diff --check`: clean;
- no source, archive, public deployment, DNS, or external product repository
  was deleted or changed by this receipt.

The physical caches can be regenerated from their retained manifests and
lockfiles. The archived mixed compilation can be inspected or selectively
re-derived, but it must never be restored wholesale as active Emergentist
authority.
