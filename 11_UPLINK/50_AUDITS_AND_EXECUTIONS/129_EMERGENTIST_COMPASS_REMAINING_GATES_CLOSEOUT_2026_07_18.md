---
receipt: 129
title: "Emergentist Compass — Remaining Local Gates Closeout"
status: "SAFE LOCAL GATES CLOSED — new observations, independent replication, and hosted provisioning remain external"
date: 2026-07-18
evidence_tier: "[B] local execution and reproducibility receipts; [I] framework crosswalks; [C] unrun external discriminators"
branch: codex/emergentist-compass-calibration
agentz_foundation_commit: 66b480c
agentz_hardening_commit: b00dee7
agentz_receipt_attestation_commit: 11687b9
calibration_foundation_commit: 64cfccd
calibration_hardening_commit: 39a58dd
calibration_protocol_commit: f119070
calibration_analysis_binding_commit: 0b0af50
calibration_semantic_review_commit: bd465b1
whole_calibration_stage: not_assigned
validated: false
---

# Receipt 129 · Emergentist Compass — Remaining Local Gates Closeout

## Verdict

Every remaining task that could be completed safely from the local repository
and installed toolchain is closed. Three different statements remain important:

1. the seven-row Rosetta configuration is now one tracked, typed source with a
   deterministic Documents-root runtime projection;
2. the hosted surface has a genuine fail-closed local control plane but is still
   `unprovisioned_x0`; and
3. external calibration now has an outcome-sign-neutral receipt gate and an
   executable queue, but no claim has been promoted to `X2`, `X3`, or `X4`.

This receipt does not call operator-, credential-, observation-, or
independent-team gates “complete.”

## 1. Root Rosetta dispatcher

The tracked Managed Agent YAMLs are now the selected semantic authority. The
[root dispatcher compiler](../../09_TOOLS/02_COMPILERS/sync_root_agentz_dispatch.py)
deterministically derives exactly 13 ignored runtime-kernel files from those
seven profiles. It replaces `GOD_SELECT` with typed `ROUTE_SELECT`, retains the
four operational moves plus three read-only Executive boundaries, makes L4 the
only mutation-capable row, and separates D5 represented possibility from D4
actual carriers, actions, and receipts.

The final live synchronization was triply guarded by the observed target
preimage, the complete reviewed 13-source bundle, and the exact deterministic
13-output digest:

```text
preimage   bf4fc02b6b8ebe96a031c05552b1797f4ffd4b9f5e4c51cff46202e6c7c9af4e
source     51948ad2687e0109acf44e1aa917002ca43da17b005d78b54966f126cb0648b7
output     0b26e148491c4ed7a7685e1828931469d49486d86844cc37385f7ac86125a24c
postimage  2a4da0af1edecce0f38620b136184c90453ea0fec2848be66dae32bedbb2d41c
```

All seven observed operator-token surfaces are homologous. The 19 files under
`rows/cx_suite/` were checksummed before and after and are byte-identical. They
remain a separate Skyzai/product-role surface and were neither loaded nor
edited. The 13 generated root files remain ignored runtime state; no force-add
or root-repository commit was used.

God/Demon language is no longer configured identity or authority. A fully
validated, freshly signed attempted commitment; a freshly signed world outcome
under a distinct hash-pinned key and causally ordered within the configured
clock-skew tolerance; complete bearer coverage; non-null deltas; and a complete
Justice assessment are prerequisites to any classification. Ambient
observations remain unclassified. Demon-bearing means
focal-beneficiary gain with any named bearer loss. God-bearing means Justice is
satisfied, no named bearer loses, and at least one gains. Justice-compliant
all-zero stasis is preservative but not God-bearing; strict syntropy is the
stronger subset in which the named individual and whole both rise and every
other bearer is nonnegative. These remain `[I]`, receipted, and contestable
consequence predicates—not agent identities.

## 2. Managed Agentz local control plane

The [local contract](../../08_FRAMEWORK_SUPPORT/08_AGENTS/MANAGED_AGENTS/contract.py),
[closed schema](../../08_FRAMEWORK_SUPPORT/08_AGENTS/MANAGED_AGENTS/managed_agentz.schema.json),
and deterministic [bundle lock](../../08_FRAMEWORK_SUPPORT/08_AGENTS/MANAGED_AGENTS/agentz.lock.json)
bind the seven agent payloads, environment, topology, authorization, budgets,
deployment receipt, commitment receipt, and environmental outcome receipt.

```text
bundle  e627a9cd92dacf1945c3ef17b8720f83d2c1e3c1016facbb76b1cb498370af70
status  unprovisioned_x0
```

Consequential requests now bind a structured action plan and canonical target
paths to a fresh BIP-340-signed authorization. The local ignored trust policy
names allowed public keys and freshness windows, and preflight also requires
the independently reviewed semantic hash of that policy. Requested operations
and paths must be subsets of the signed repository/ref scope. The same action
plan hash survives into separate commitment and outcome receipts. Deployment
claims likewise require a fresh signature from a trusted verifier; a document
cannot designate itself trusted. Commitment and world-outcome receipts also
carry fresh BIP-340 attestations. Their keys and issuer identities are both
disjoint and hash-pinned; the outcome is causally action-linked and may not
timestamp earlier than the attempted commitment beyond the hash-pinned clock
skew allowance. A shallow dictionary, caller-injected role name, ambient
observation, stale receipt, or forged signature cannot enter consequence
classification.

Provisioning defaults to offline check/plan and makes zero client calls.
`--apply` returns `REMOTE_ADAPTER_UNSUPPORTED`; execution refuses until a
pinned adapter can retrieve and hash the complete observed remote state and can
enforce and observe every registered budget. A model message cannot issue its
own world outcome receipt. No external API mutation, paid resource creation, or
hosted session occurred.

## 3. External-calibration honesty repair

The X2 gate previously required a favorable `supported` result. That was a
confirmation-bias defect. It now accepts exactly the declared outcomes
`supported`, `failed`, `null`, or `mixed` when every freeze, artifact, rival,
hash, commit-order, and receipt condition is satisfied. X2 records the rigor of
an independent discrimination, not victory for Emergentism.

The gate now also requires a separate, claim-specific closed schema-`2.0`
analysis manifest at the freeze commit. It binds a content-identified dataset
and checksum rule; fully specified candidate and rival models; operational
variables and outcomes; preprocessing and exclusion rationales; stopping,
fold, and seed rules; hashed analysis code, environment lock, and custody log;
separate native-unit cost ceilings; and an accountable
no-access/no-collection attestation. Every referenced dependency must exist at
both freeze and analysis commits with the declared bytes; analysis-time drift
followed by restoration fails. The result artifact must be absent at freeze and
hash-bound later at analysis. The two disclosed July 2 packet
paths and hashes cannot be laundered into a retrospective X2 result. Typed
X2/X3 verdict fields equal the receipt outcomes, while `currentVerdict` is only
the exact machine token—suffix prose is forbidden—so a failed receipt cannot be
narrated as support or vice versa. Git proves order, absence, and bytes—not a
human's memory—so the access attestation remains an auditable claim rather than
physical proof.

The frozen manifest additionally requires parseable, substantive analysis
code, a structured environment lock, a nonempty access log, and a separately
hash-bound semantic-review receipt. That receipt records an explicit checklist
covering model/rival adequacy, operationalization, exclusions, stopping,
estimands, falsifiers, costs, and ethics. The named review team must be distinct
from the analysis team and from the access attester/custodian. These are
accountability and provenance constraints: the machine can verify structure,
parseability, hashes, chronology, and declared identity separation, but it
cannot establish that prose is scientifically adequate or that humans were in
fact independent. Filler, collusion, or a contrary external audit kills the X2
claim rather than being laundered through a green validator.

The [calibration ledger](../../03_METHODOLOGY/00_EXTERNAL_CALIBRATION_LEDGER.md)
now discloses three already-known negative bearings without retroactive stage
promotion:

- Munnell production data: all four product/balance kill criteria failed;
- ultimatum-game data: the product-only score was not supported; and
- the developmental P-EQ pilot: the predicted middle peak failed in favor of
  the top tertile.

Both public-data packets reproduced their committed verdicts without changing
tracked bytes. The vesicle macro-constraint toy reproduced
`W_C=-0.426554` and `macro_claim=fails_and_contracts`.

The [execution queue](../../03_METHODOLOGY/03_PREREGISTRATIONS/05_EXTERNAL_CALIBRATION_EXECUTION_QUEUE_2026_07_18.md)
separates four possible retrospective-data discriminators from six claims that
require genuinely new observations, and separates both from independent X3/X4
replication. No claim-specific dataset was silently selected or accessed in
order to manufacture closure.

## Acceptance evidence

| Gate | Result |
|---|---|
| Complete compiler discovery | 685 passed in 409.150 s at `bd465b1` |
| Calibration validator | `CAL-OK sources=31 claims=12 whole=not_assigned validated=false` |
| Integrated focused suite | 136 passed |
| Calibration focused suite | 90 passed |
| Managed Agentz adversarial suite | 17 passed |
| Root dispatcher adversarial suite | 19 passed |
| Rosetta homology suite | 10 passed |
| Root live convergence | triple-digest-guarded deterministic 13-file postimage; homology pass |
| Excluded product roles | 19 `cx_suite` files byte-identical before/after |
| Negative empirical reproductions | Munnell, ultimatum, and macro toy reproduced |
| Static lint | Ruff passed on every changed Python control-plane/compiler file |
| Managed contract | schema, lock, local provisioning check, and seven-row contract passed |
| Changed Markdown links | 29 local links checked; 0 broken |
| Patch hygiene | `git diff --check` passed |
| Independent logic review | PASS at `bd465b1`; prior filler/empty-file mutant rejected |
| Independent security/scope review | PASS at `bd465b1`; no frozen-tree or claim-boundary blocker |

## Kintsugi review seams

Two adversarial review passes initially refused closure. Their defects were
real and are repaired rather than papered over:

- calibration results lacked a claim-specific frozen analysis contract and
  could let typed outcome and public prose disagree;
- authorization did not bind the concrete path-level action plan;
- authorization and deployment trust could be self-asserted;
- root synchronization approved only the old target bytes, not the reviewed
  source and generated output bytes;
- incomplete receipts could be overclassified; and
- Managed documentation described a future hosted topology as current runtime.

A second counterexample pass then found that the analysis manifest still
accepted placeholders, a correct verdict prefix could hide contradictory
prose, an ambient outcome could be classified without a real commitment, and
receipt trust was only caller-injected text. Those paths are now rejected by
structured frozen records, exact verdict tokens, full commitment revalidation,
action-only classification, fresh signatures, hash-pinned trust, disjoint
issuer roles, and temporal ordering.

The next pass found one time-of-check gap: analysis code or another frozen
protocol dependency could drift at `analysisCommit` and be restored later. The
validator now requires the preregistration, manifest, analysis code,
environment lock, and custody log to retain their frozen hashes at the analysis
commit itself; five tamper-and-revert mutations are locked negative.

The final counterexample pass showed that length-valid lorem ipsum and
zero-byte support files could satisfy a structurally closed manifest. The gate
now parses the analysis program, validates the environment record, requires a
substantive access log, and binds an independent semantic-review checklist and
receipt before a frozen run may qualify. The implementation intentionally stops
at machine-verifiable accountability: scientific meaning and actual reviewer
independence remain human audit questions and explicit kill surfaces.

Commits `39a58dd`, `b00dee7`, `f119070`, `11687b9`, `0b0af50`, and `bd465b1`
close those seams with negative tests. The prospective/runtime distinction is
now explicit:
the seven local profiles and their compiler are executable; hosted creation,
remote topology observation, and provider-enforced budgets are not.

## Remaining external gates

These are not agent-local work:

- freeze a claim-specific acquisition/analysis manifest before selecting and
  accessing each new raw outcome artifact;
- obtain the accountable independent semantic-review receipt required by that
  frozen manifest before any X2 run;
- collect the six genuinely new observation surfaces named in the queue;
- obtain externally controlled independence attestations for X3/X4;
- supply an accountable principal, supported pinned adapter, credentials, and
  any spending authority before hosted provisioning; and
- run the hidden, equal-budget dispatch benchmark before claiming performance.

Until those events occur, the honest summary remains: internally coherent,
externally anchored at X0/X1, prospectively calibrated in method, and not yet
externally validated as a whole.

## Scope boundary

No file changed under `12_PUBLIC_SITE`, `90_ARCHIVE`, `91_COMPATIBILITY`, or
`11_UPLINK/60_SESSION_PACKETS`. No public deployment, push, archive rewrite,
compatibility rewrite, historical-packet mutation, paid service, recruitment,
or external proclamation occurred.
