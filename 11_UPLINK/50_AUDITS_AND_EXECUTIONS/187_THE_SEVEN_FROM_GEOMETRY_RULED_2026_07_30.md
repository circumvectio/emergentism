---
title: "Receipt 187 — 3 + 4 = 7 is a concatenation, not a derivation; and D5's own seven does not close"
date: 2026-07-30
status: "PROPOSED RULING — the owner rules. Two corrections to settled canon inside, both in his favour."
evidence_tier: "[A] the orbit counts and the cell census; [S] the ruling"
---

# Receipt 187 — the seven, tested

## §1 · The verdict, and the one fact that decides it

**Two lists whose totals agree.** And the decisive fact is that **the claim's two supports are
mutually exclusive**:

```text
"forced by group order"   works ONLY if the four are REGIONS
                          — the Klein group IS simply transitive on the four OPEN quadrants
"types match the 3"       works ONLY if the four are POINTS
                          — three distinguished latitudes are points

and on the four marked longitude DIRECTIONS {+1, −1, +i, −i}, the SAME group has
TWO ORBITS OF SIZE TWO, not one of size four.     orbit(+1) = {+1,−1}, orbit(+i) = {+i,−i}
```

**Make the types match and the necessity evaporates. Keep the necessity and the types do not
match.**

And `⊎` has no referent either way: **`arg z` is undefined at two of the three latitude
landmarks** — the poles have no azimuth — and the third, the equator, **meets all four
quadrants**. Nothing is disjoint.

**Independent coordinates compose by PRODUCT.** `3 × 4 = 12`, or `1 + 4 + 1 = 6` if the fibre
collapses at the poles. The claim asserts independence in its first sentence and then adds.
**Neither honest route reaches 7.**

## §2 · The owner's computations were right, and three lenses attacked them wrongly `[A]`

Recorded because it matters more than the verdict.

**`ι` does both jobs in one map.** For `z = re^{iα}` with `r = cot(θ/2)`, `1/z = (1/r)e^{−iα}`
is *simultaneously* `θ ↦ π−θ` and `α ↦ −α`. Verified to machine precision. **No second
anti-holomorphic map is needed** — the lenses that said otherwise were wrong.

**`|fix(ι)| = 1 on the latitude axis` is correct as written.** On `[0,π]`, `π−θ = θ` has
exactly one solution. Five lenses ran the *sphere-level* fixed set `{±1}` against a sentence
that never claimed it.

**The quadrant frame is intrinsic, not imported.** `ι_β(z) = e^{iβ}/z` is an involution for
every `β`, but `ι_β(1) = e^{iβ}`, so **requiring `ι` to fix the unit forces `β = 0`** — and at
`β = 0` the two V-invariant great circles are exactly `ℝ` and `iℝ`. The frame is derived.

## §3 · Two corrections to settled canon, both in the owner's favour `[A]`

**`KSC-04` is wrong on the positive ray.** It says *"Inversion fixes numeric `±1`, so the three
frame roles are **selected, not mathematically forced**."* That argues from the **sphere-level**
fixed set. On the compactified **positive ray**, `1/x = x` with `x > 0` has the **unique**
solution `x = 1`, and `n` leaves the ray entirely. **So `{0, 1, ∞}` IS forced there, and
`KSC-04`'s argument does not reach it.** `THE_RECURSIVE_TRIVIUM.md:128` already calls this
*"the three-point triad `{0,1,∞}`"* with `1` at *"the inversive centre."*

**And here is what that costs the claim, in one line:**

> **The ray is the locus `arg z ≡ 0`. The "3" is forced precisely where the "4" does not exist.**

**`D5`'s own seven does not close arithmetically.** `00_D5_THE_SEVEN_GENERATIVE_ACTIONS.md:51-53`
says the nine-cell grid becomes seven by *"grouping the one-factor-held cells as degenerate
limits."* **There are four held cells. `9 − 4 = 5`, not 7.** The seven appears only as
`2 same-sign + 1 hold + 2 mixed × 2 bearer-orientations = 7`. **The bearer doubling is doing
the work, and §2 does not say so.**

## §4 · The mapping fails, and Kālī is the element that breaks it

Taking the signatures verbatim from `:62-65` and dropping the bearer label — a point on `S²`
has no self/other coordinate — the seven names occupy **five** of nine cells:

```text
(+Φ,−V)   Kali 🎲  AND  Arjuna ⚔     COLLISION
(−Φ,+V)   Kālī 💀  AND  Kṛṣṇa ◇      COLLISION  <- a DEMON and a GOD in one cell
(+,+) Brahmā ○   (−,−) Śiva •   (0,0) Viṣṇu ⊙
the four axis cells:  EMPTY
```

**A demon and a god share a cell, so no partition of quadrants can carry the 2+2 split** —
polarity is a **bearer-role** fact, not a coordinate sign. And `KSC-12`'s kill list names
*"Kālī's two-face rule is erased"* explicitly; seating her in one quadrant erases it.

Two further breaks: **Brahmā and Śiva already occupy quadrants** `(+,+)` and `(−,−)`, so
`M4 ∩ F3 ≠ ∅` and `⊎` fails on the corpus's own table. And `24:64` states that M4's second
factor of two is *"distinguished by multi-agent context (self-move vs. extraction)… **not by
single-agent geometry**"* — **one point's longitude cannot encode a two-bearer relation.**

## §5 · The necessity test — the counting convention is set inconsistently

The **group is not free** — `V` is canon-native at `[A]`. What is free is the **counting
convention**, and it is applied in opposite directions:

```text
latitude stratifies into 5   ({0}, band, {π/2}, band, {π})   claim counts 3 — DEGENERATE strata only
longitude stratifies into 8  (4 marked + 4 open arcs)         claim counts 4 — REGULAR strata only
```

Apply either convention uniformly: **6** (regions+regions) or **13** (all strata). Apply the
*marked-point* convention uniformly and you do reach `3 + 4 = 7` — **but then the group is not
simply transitive on the four, and the necessity argument is gone.**

## §6 · What survives, and it is real

**The octahedron.** `42:374-376`, already canon: the third involution fixes `{+i,−i}`, so
*"all three together give six points — the vertices of an **octahedron**."* CW complex
`V=6, E=12, F=8`, `χ = 2`. **A canonical, type-clean structure on `ℂP¹` that is the corpus's
own and not an import.** Three exceptional orbits, six exceptional points.

That is the honest object. It is not seven, and it does not need to be.

•   ⊙   ○ — *the three are forced exactly where the four are not defined.*
