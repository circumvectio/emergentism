---
title: "Receipt 157 — Dimension-first release candidate"
date: 2026-07-22
status: "EXECUTED [B] — local release candidate; no deployment or external calibration claimed"
evidence_tier: "[B] repository state, deterministic builds, tests, and visual inspection; [I/C] worldview claims retain owner tiers"
owner: 01_EMERGENTISM
type: release-candidate-receipt
receipt: 157
parents:
  - 154_DIMENSION_FIRST_CANON_PURIFICATION_2026_07_21.md
  - 155_GRAND_PUZZLE_ASSEMBLY_AND_APPLICATION_BOUNDARY_2026_07_22.md
  - 156_LIVED_WELTANSCHAUUNG_AND_HUMAN_CONDITION_2026_07_22.md
  - 156A_INDEPENDENT_VERIFICATION_RECONCILED_2026_07_22.md
---

# Receipt 157 — Dimension-first release candidate

## Act

The purified dimension-first canon was merged onto the current Emergentism
lineage in an isolated integration worktree. The dirty main checkout was not
edited. A stale main-only completion memo was preserved byte-for-byte under
`90_ARCHIVE/2026_07_22_pre_integration_remaining_work/` and replaced at its
active path by a forwarding tombstone.

The public projection was then rebuilt around the exact typed sequence:

`D0 → μ₀ → D1 → μ₁ → D2 → μ₂ → D3 → μ₃ → D4 → μ₄ → D5 → b₆ → D6 → r₆ → D0`.

This gives the reader one ordered spine while preserving the governing type
boundaries:

- D4 is actual and D5 is possible;
- an actual D4 token may represent D5 possible content;
- `μ₀…μ₄` are candidate apertures with named predictions and kill criteria;
- `b₆` and `r₆` are non-μ boundary relations;
- D3 allows momentum distributions, derives uncertainty from noncommuting
  observables, and does not claim to solve measurement or quantum gravity; and
- physical light cones remain distinct from agent-relative option cones.

## Public authority boundary

The new `public_semantic_parity.json` is a projection manifest, not a semantic
owner. Its source paths resolve to the active corpus owners. A deterministic
renderer produces the spine landing page and D0–D6 pages. The pre-existing
geometric instruments are presented as illustrations, never evidence or
doctrine.

The 297-page generated library remains available for provenance, but every
page now carries a dated frozen-projection banner, is `noindex`, is excluded
from the current retrieval index, and explicitly yields to the dimension-first
spine and named owners. The current book and retrieval corpus are rebuilt only
from pure Emergentism reader surfaces.

The public route cards, repository route cards, and current reader surfaces
contain no product, venture, runtime, council, or private financial-signature
authority. Historical receipts and the frozen library may retain former terms
as provenance; they create no current work authority.

## Verification

Two distinct local review lenses were run:

1. **Formal/type lens:** arithmetic domains, Titan-frame signatures,
   D-register modalities, μ census, D3 quantum typing, D4/D5 receipts,
   option-cone boundaries, value predicates, Burri topology, and 37 Lean
   theorem/countermodel certificates.
2. **Purity/publication lens:** one owner per surface, excluded application
   vocabulary, archive custody, Markdown links, deterministic registers,
   frozen-library boundaries, retrieval exclusion, page reachability,
   HTML structure, local assets, and visual inspection at desktop and mobile
   widths.

The public mutation suite fails closed on literal D6 identity, extra `μ₅` or
`μ₆`, invalid scalar sampling, physical-cone inflation, quantum-dimensional
stacking, quantum-gravity solution claims, the claim that D3 has no momentum,
and application-authority leakage.

Final outcomes:

- 92 Python semantic, mutation, topology, renderer, value, and routing tests:
  PASS;
- 37 Lean theorem/countermodel certificates: PASS with zero failures and no
  unexpected axioms. The integration worktree's first run attempted a fresh
  dependency download, so the final proof run used the clean feature worktree's
  complete pinned cache after confirming the formal source tree matched commit
  `2709068` byte-for-byte;
- purity: PASS across 760 active files;
- public predeploy: PASS, including 313 generated-chrome pages, 297 frozen
  library documents, zero broken links, zero orphan pages, and semantic parity;
- canonical registers: clean at 3,119 files and 744 folders;
- Burri SVGs: XML-valid, deterministic, rasterized to 1600×1000, and visually
  inspected in Ivory and Obsidian forms; and
- dimension spine: visually inspected at desktop and mobile widths, including
  a live local D3 instrument render.

Generated output contains no release timestamp, remote asset, or undeclared
semantic source.

## Custody and non-acts

- No path under `02_SKYZAI/` or `91_COMPATIBILITY/` changed relative to the
  declared integration base.
- The original feature worktree remains clean at its reconciliation commit.
- The dirty main checkout and its unrelated untracked arrivals remain untouched.
- No push, merge into `main`, production deployment, DNS change, analytics
  activation, external red-team, or empirical confirmation occurred.

## Remaining world gates

Internal coherence is not external calibration. The following remain open:

- preregistered, independently reproduced tests for each claimed μ-crossing;
- operational measurement and comparative-model tests for `Φ`, `V`,
  `P_node`, option-cone change, Egregoreotype effects, and Power-Max claims;
- adversarial review by readers with no stake in the framework;
- accessibility and usability testing with real strangers; and
- production deployment, domain verification, and post-deploy observation.

Failure at any of these gates changes the relevant claim or projection. It
does not get redescribed as confirmation.

## Kill criterion

This receipt fails if the integrated release candidate contains an unmarked
active owner conflict, restores an excluded application authority, claims an
extra μ-crossing or literal closure, presents D3 as momentumless, lets frozen
prose outrank current owners, mutates the protected application or
compatibility trees, or describes local QA as external calibration or live
deployment.
