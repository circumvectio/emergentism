---
title: "Emergentist Exergy — owner direction, source migration and local receipt"
date: 2026-09-15
status: "LOCAL IMPLEMENTATION; public and cross-product migration not performed"
evidence_tier: "[B] source/operation receipt; [I] reconciliation; [D] downstream work plan"
---

# Direction and disposition

The owner requested replacement of the historical, retired reader expression `P = Φ × V` by
“Exergy = VMO × SKA,” then specified Vision as imaginable futures, Mission as a
constrained family of worldlines, Objectives as waypoints, and Strategies
realigned through KPIs and implemented by Agents. “Now go to work exhaustively”
authorized this implementation. The reconciled name is **Emergentist Exergy**,
with its distinction from physical exergy printed at the point of use.

The new name and six-field interpretation are selected for this local package.
Numerical multiplication, greater explanatory clarity, practical advantage,
and universal fit are not established by that direction. The default is typed
composition, not a numerical product of documents or a product of six scores.

## Source custody

Baseline Emergentism commit:
`7e2f7d80f3589e98bf6a5ed8808e0e9c5f88654d`, on the existing
`theory/parasite-load-2026-08-17` branch. The four exact upstream source hashes
are recorded in [contract.json](contract.json); `validate.py --check` rejects
drift rather than updating the pins automatically.

| Owner / surface | Disposition |
|---|---|
| [The Goal](../00_THE_GOAL.md) | Unchanged normative owner; this package applies its two horizons and bearer-complete correction discipline |
| [Formula Block](../../05_COSMOLOGY/00_CANONICAL_FORMULA_BLOCK.md) | Unchanged. Numeric reciprocal coordinates, sphere identities, and Titan boundaries are not naming-migration targets |
| [Power-Max lemma](../../05_COSMOLOGY/03_FORMAL_SYSTEM/08_EFR_POWER_MAX_LEMMA.md) | Unchanged. Preserve the retired product counterexample and conditional cardinal-candidate permission |
| [Boundary-discipline proposal](../../08_FRAMEWORK_SUPPORT/03_EVIDENCE/ROSETTA_STONE/42_VMOSKA_BOUNDARY_DISCIPLINE_2026_09_03.md) | Unchanged staged predecessor. Do not import its old symbols, singular worldline, or unattainable-Vision requirement into the new definitions |

The project-planning document was inspected as task context, then excluded from
these semantic pins: a project format is not a premise of the worldview.

The source review found a qualification worth preserving: independently
ordinal axes support Pareto comparison; the current scalar `min` convention
uses **shared** recoding. Those assumptions are not interchangeable. This
package records the distinction rather than silently changing a formal owner.

## Exact local scope

New files: `README.md`, `EVALUATION.md`, `MIGRATION.md`, `contract.json`,
`example.json`, `validate.py`, `test_contract.py`, all in this directory.
One existing file changes: the parent `01_TELEOLOGY/README.md` gains a pointer.
There is no archive move or deletion because no predecessor is replaced.
The owned change is one independently revertible commit by explicit pathspec.

The custom contract and validator describe an offline prototype. They are not
a second product, a deployment adapter, a model runtime, a full evidence store,
a JSON Schema claim, or a source-adoption service.

## Downstream work, in order

1. **Reader adoption at owning sources.** Use this packet to amend selected
   reader descriptions and claim cards explicitly. Keep old quotations,
   proof notation and historical receipts readable. Do not replace every `V`:
   Vision, physical volume, actual-power `V₄` and lowercase chart `ν` are
   different symbols. Keep the experimental product separate from the current
   ranking. Cross-repository project-format grammar has its own owner and must
   not be rewritten from this subordinate package.
2. **Resolve source/public dependencies together.** The current public parity
   manifest references the Canonical Formula Block. It and many website files
   were already dirty at entry. Do not rewrite pins or regenerate their
   dependants while another lane's changes are unresolved. The inspected
   dependency is positive evidence of coupling, not a claim that a text search
   found every consumer.
3. **Public projection.** After source reconciliation, project the six-field
   table and example into the relevant practice/explanation surfaces, preserve
   the physical/interpretive distinction in AI-readable summaries, and run the
   site's own deterministic generators, semantic parity, accessibility and
   predeploy gates. No public route or manifest is changed by this commit.
4. **APU/Cortex adapter trial, separately scoped.** APU may preserve input,
   source and qualifications; a consistency review may return contradictions
   and draft revisions. Neither may turn a proposed correction into adopted
   source truth or expand an execution grant. First prove read-only round-trip
   preservation of unknowns, bearer costs, evidence and parent links using
   synthetic data; only then consider a scoped consumer adapter.
5. **Empirical trial before stronger claims.** Freeze an evaluation from
   [EVALUATION.md](EVALUATION.md). No amount of indexing or code validation
   establishes the new measure, a fifth interaction or moral truth.

## Review and verification receipt

L1 read the exact Power-Max owner; L2 developed composition and numerical
alternatives; L3 ranked them and exposed the ordinal/shared-coding distinction.
L5 recommended one subordinate offline package; L6 preserved predecessors and
excluded foreign/public edits; L7 retained all six fields and correction of
ends as well as means. These are internal agent reviews, not independent
scientific validation or authorizing dispositions.

The first code audit reproduced stale scores after a Strategy change,
retrospective target lowering that could relabel old failure, a huge-integer
error path, and an incomplete error-result disclaimer. The implementation now
binds calibration to the evaluated context and bundles, requires fresh
observations or unknown status after criterion changes, rejects unsupported
numbers through bounded errors, and returns both non-execution and
non-validation flags on error. Regression tests preserve each failure case.
Declared changes and predecessor evidence are checked; the prototype checks
one supplied revision link, not an entire authenticated history chain.

The follow-up review also found an overstrict criterion check: distinct upper
and lower bounds sharing one observation blocked an otherwise valid Strategy
revision. The check now prefers an existing KPI's own prior identity and tests
renamed criteria separately; paired bounds and rename-only revisions pass.
Reusing the same dated observation cannot silently change its measured value.

Local verification, 2026-09-15:

- `python3 -B -m unittest -q test_contract.py`: **44 tests pass**, including
  adversarial subcases and deterministic repeated CLI checks.
- `python3 -B validate.py --check`: four source pins and synthetic record pass;
  example remains `HOLD`, unscored, non-executing and empirically unvalidated.
- Existing purity scanner on the exact eight owned files: **zero findings**.
- Existing link collector on touched Markdown: **29 local links, zero broken**.
- Existing barred-claim policy on package Markdown: **zero findings**.
- Corpus node-product-ranking gate: **PASS, 1,315 active files scanned**.
- `git diff --check`: pass.

The full local link gate still fails on **10 links outside the owned pathset**,
in the Seed and machine-intelligence trial corpus. Those affected files have
no working-tree diff. The full purity gate also reports failures outside the
owned pathset, including frozen-digest and projection-inventory drift. These
were not repaired, waived, or relabeled green. No full public predeploy or
website-generation success is claimed. The global failures and foreign dirty
website are reasons not to represent this commit as a release candidate.

The source-version probe for Rosetta's A3 recorder **held before writing**:
observed frozen-v0 HEAD `21ebb5c97709a52ac54bcc287662e523167dfac0`, expected
`54658d7797198cfc45677716aa743e728e7012d2`. It was not bypassed. Dispatch ID:
`emergentist-exergy-vmoska-2026-09-15`. No recorder event commit or transmission
is claimed. Local source work proceeded independently of that recorder hold.

Release meaning: **offline source/contract implementation only**. No claim of
full-corpus migration, website release, APU/Cortex integration, push, deployment,
external review, thermodynamic identity or empirical validation.
