---
title: "Canonical Agent Pipeline — tracked source and derived root dispatch"
type: governance
status: active
date: 2026-07-18
owner: L5 Brāhmaṇa (configuration architecture) + L3 Vaiśya (audit) + L4 Kṣatriya (bounded synchronization)
evidence_tier: "[B] tracked paths, deterministic bytes, and executable validators; [S] selected row grammar; [I] cross-domain interpretation"
companion_to:
  - ../08_AGENTS/MANAGED_AGENTS/README.md
  - ../../09_TOOLS/02_COMPILERS/validate_agentz_rosetta.py
  - ../../09_TOOLS/02_COMPILERS/sync_root_agentz_dispatch.py
rosetta:
  primary_level: L5
  primary_column: Agent Architecture
  secondary:
    - level: L3
      column: Reproducibility Audit
      role: "verify source contract, generated bytes, and cross-surface token parity"
    - level: L4
      column: Bounded Synchronization
      role: "write only from an explicit preimage digest"
    - level: L6
      column: Source Boundary
      role: "keep ignored runtime projections subordinate to tracked source"
  operator: "Brahmā ○"
  tier: "Executive boundary"
  regime: "Brāhmaṇa"
  register: "[B/S/I]"
  canonical_phrase: "One tracked configuration, deterministic projections"
---

# Canonical Agent Pipeline

> `[金]` The former hand-maintained `.codex/agents/rosetta_agent_rows.toml`
> pipeline is superseded. That index does not exist in the live Documents tree,
> and the ignored row TOMLs cannot serve as durable semantic authority. Its
> provenance remains in Git history and dated audits; it is not an active input.

## Source authority

The seven tracked Managed Agent YAML files are the selected configuration
authority:

```text
08_FRAMEWORK_SUPPORT/08_AGENTS/MANAGED_AGENTS/agents/
├── 01_candala_firewall.agent.yaml
├── 02_sudra_explorer.agent.yaml
├── 03_vaisya_auditor.agent.yaml
├── 04_ksatriya_executor.agent.yaml
├── 05_brahmana_architect.agent.yaml
├── 06_sadhu_compressor.agent.yaml
└── 07_rsi_constitution.agent.yaml
```

They carry the exact Varṇa, Pramāṇa, Reasoning, -ology, Regime, Equation,
equation domain, evidence tiers, input/output types, stop conditions, budgets,
evaluation contract, D4/D5 type distinction, permissions, and authorization
mode. These are selected and testable configurations, not discovered laws of
cognition.

The contract gate is:

```bash
python3 09_TOOLS/02_COMPILERS/validate_agentz_rosetta.py --check
```

It requires the four operational moves plus three Executive boundaries, while
allowing mutation only at L4 through `write`, `edit`, and `bash`, each
`always_ask` and complete-AuthorizationEnvelope gated.

## Derived Documents-root runtime kernel

The ignored `/Users/Yves/Documents/.codex/agents/` surface is a local runtime
projection, not source authority. The deterministic compiler manages exactly
thirteen files:

```text
DEPLOYMENT_MANIFEST.md
DISPATCH.md
MANAGED_AGENTS.md
README.md
ROOT_AND_GOD_DEPLOYMENT.md       # compatibility filename only
rosetta_dispatch_schema.toml
rows/01_L1_candala_firewall.toml
rows/02_L2_sudra_explorer.toml
rows/03_L3_vaisya_auditor.toml
rows/04_L4_ksatriya_executor.toml
rows/05_L5_brahmana_architect.toml
rows/06_L6_sadhu_compressor.toml
rows/07_L7_rsi_constitution.toml
```

`rows/cx_suite/` is a separate product-role surface. It is explicitly excluded:
the seven-row compiler neither loads, hashes, edits, nor interprets it as extra
Rosetta levels.

## Generation and synchronization

```text
tracked Managed Agent YAMLs × 7
              │
              ├── validate_agentz_rosetta.py ── fail closed
              │
              ▼
sync_root_agentz_dispatch.py ── deterministic render
              │
              ├── --check: compare bytes and print aggregate preimage SHA-256
              │
              └── --write --expect-preimage <SHA-256>
                         │
                         ▼
             ignored 13-file root runtime kernel
```

Read-only comparison:

```bash
python3 09_TOOLS/02_COMPILERS/sync_root_agentz_dispatch.py --check
```

A write is deliberately two-step. Copy the exact aggregate digest printed by
`--check`, inspect the reported drift, and then pass it explicitly:

```bash
python3 09_TOOLS/02_COMPILERS/sync_root_agentz_dispatch.py \
  --write \
  --expect-preimage <SHA-256-from-check>
```

The writer holds an exclusive compiler lock, snapshots all managed bytes,
stages every output, rechecks the preimage, atomically replaces individual
files, and writes the deployment manifest last. A stale digest fails without
overwriting the live kernel.

## Typed semantics preserved by the projection

- Dispatch is `PARSE → ROUTE_SELECT → DRAFT → LINT → GROUND`; `GOD_SELECT` is
  superseded.
- D5 is represented possible content: alternatives, rankings, and modeled
  futures. D4 is the actual carrier: tool call, action, receipt, and record.
- L1–L4 are operational moves; L5–L7 are read-only Executive boundaries.
- Only L4 may request mutation.
- Platform confirmation is necessary but insufficient. L4 also requires the
  complete principal, mandate, scope, consent, custody, expiry/revocation,
  contest path, actor, consequence bearer, payer, and beneficiary envelope.
- A commitment receipt records the attempted action. A later environmental
  outcome receipt records what happened. The selector cannot manufacture its
  own consequence.
- Private-DAV K2 and public governance are separate implementation rails, not
  worldview primitives and not substitutes for the authorization envelope.

God/Demon language is forbidden as configured agent identity. It may appear
only as an `[I]`, retrospective analogy over independently validated and tiered
receipts: ego-only or cost-externalizing consequences versus durable mutual
individual-and-collective potential increases under Justice. It transfers no
authority or moral essence.

## Cross-surface audit

`agent_homology_audit.py` now reads canonical operator tokens from the tracked
YAMLs and may compare them with the generated root row, doctrine narrative,
Skyzai runtime, Agentz phenotype, Claude dispatch, and Goose projection:

```bash
python3 09_TOOLS/01_SCRIPTS/agent_homology_audit.py
```

Success proves only observed symbolic parity. It does not prove agent quality,
the sevenfold configuration's superiority, ontology, or moral type. Behavioral
claims require blinded, budget-matched comparison against flat and shorter
rival configurations.

## Amendment protocol

1. Amend one tracked Managed Agent YAML.
2. Run `validate_agentz_rosetta.py --check` and its unit tests.
3. Run the root compiler with `--check`; inspect every proposed drift.
4. Synchronize only with the exact preimage digest and appropriate workspace
   authority.
5. Run the homology audit and focused compiler tests.
6. Treat Skyzai product-role and C-suite profiles as a separate owner-lane
   reconciliation; never fold them into the seven-row kernel by recursive glob.

Zero-Sum Resolution Equation

## Execution Surface

- **Canonical Path:** `01_EMERGENTISM/08_FRAMEWORK_SUPPORT/01_GOVERNANCE/00_CANONICAL_AGENT_PIPELINE.md`
