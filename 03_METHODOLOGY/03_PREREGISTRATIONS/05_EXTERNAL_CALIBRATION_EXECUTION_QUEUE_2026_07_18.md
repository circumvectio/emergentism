---
title: "Compass External Calibration — Executable Queue"
status: "OPEN EXTERNAL GATES — metadata screened; no new raw outcome access"
date: 2026-07-18
evidence_tier: "[B] local reproducibility and source inventory; [C] unrun discriminators"
---

# Compass External Calibration — Executable Queue

This is an execution queue, not a result receipt and not a substitute for a
claim-specific freeze. It prevents “externally calibrate” from becoming a vague
instruction or a retroactive promotion.

## What has already run

The pre-existing Munnell production and ultimatum-game packets reproduce their
committed negative product-form verdicts. The vesicle macro-constraint toy also
reproduces its negative result, `W_C=-0.426554`. These are bounded evidence for
the exact analyses they ran. Because the outcomes were known before the July 18
Compass contract, none is relabeled `X2`.

## Lane A — possible retrospective X2

The following cards have published external observations identified but no
claim-specific frozen analysis manifest:

| Claim | Candidate source families already registered | Gate before raw outcome access |
|---|---|---|
| `CAL-AGENCY-01` | Peters, Daw, Doll, Akam | select one exact repository/artifact and freeze the future-representation intervention, exclusions, variables, models, folds, seeds, and costs |
| `CAL-CONSTRAINT-01` | Montévil–Mossio, Hoel, Flack, Rosas | select one observable system and freeze microstate, environment, macro-map, interventions, unit-typed costs, and fair micro rivals |
| `CAL-MU-01` | Vicsek, Carpenter, Ratcliff, West | select one crossing and freeze control parameter, partition family, recovery test, smooth-crossover rival, and reduction decision |
| `CAL-REFLEXIVITY-01` | Heemeijer and Smith market protocols | select one exact dataset and freeze lagged cognitive/participating variables, reinforcing/corrective sign test, and non-reflexive rivals |

For each card, the freeze commit must bind a separate claim-specific JSON
analysis manifest before first raw-data access. That manifest names the
HTTPS or content-addressed immutable dataset locator, checksum mechanism and
expected hash, fully specified candidate and complete rival set, operational
variables and outcomes, nonempty preprocessing and exclusion rationales,
stopping rule, fold count, random seeds, code and environment-lock paths plus
hashes, separate compute/data/labor ceilings, and dated attestation/custody
records with a hashed access log. The freeze commit must bind every one of those
records and referenced files. The selected data artifact must be absent at the
freeze commit and present only in the later analysis commit.
Only then may the artifact be acquired and analyzed in a later commit. The
outcome may be `supported`, `failed`, `null`, or `mixed`; all four can earn X2
methodological status and all four must change the verdict honestly.
The public verdict is machine-sized, not narrative:
`X2 outcome=<supported|failed|null|mixed>` exactly. Explanatory prose belongs in
the dated receipt or ledger, where it cannot reverse the token.

## Lane B — genuinely new observations required

| Claim | Minimum new observation surface |
|---|---|
| `CAL-CONE-01` | hidden controllability tasks with reachability, empowerment, POWER, viability, entropy, utility, risk, and cost separated |
| `CAL-AGGREGATOR-01` | bounded tasks directly measuring present `Φ`, `V`, and receipted outcomes under every registered aggregator |
| `CAL-EGREGORE-01` | carrier-turnover × trace preserve/erase/scramble/counterfeit × incentive-reversal microsociety |
| `CAL-JUSTICE-CONSEQUENCE-01` | institutional comparison with every bearer, payer, beneficiary, contest, reversal, and exit receipt retained |
| `CAL-SPHERE-01` | one identifiable observable domain and prespecified `S²`, Euclidean, hyperbolic, toroidal, simplex, and flexible-manifold fits |
| `CAL-DISPATCH-01` | hidden matched tasks, equal budgets, frozen 4+3 router, all registered rival routers, and blinded external evaluation |

The separate developmental P-EQ protocol still requires the specified
blind-human study. Its AI-coded pilot already fired the preliminary middle-peak
kill criterion and must remain visible.

## Lane C — independent replication

`X3` requires a different team to preregister, collect new observations, and
repeat the discriminator after the X2 receipt. `X4` requires at least two teams
and two domains. Neither can be manufactured by another local run, an agent
name, or a self-authored independence boolean; the external attestation verifier
is intentionally still closed.

## Stop condition

Do not download a selected raw outcome artifact until its claim-specific
manifest is committed. Do not provision paid infrastructure, recruit people,
or claim team independence without an accountable principal and explicit
authority. Those are world/operator gates, not missing prose.
