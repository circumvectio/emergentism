---
type: routing-stub
title: "R2_HARNESS — agent routing"
status: "ACTIVE 2026-07-20 — additive routing stub (K2-delegated L4 Kṣatriya wave, receipt 146/147 authority)"
owner: 01_EMERGENTISM
parent: ../AGENTS.md
canonical_front_door: README.md
---

# R2_HARNESS — Agent Routing

This sub-folder holds the runnable kit for the R2 Charioteer Benchmark study
(per the parent `PEER_REVIEW_PROGRAM/` and the prereg at
`../R2_CHARIOTEER_BENCHMARK_PREREG.md`). Battery `v0.1` is built and
smoke-tested; the prereg target is 40 adversarial scenarios — expand, then
freeze before the registered run.

**House rules (inherited):** every condition, scenario, and result must
trace to its `target_claim` and `plausible_core`; the headline never
outruns the components. The `BATTERY_FROZEN_SHA256.txt` lock is the
boundary; do not edit files after the SHA is frozen without a K2 envelope.

**Run order:** `conditions.json` → `scenarios.json` (or `scenarios_40_candidate.json`
while expanding) → `run_benchmark.py` → `judge_transcripts.py`. Refine with
`Edit`; do not move or rename. K2 signatory for irreversible acts only.

η = 0.
