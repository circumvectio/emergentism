---
rosetta:
  primary_level: L5
  primary_column: Meta
  operator: "Brahmā ○"
  tier: "Executive"
  regime: "Brāhmaṇa"
  register: "[D]"
  canonical_phrase: "SKYZAI family blueprint-completion frontier ledger + org verdict (graded on receipts)"
type: skyzai-frontier-ledger
title: "THE SKYZAI FAMILY — BLUEPRINT-COMPLETION FRONTIER LEDGER + ORG VERDICT — 2026-07-18"
date: 2026-07-18
---

# THE SKYZAI FAMILY — BLUEPRINT-COMPLETION FRONTIER LEDGER + ORG VERDICT — 2026-07-18

**Status:** [D] staged analysis. Graded on **receipts, not doctrine** — 14 graders read the runtime-truth / integration-status / P-score docs and several *ran the test suites themselves*. A green banner counts for nothing; "spec exists" is L2, never L3. Workflow `wf_d096d09a-36d` (14 graders + 3 architects + 1 judge).
**Completion ladder:** L0 concept · L1 doctrine · L2 spec · **L3 built-not-integrated** (code + own tests pass, but no packet crosses an organ boundary in production) · L4 integrated (live edges) · L5 live (real users/receipts).

---

## 1. The frontier — where every product actually sits

| Product | Level | Last-green receipt | The one gap to the next rung |
|---|:---:|---|---|
| **Agentz / APU** (SHOULD) | **L3+** | Live LLM council runs — signed K2 envelopes, real ~$0.03–0.06 burn; 71 backend tests pass. P: code 0.45 / runtime 0.30. | One *real* organ-boundary edge (today every edge is synthetic/local — "deterministic local council patch + non-live F2"). |
| **RealityFutures** (COULD) | **L3 / L4−** | 18/18 Forge suites pass, 115 build artifacts. But contracts undeployed; the one "live" F2 signal is synthetic, on localhost, ingested by no one. | A model-backed forecast actually consumed across F2→F3 + one market resolving on Hedera testnet. |
| **TheCircle** (IS) | **L3** | PWA buildable (18/18). Only [B] proof = a local RSS run; **receipt dated 2026-04, ~89 days before this ledger's 2026-07-18 publication; the 14-day gate is by receipt-date, not by ledger-date — row is fresh-flagged per L1 verifier audit 2026-07-18, the receipt is stale**. circle.news 404s; Kind 31339 not live. | Redeploy + emit one live F1 packet to the Nostr relay so APU consumes live, not mocked. |
| **Skyzai** (ACT) | **L3** | 303/303 backbone + 221/221 Elixir + contract suites green. But all settlement is **mock**; the e2e loop passed "proof environment only"; G1 never fired. P runtime 0.22. | Fire G1 — one real on-chain settlement across F3→F4 with a real human K2 signature. |
| **Evolutionary.Network** (SHOULD-NOT) | **L3** | 261 tests pass (0.55s), ~8k LOC. No real immune case has crossed a boundary. | One genuine detected pathogen flowing through the due-process chain in production. |
| **Open Finance Network** | **L3−** | Architecture + the RP-1…14 receipt standard. Every crossing is **mocked**. | One real receipt crossing one real boundary (replace mock settlement). |
| **Spectre** (substrate) | **L3−** | ~6,800 LOC present, code-tier only. **0 live nodes.** | The deferred 10-node testnet / first metered Proof-of-Relay receipt on real bytes. |
| **Menexus** (legal/admin) | **L2+** | The operating/contracting lane exists; first-proof wedge is **counsel-blocked**. | One executed real edge (the Monthly-Managed-Residence first proof). |
| **NOSTR_VPN** | **L2 / L3−** | Golden-split allocation math ratified [S]; rides a rake-free MIT upstream. **No VPN product code.** | Write the destination-first VPN product code on the fork. |
| **PRISM** (membrane) | **L2** | A [D] ceremony spec — but "no PRISM ceremony has ever run," the verifier **cannot even import** (guaranteed ImportError), 0 tests. | A buildable, tested councilor-verifier + one dry-run ceremony bundle. |
| **Helios** | **L2** (L1↔L2) | One [B] wedge-validity harness (the honest "for a healthy person the output is silence" proof). | Running genome-prior→phenotype product code with its own passing tests. |
| **Aureus** | **L1+** | [C] architecture (AUg/XFIAT); specs not yet promoted to [D]; no acceptance tests. | Ratify the specs [C]→[D] with acceptance tests defined. |
| **Yantra** (DePIN) | **L1+** | Node concept; load model incomplete, no vendor receipts. | Finish the spec with real receipts (load model, vendors). |
| **QNTM** | **L1+** | Pre-licence narrative + one code module (IPCM). | One [B] build/test receipt on the module that exists. |

## 2. The headline — the family is Φ-complete and stuck at exactly one wall

Read the "gap" column top to bottom and it says the **same sentence eleven times**: *one packet crossing an organ boundary in production.* Every organ is built, tested, green — and V-starved at the identical L3→L4 transition. **Nothing is at L4 (integrated) or L5 (live).** The binding constraint of the entire family is not building — it is **deployment and the first live edge.** More doctrine, more specs, more refinement docs (this one included) raise Φ and move nothing; the whole estate turns on firing *one real packet through the loop*.

This also confirms, with receipts, the earlier diagnosis: the organs cluster at **L3** (built), the ventures at **L1–L2** (doctrine/spec), and the wall is uniform. The organism is a fully-drawn engine that has never once turned over under its own power in production.

## 3. Is the organization perfect? — No, but the loop is right; only the labels are wrong

Three architects (emergentist / builder / market lenses) independently returned **KEEP-WITH-FIXES** — none said "perfect," none said "reorganize." The verdict:

**What is right and must NOT be broken** (the expensive, near-impossible-to-retrofit property is already correct):
- The **organ loop ordering on disk** — IS → COULD → SHOULD → ACT + the SHOULD-NOT gate — physically encodes the D4→D5 dependency chain, so "what depends on what" is legible from the folder tree. Never sort this tree by completion tier (tier changes every sprint; loop position almost never does).
- The **per-organ constitutional boundaries** — each organ forbidden the next's job — the separation-of-powers moat.
- The organism kept **bound as one thing, outside the ventures folder** — the organs are not sellable SKUs; do not shatter it into six products.
- **Spectre peeled out as substrate**, and the **≥2-person K2 signer kept as the apex.**

**The three real flaws** (all fixable by relabeling + one index — *not* a reorg):
1. **PRISM is misfiled as the sixth organ.** It is not a cognitive organ — it is the **signing membrane** (the S1 apex primitive). All three lenses flagged this. *Fix: relabel it out of the organ list into the membrane/apex layer.* (Load-bearing.)
2. **Evolutionary.Network is filed in three places at once** — as an organ, as a venture lane, and as a relocated body — with a stale tombstone and dead links in the loop's own health report. *Fix: collapse to one canonical home (the built organ, 261 green tests) with thin pointer-stubs elsewhere.*
3. **No single view of what's built and what's next exists** — state is fragmented across P-scores, integration-status docs, and prose, in three incommensurable vocabularies. *Fix: this ledger is that missing artifact* — one uniform frontier (product · in-loop? · tier · last-green + staleness · next crossing) spanning organs *and* ventures.

**The honest top-level shape** (a labeling/manifest overlay, NOT a physical refold — the folders stay put):

- **THE LOOP** — TheCircle (IS) → RealityFutures (COULD) → Agentz (SHOULD) → Skyzai (ACT), + Evolutionary.Network as the SHOULD-NOT gate. Organs, boundaries intact.
- **THE SUBSTRATE** — Spectre (owns nothing); OFN's receipt standard and Yantra's DePIN nodes tagged as shared rails, peer to Spectre.
- **THE INSTITUTIONS** riding the same ethics (D5, independently shippable) — Aureus, OFN AG, Helios, NOSTR_VPN, QNTM, Menexus (each with its own legal shell).
- **THE APEX + MEMBRANE** — the ≥2-person K2 signer as a first-class structural element, with **PRISM relabeled here as the signing membrane**, out of the organ list.

## 4. The one-line answer

> **Not perfect, but the loop is right and only the labels are wrong: keep the trophic organ tree exactly where it is, relabel PRISM as the signing membrane, collapse Evolutionary.Network to one home, keep this build-tier + next-crossing ledger current — then stop reorganizing and go fire one real packet, because the binding constraint is deployment, not taxonomy.**

*[D] staged; graded on receipts; nothing built, no canon changed. The reorganization to AVOID is as important as the fixes to make — do not tier-sort the tree, do not shatter the organism, do not let the membrane masquerade as an organ. Receipts: workflow `wf_d096d09a-36d`, `P-SCORES.md`, per-product `INTEGRATION_STATUS`/runtime-truth docs. η = 0.*
