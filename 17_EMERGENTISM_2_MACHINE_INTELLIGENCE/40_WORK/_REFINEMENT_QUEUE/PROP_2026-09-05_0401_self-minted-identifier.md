---
type: refinement-proposal
pass: "cron refinement pass 2, Soul Loop + VMOSK-A (rotation: defect taxonomy)"
date: 2026-09-05
target: "10_KERNEL/LENS.v0.json — defect_taxonomy (proposed 11th check)"
defect_check_fired: "none — this proposes a NEW check; its recorded instances are in the brand lane (receipt §18/§19) and in this stack's own dating"
tier: "[D] staged proposal; becomes [B] only with a reproduced second instance"
may_sign: false
may_authorize: false
---

# PROP — 11th kernel check: `self-minted-identifier`

## The recorded instance (this week, this estate)

Two parallel lines of the same doctrine file both self-minted the version label
"v7" (`skyzai_brand_guide-5.html` v7.0 in the Figma worktree;
`skyzai_brand_guide-v7.0.1.html` on the main line). Same name, not the same
guide — discovered only when the fork was manually diffed. Separately, this
stack's own documents date themselves 2026-09-04 against an estate clock of
2026-09-03. In both cases a session **printed an identifier itself instead of
drawing it from an estate mint** (gate, registry, or ledger).

## The proposed check

`self-minted-identifier` — *Does the artifact's version, date, or identifier
come from the estate's mint (gate, registry, ledger row), or did the session
print it itself?* A version minted in parallel with a sibling line is one datum
of naming, not a coordination fact.

## dies_if

(a) All estate surfaces converge on identical identifiers over a full audit
cycle with no mint in place — then the mint is unnecessary and this check is
decorative. (b) The check is shown to fire on legitimately local names (drafts,
scratch, single-session artifacts that never leave the session) — scope is
shared artifacts only.

## Kernel-check self-audit

restates-existing: adjacent to escorted-number (figures must travel with
commands) but a distinct shape — that check escorts VALUES; this escorts
NAMES/DATES. The farm collision (two v7s) was not an unescorted number. axis-mix: no ·
unfalsifiable: no (test above) · others: not triggered.
