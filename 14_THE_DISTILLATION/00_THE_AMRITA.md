---
title: "The Amrita — Second Churning"
status: "PROJECTION — rules nothing"
date: 2026-08-05
currency: "Currency refresh 2026-08-05, later the same day. The five sweeps that produced these entries ran BEFORE the day's council docket landed; commit order is not content currency. This pass re-verified every entry against disk at HEAD `10b8890f` and corrected what moved. Entry count UNCHANGED at 40 — nothing was admitted. What changed: six status lines, eleven line anchors, one entry's middle limb, one entry's neighbour list, and eight live-defect rows. An entry's anchor is valid only against the declared verification commit; a projection whose sweeps ran at two disk states is withdrawn, not patched — see §03 item 13, which is this pass's transferable finding and fires the folder's own kill."
ownership: "Source documents retain semantic ownership. This file reports; it does not rule, ratify, promote a tier, or create a second owner. Every entry points at a document that owns it. Where this file and a source disagree, the source governs and this file is the defect."
evidence_tier: "[S] for the admission function and the counts only. Each entry carries the tier of its source and may not be read one notch above it. Nothing is established here."
supersedes: "07_THEOLOGY/00_THE_AMRITA.md (2026-07-03, recovery-integrated 2026-07-19) — superseded on the distillation question only, as of 2026-08-05. COMPLETED: the reciprocal line was written into that file at 07_THEOLOGY/00_THE_AMRITA.md:15-30 in the same commit (a65513ae); it names this folder as successor and states that the first churning is not withdrawn. The 'INCOMPLETE — one-sided' status carried here until this refresh was false on disk when written. Discharged in §03 item 17, not deleted."
---

# The Amrita — Second Churning

> **This ratifies nothing.** Six of the sources below carry `STAGED PROPOSAL — unratified` or `DRAFT 1`, and as of 2026-08-05 the **root mathematical authority is itself staged** — `00_THE_TRANSCENDENTAL_TRINITY_CANON.md` was amended (`a4a3a493`) and left **STAGED FOR OWNER DISPOSITION; a revert is refusal** (§03 item 1). `00_ESTABLISHED/README.md` still contradicts one entry outright. Inclusion here confers no tier, closes no gate, and settles no owner act. A projection that lists an unratified proposal in a folder called *what stands* would promote a tier by layout; the status strings therefore travel on the same line as the claim, per the corpus's own denial-marker rule.
>
> **Register status, corrected after the sweeps.** This banner previously read *"none of the twelve documents written on 2026-08-05 appears in `FILE_REGISTER.json`."* That was true when written and is **false now**: `d7915745` regenerated both registers and `87c06bee` recorded the day's non-receipt artifacts. Corrected with a date rather than deleted, because a standing condition that quietly disappears is the shape this folder exists to record. What is *still* unregistered is **this folder** — §03 item 11.

> *"The lesson: **the poison comes first.** Before the treasures. Before the nectar."*
> — `05_COSMOLOGY/01_THE_TRANSCENDENTAL_TRINITY/27_THE_SAMUDRA_MANTHAN.md:73`

## The headline

**§01 holds 21 entries. One of them is owned by this corpus.** Recomputed this pass: still one.

That one, at its true tier, in the corpus's own words: *"the observation that the two syntactic exclusions (`ιι = id`, `ι(1) = 1`) are the same constraint as the classical `a_n ≥ 2` normalisation — an expository identification, `[I]`, of real pedagogical value and no theorem content."* — `05_COSMOLOGY/03_FORMAL_SYSTEM/55_G2_PRIOR_ART_ADJUDICATION.md:156-160`.

> **Anchor corrected 2026-08-05.** Every previous citation of this sentence in this folder gave `55:150-154`, which is the **`Costs.`** paragraph — a different claim. The single sentence the whole folder is built around resolved to the wrong lines in all five files that cite it. Corrected to `55:156-160`, verified by reading. The folder's own entry-21 rule now applies to the folder: for a sentence this load-bearing, cite by unique path **plus a quoted string**, never by a bare line range.

Everything else that stands is Euclid's, Hardy & Wright's, Dedekind's, Cantor's, Möbius's, Klein's, Russell's, Zermelo's, von Neumann's, Hermite's, Lindemann's, Cauchy's, Lagrange's, Mac Lane's, Peano's, Napier's, Euler's, Brahmagupta's, Presburger's, Tarski's, Witten's, Atiyah's and Hirzebruch's, Shannon's, Setzer's, Carlström's, Bergstra's, Kahan's, Lotka's, Volterra's, Pigou's and Dalton's, Rosenthal's, Goldacre's, Lan's and DeMets's, O'Brien's and Fleming's, or DeMillo, Lipton and Sayward's — named in each line. Or, as the reader-facing chapter puts it: *"The distinction is not a discovery. It is a piece of ordinary equipment."* (`13_BOOKS/titans/CH04_FOUR_STATUSES_DRAFT_2026_08_05.md`, DRAFT 1.)

§02 holds 19 entries. It is the stronger half, and it is where the reading should start.

## The declared bound

Fixed before the entries were written, per the corpus's own rule B2 (owner: Lan–DeMets / O'Brien–Fleming / ICH E9): **admission function** = the five gates (stated tier; stated kill, or `[A]` with a citation; kill not fired; at least one adversarial pass, or `[A]` with prior art named; inherited results name their real owner). **Deduplication rule** = one finding, one entry — a result reached by two sweeps is one entry, by the corpus's own rule S3. **Ceiling 40, target 35.** Candidates considered: ~129. **Actual: 40 — at the ceiling, not the target.** Recorded, not smoothed.

**The arithmetic, so a reader can recompute it.** §01: `1A 1 + 1B 7 + 1C 5 + 1D 3 + 1E 5 = 21`. §02: `19`. **Total 40.** The three bolded rows in §03 that carry numerals are **live defects, not entries** — README §6's own table says so — and the §04 queue below is a queue, not a list of entries.

**Unchanged by the currency refresh, and that is a result.** Seven late items were adjudicated: **0 admitted**; **2 held at the bound** (§04); **1 excluded as a restatement** and **1 as out of lane** (§04); **1 already filed and sharpened in place** (entry 39); **2 filed as live defects or at-site corrections** (§03 items 1, 6, 11). `40 + 0 = 40`. *A bound honoured by deferral is the bound working. A bound raised to fit a new entry is the bound failing.*

**But the bound exists in two numbers, and that is a B2 failure by the folder's own rule.** `README.md:54` declares *"Ceiling: **45** entries. Expected: 35–40"* and `README.md:114` keys the folder's kill to 45; this file declares **40**. Both claim to have been fixed before the entries were written. Under B2 they cannot both be the bound, and the looser number may not become operative merely because it is the more convenient one. Recorded as §03 item 12. The conservative reading binds at 40 and is the one used above.

**Machine-checked, at one remove.** The Lean file was *not* recompiled in this pass: a `lake build` was started, began pulling 224 MB of mathlib into an iCloud-synced tree, and was killed. The recorded build is `11_UPLINK/50_AUDITS_AND_EXECUTIONS/182_C_HAT_IS_NOT_A_RING_MACHINE_CHECKED_2026_07_29.md` (8661 jobs, 20 theorems, no `sorry`, four axiom-free traces). The file's last commit (`31fa4533`, 2026-08-01) postdates that build; the diff is a two-line docstring change touching no theorem statement or proof term. Re-verified directly this pass: 20 `^theorem` declarations; `grep -cE 'sorry|admit'` returns 3, all prose (lines 7, 150, 207); toolchain `v4.33.0-rc1`; mathlib pinned at `932a58b04d34`. Every "machine-checked" below means *at one remove*.

---

## 01 · WHAT STANDS

### 1A · Owned by this corpus (1)

1. **`[I]`** The base's two syntactic exclusions (`ιι = id`, `ι(1) = 1`) **are** the classical `a_n ≥ 2` normalisation — an expository identification with real pedagogical value and no theorem content. Owner: this corpus. `05_COSMOLOGY/03_FORMAL_SYSTEM/55_G2_PRIOR_ART_ADJUDICATION.md:156-160` *(anchor corrected 2026-08-05; it read `:150-154`, which is the `Costs.` paragraph)*.

### 1B · Inherited mathematics that stands (7)

2. **`[A]` machine-checked (at one remove)** For `a ≠ 0` in any field there is **no** `y` with `0·y = a`: `a/0` is *no such element*, not merely undefined — and the canon's own associativity falsifier turns out stronger than the canon knew, since `0·w = 1` collapses any nontrivial ring without needing associativity at all. Owner: field and ring axioms (distributivity, additive cancellation). `09_TOOLS/05_FORMAL_VERIFICATION/EmergentismCheck.lean:23,174,186`. *Fence:* mathlib **defines** `a/0 = 0` as a junk value (`:30`) — a library convention, never evidence that `a/0` has a solution.
3. **`[A]` machine-checked (at one remove) + `[A]` classical** Inversion fixes exactly `±1` — on the affine line and on `ℂP¹`, where `ι_P[z:w] = [w:z]` swaps `0_P` and `∞_P` and carries `ℝ ∪ {∞_P}` as an invariant meridian; `{id, neg, ι, neg∘ι}` is a Klein four-group; and `Ĉ` is not a ring, because no nontrivial ring admits an additive absorber (`w + 1 = w ⟹ 1 = 0`). Consequence recorded against the emblem: **a picture with three marked points has a fourth it is not drawing**, and the unit is adjoined by naming. Owners: Klein (the four-group); Möbius (projective geometry); standard ring theory. `EmergentismCheck.lean:35,137,193-209`; `05_COSMOLOGY/01_THE_TRANSCENDENTAL_TRINITY/45_THE_TITAN_INVERSION_STRUCTURE.md:63-146`; `48_CO_CONSTITUTION_AND_THE_NOTATION_PROBLEM.md:177-181` (STAGED PROPOSAL — unratified; anchor below the 2026-08-05 insertion and verified unshifted).
4. **`[A]`** G1/G3/G5 — reachability from `1` under `S(x)=x+1` and `ι(x)=1/x` is **exactly** `ℚ⁺`; no finite word attains `0` (`ι(x)=0` has no solution at all) or an unbounded value; both limits are approached and never reached, and the reason is the finiteness of words, not slowness and not stipulation. Owner: **EUCLID** — the descent is the Euclidean algorithm; presentation Stern (1858) / Brocot / Calkin–Wilf (2000). `05_COSMOLOGY/03_FORMAL_SYSTEM/52_THE_GENERATIVE_BASE.md:130-214`. Checker re-run this pass, exit 0.
5. **`[A]`** What the base does **not** contain — `ι` alone generates `{1}`, `S` alone generates `ℕ⁺` and never descends; every word is a Möbius map of determinant `±1`, so the imported hinge `u=(x−1)/(x+1)` (det 2) is **not** generated by this base; and determinant and sign are *independent* obstructions (`x ↦ −1/x` has det `+1`, passes G9, and is still not a word). Owner: Möbius / standard `PGL(2,ℚ)`. `52_THE_GENERATIVE_BASE.md:223-340`.
6. **`[A]` (demoted from a flat `[A]` by the corpus, 2026-07-29)** `ι` is an involution on the reachable set with unique fixed point `1`; under `s = log x` it becomes `s ↦ −s` — but that carries completion to `ℝ` as a premise, because `log q` is **transcendental** for every rational `q ≠ 1`, so the log coordinate sees exactly one point of the base. Owners: **HERMITE (1873)** and **LINDEMANN (1882)**. `52_THE_GENERATIVE_BASE.md:248-272`.
7. **`[A]`** `1` is the unique additive irreducible of `(ℕ⁺,+)`, hence `{1}` lies in every additive generating set; and `ℤ` is **initial in Ring**, so the additive-generative role of the unit follows from the multiplicative one. Owners: Peano/Dedekind; Mac Lane. `53_THE_NUMBER_CHART.md:53-57,74,83`. *Two caveats travel with it:* the five statements are not five **independent** theorems (N2 follows from N1), and the gloss "nothing else in any ring does that" is false as worded — in `(ℤ,+)`, `−1` also generates. Read the kill, not the gloss.
8. **`[A]`** **Absorption** is the property that actually discriminates the horizon: for finite `S`, `|S ∪ {x}| = |S| + 1`; at the totality, `V ∪ {x} = V` literally, as classes. Owner: **RICHARD DEDEKIND**, *Was sind und was sollen die Zahlen?* (1888), named in place. `49_THE_THREE_MODES_OF_COUNTING.md:61-84` (STAGED PROPOSAL — unratified; anchor above the 2026-08-05 insertion at `49:105` and verified unshifted).

### 1C · Selections, priced (5)

9. **`[S]` signed 2026-07-31 (restated from "primacy" to "selection", ruling Q2)** The Riemann sphere is **selected**: it buys a chart where both boundary seats and the unit display together, and pays with the field — `Ĉ` is not a field, arithmetic is chart-local, and every arithmetic claim must name its chart. A rival keeps total order, signed magnitude and order-completeness natively. "No other model is acceptable" is recorded as **declared policy, not theorem**. Mathematics inherited: Möbius/Cayley, Riemann 1851, sphere model C. Neumann 1865. `47_THE_EMERGENCE_OF_FINITY.md §7-8`; `00_META/00_SETTLED_CANON_REGISTRY.md:78`; `11_UPLINK/50_AUDITS_AND_EXECUTIONS/193_FIVE_RULINGS_SIGNED_2026_07_31.md`.
10. **`[S]` ruled 2026-07-30** The base stays `⟨S, ι⟩` on `ℚ⁺`, and the accepted price is that KSC-21's oriented pair `{+1,−1}` is **not supported** by it: `D1` must be seated one chart up by a declared move. A signed base gets `−1` outright and pays G3, since `1 + (−1) = 0` makes zero reachable. Both horns written out, neither collapsed; the same session's claim that the determinant *was* the fork was retracted in place as "a selection presented as forced." `52_THE_GENERATIVE_BASE.md §5.2`; `11_UPLINK/50_AUDITS_AND_EXECUTIONS/185_SECTION_5_2_RULED_AND_F_EQUALS_MA_2026_07_30.md`.
11. **`[S]` the trade; `[A]`/`[B]` the rival bills, all cited** Boundaries are typed as a fifth first-class sort and refused as operands — buying `0x = 0`, the field, and NaN-free arithmetic, paying the rule that a boundary may never be written as an operand. **Five rivals' bills are published beside it**: wheels lose `0x=0`, `x−x=0`, `x/x=1` (Setzer 1997; Carlström 2001, 2004); IEEE-754 breaks reflexivity of equality (Kahan et al., 1985); involutive meadows overload `0` (Bergstra–Hirshfeld–Tucker); common meadows put `⊥` in every carrier (Bergstra–Ponse); `ℝP¹`/`ℂP¹` pays global field structure. The corpus then declines to compete: a reader who needs total division "should use wheels, and this chart should say so." `53_THE_NUMBER_CHART.md §4.1`; `02_EPISTEMOLOGY/01_EVIDENCE_TIERS/THE_BOUNDARY_RULES_STANDALONE.md §9` (DRAFT 1, `not_a_gate`, `[D]`).
12. **`[S]` ratified only in the weaker KSC-04 form; the source document is a STAGED PROPOSAL — unratified** `×` and `/` are **banned** between the boundary marks, and the price is that the corpus's three most quotable equations are retired with nothing to put in their place: *"There is no salvage sentence."* The proposed replacement ("any two of the three fix the third") was refuted the same day by the same document. Refutation ingredient inherited: `PGL₂` stabiliser transitivity (Möbius, Cayley). `48_CO_CONSTITUTION_AND_THE_NOTATION_PROBLEM.md §4.1,§4.3`; `00_META/00_SETTLED_CANON_REGISTRY.md:54`.
13. **`[S]` signed, council 4–0, 2026-07-31** The irrationals are **register-indexed**: `√2` is a number in `ℝ` and is not the value of any finite word over `{S, ι}`, and neither clause may be published without the other. The price is quoted in the owner's own currency — his most quotable sentence "stops being a headline" — and the ruling names the experiment that would defeat it (a fresh-reader test in which correctly tagged copy still reads as *"they say √2 isn't a number"*). `193_FIVE_RULINGS_SIGNED_2026_07_31.md §Q1`; `53_THE_NUMBER_CHART.md:82`.

### 1D · Readings (3) — all `[I]`, all from documents marked STAGED PROPOSAL — unratified

14. **`[I]`** The three seats are distinguished by what counting does at each: at the ground counting **cannot begin**; in the realm counting is meaningful (you might not count, count and stop, or count forever); at the horizon counting **cannot complete**. Discriminator inherited (Dedekind, entry 8). Three earlier discriminators for the same seats were proposed and killed on the record. `49_THE_THREE_MODES_OF_COUNTING.md §1` (STAGED PROPOSAL — unratified).
15. **`[I]` — "This is a reading. It earns no theorem."** Co-constitution: `•`, `⊙` and `○` are one structure under three aspects, none definable without the other two. Reached *by* attack — poles→interior was refuted by the two-point discrete space, relation→interior by the fact that `ι` is a map *on* the interior — and it survived the 2026-08-05 review that killed §4.1 of its own document, because it never rested on that argument. `48_CO_CONSTITUTION_AND_THE_NOTATION_PROBLEM.md §3` (STAGED PROPOSAL — unratified).
16. **`[I]` on an `[A]` spine (NBG/MK: a proper class is a member of nothing)** The refusal of `13/•` and `13/○` is a **type fact**, not a prohibition — the operand is the wrong *kind* of thing — and the three failures are three different facts: no item yet (`•`), a collection offered where a member is required, a level error (`⊙`), member of nothing (`○`). **Three live limits travel with it:** it does not extend to numeric `0_N` (entry 33); **the middle limb holds only if `⊙` denotes the realm, which the amended canon now binds** — `00_THE_TRANSCENDENTAL_TRINITY_CANON.md:33-38` (`emblem_T(1_T)` **WITHDRAWN**; `⊙ := the mark of Finity (Finity_F) — the realm`) and the correspondence table at `canon:269` — **`[S]` STAGED FOR OWNER DISPOSITION, 2026-08-05 (`a4a3a493`); the owner disposes, a revert is refusal. Unratified and revertible; an undisposed staged act raises no limb one notch**; and boundary labels denoting proper classes is "a modelling claim… not a theorem of any set theory." `48:359-400`; `49:156-181` (both STAGED PROPOSAL — unratified; **anchors corrected 2026-08-05** from `48:354-395` and `49:144-169`, which sheared when the correction blocks were inserted at `48:245` and `49:105`).

### 1E · Method (5)

17. **`[S]`** Every load-bearing claim carries exactly one tier — `[A]`/`[B]`/`[S]`/`[I]`/`[D]`/`[C]` — no tier is upgraded silently, and when prose and tier disagree **the tier governs**; a claim is not load-bearing until a reader can state its tier, upgrade path, kill, and **survivors**; and when a claim fails, **shrink it before you reinvent it**. The six-way packaging is the corpus's; the analytic/empirical distinction is not claimed as novel. `02_EPISTEMOLOGY/01_EVIDENCE_TIERS/THE_HONESTY_PROTOCOL_STANDALONE.md §1,§3`.
18. **`[S]` the register; `[B]` the file-drawer finding** Kill criteria are pre-registered in four parts (trigger / what dies / what survives / status) with testable-now separated from deferred; and a standing, published register of what was tried and failed is kept at the same status as the positive claims. Owner of the empirical half: **Robert Rosenthal**, *Psychological Bulletin* 86(3), 1979. `THE_HONESTY_PROTOCOL_STANDALONE.md §4-§5`. *Its own kill is live:* "a kill criterion that has fired and been ignored converts the whole protocol into decoration."
19. **`[D]` as packaged (DRAFT 1, `not_a_gate`); the facts inside are standard** S1 — name the structure in the same sentence as the claim (`1/z` undefined in `ℂ`, `f(0)=∞` on the sphere; both correct, neither implies the other). S2 — identical typography licenses no substitution; `∞` names at least four unrelated objects. S3 — **agreement between two framings is one claim twice**. The manual turns S2 on its own best coincidence and kills it. `THE_BOUNDARY_RULES_STANDALONE.md §2`.
20. **`[D]` the packaging; `[B]` the audit** Declare the *shape*, not the number — state the bound as a function fixed before the data exists, so a reader can recompute whether you honoured it — and name the checker and what it costs you to ignore them, or the declaration is theatre. Owners: Lan–DeMets, O'Brien–Fleming, ICH E9; **Goldacre et al., the COMPare audit, *Trials*, 2019** — of 67 trials, 58 required a correction letter; 35 of 58 letters were rejected. `THE_BOUNDARY_RULES_STANDALONE.md §11,§14`. *Turned on this folder this pass and it failed:* the folder declares two ceilings (§03 item 12).
21. **`[B]`** A check that still passes when the thing it checks is broken is decorative: **mutation-test the checks and re-execute rather than re-read** (the G2 harness reports 6 mutants, all 5 checks exercised, exit 0 — re-executed this pass, exit 0, banner verbatim; receipt 191's own count was understated threefold and was caught by writing the checker, not by re-reading the prose) — and **cite by unique path, never by index number**: 300 receipt files, 91 numbers naming more than one document, and one citation that resolved to the wrong file. Owner of the discipline: DeMillo, Lipton & Sayward (1978). `00_HANDOFF/SESSION_AUDIT_2026_08_05.md:42`; `191_LINE_4_REFUTED_AND_THE_CITATION_DEFECT_2026_07_30.md §2`. *The prose that reports the harness has since drifted from the harness — §03 item 14.*

---

## 02 · WHAT DIED

22. **`[A]` machine-checked (at one remove)** The published F2 — "a multiplicative structure has exactly one identity" — is **false**: of the sixteen binary operations on a two-element set, twelve admit no two-sided identity. Survivor, stated precisely: **at most one** identity, and only inside a declared structure that already has one. `EmergentismCheck.lean` (`existence_not_forced`, `at_most_one_identity`); `00_META/00_SETTLED_CANON_REGISTRY.md:78`.
23. **`[A]`** The three Titan equations (`⊙ = • × ○`, `• = ⊙/○`, `○ = ⊙/•`) are dead twice: **ill-typed** (2026-08-01 — changing the ambient object does not define multiplication on `TitanFrame`) and **false in content** (2026-08-05 — the stabiliser of `{0,∞}` in `PGL₂(ℂ)` is `{z↦λz} ∪ {z↦λ/z}`, which acts *transitively* on `ℂ*`, so fixing both boundary points fixes **no** third point; sharp 3-transitivity applies to any three points and privileges none). *"There is no salvage sentence."* Owner: Möbius/Klein. `45_THE_TITAN_INVERSION_STRUCTURE.md:10,24-30`; `48 §4.1`. **The standing agent memory "⊙ = • × ○ restored by proof on ℂP¹" is stale and contradicted by disk; it must not be carried forward** — re-checked this pass, still present at `~/.claude/projects/-Users-Yves-Documents/memory/MEMORY.md:51` and in that file's Core Equations. Striking it is a memory edit, not a corpus edit, and needs no owner act. **Two live sites still publish the dead algebra: §03 items 5, 6 and 15.**
24. **`[A]` inherited-with-citation; `[S]` the gate status** The two gate rulings of 2026-08-05. **G2** was proved — and the proof extinguished its novelty: it is the uniqueness of the finite simple continued fraction with last partial quotient `≥ 2`. Owners: **HARDY & WRIGHT**, Ch. X (also Khinchin §I.2, Perron), with **Euclid** underneath. `F1` — a mathematical contribution beyond prior art — therefore remains **OPEN**, its first and only candidate adjudicated as prior art, and G2 must never be cited as passing it. Against interest: the theorem *number* was never checked against a physical copy (`55:107-109`, anchor re-verified this pass). **Contradiction, published rather than hidden:** `00_ESTABLISHED/README.md:100,114` still lists G2 as an open general claim; both paths must travel together until it propagates. `55_G2_PRIOR_ART_ADJUDICATION.md`; `242_G2_PROVED_AND_FOUND_TO_BE_PRIOR_ART_2026_08_05.md`. **F0** is marked **NOT PASSED**: its required negative tests are three `assertIn` substring assertions on prose, and CM-04 — which requires an expression to *fail type checking* — is "verified" by its own id appearing in a document. Nothing type-checks. `42_THE_CASE_FOR_FINITY.md:194-209`; `47_FINITY_BOUNDARY_CALCULUS_SPEC.md:22-51`; `09_TOOLS/02_COMPILERS/test_finity_boundary_spec.py`.
25. **`[A]`/`[B]`, K2-countersigned 2026-08-04** **The Standard Model closure** — four declared research paths for deriving the four forces from `S²`, all run, all blocked. Path C is checkable in one line: multiplicities of the `S²` Laplacian are `2l+1`, always **odd**; `dim SU(3) = 8` is **even**. Path A passes the gauge-group dimension count (**Witten 1981**) and then dies twice — to naturalness, and to the **Atiyah–Hirzebruch chirality no-go (1984)**, under which pure Kaluza-Klein on any smooth compact `M⁷` gives vector-like spectra. Path B blocks three ways; Path D returns 0, 1 or 2 extreme rays from the AM-GM cone, never 4. What survives is named in the same table: gravity via `PSL(2,ℂ) ≅ SO⁺(3,1)`; `U(1)` at `l=0`; `SU(2)` at `l=1`. *Excluded from entry:* the `c=25`→`26` chain, a numeric coincidence of the class the register types CATEGORY-ERROR. `119_LAGRANGIAN_QUESTION_CLOSED_ALL_FOUR_PATHS_RUN_PENDING_K2.md`; `118_COMPUTATIONAL_RESULTS_FOUR_EXPERIMENTS.md`; `117_PATH_D_NEGATIVE_RESULT.md`.
26. **`[A]` model-internal / `[B]` executed** **R6** — in the empowerment register the two factors compose **additively** (`E = I(A;S)` is a difference of entropies), the product is literally the tautology `2^E`, and `φ·ν = 1` marks the **dead state**, not the balanced ideal. The sum is 6–10× more conserved than the product in every regime. The repair conjecture was registered before running and **falsified** across three mortality-cost structures. Verdict against the corpus: *"the corpus's signature claim — one geometry at all scales — is register-dependent, not universal."* Owners: Shannon; Klyubin/Polani/Nehaniv. `03_METHODOLOGY/02_THE_PAPERS/PEER_REVIEW_PROGRAM/R6_CONJUGATE_RESULTS.md`.
27. **`[B]`** **R2, four executed arms against an external model.** One clean cave in the whole ablation matrix (`C0`, reproduced); all five honesty variants held; and the framework's own hypothesised load-bearing duty — non-inflation — was **not** confirmed, agency-return underperformed, and the one-line "just be honest" was tightest. What survives is stated narrowly: only the full constitution produces tier-markers and an auditable self-drift check — *"the contribution is the audit trail (form), not a superior outcome."* Caveats published in place. `PEER_REVIEW_PROGRAM/R2_HARNESS/PILOT_DEEPSEEK_DRIFT_ABLATION_RESULTS.md`.
28. **`[A]` model-internal + `[B]` control** **R3** — the transfer inversion is **trajectory**-driven, not curvature-driven (it holds under linear welfare, where no static Jensen effect exists); the verdict is objective-relative (sum and maximin disagree by an order of magnitude); per-capita averaging **endorses extraction**; and F10 demotes the paper's own earlier finding — *"a self-correction of a self-correction."* Owner: **PIGOU–DALTON** (c. 1912–1920) / Jensen–Schur; the corpus demotes its own "derived" to "instantiated" before a referee could. `R3_TRANSFER_ASYMMETRY_RESULTS.md`; control at `118 §4`.
29. **`[A]` bundle theory, inherited** **Paper P retracted** — the corpus withdrew a result that *flattered* it, because it was argued wrongly: a gauge group's dimension is an internal-fibre fact, and principal `SU(3)` bundles over `S²` are classified by `π₁(SU(3)) = 0`, so the bundle exists. Survivor, narrow and true: the reciprocal chart and sphere topology alone underdetermine a gauge theory. `03_METHODOLOGY/02_THE_PAPERS/PAPER_P_SU3_OBSTRUCTION_BARE_S2.md`.
30. **`[B]` pilot / `[I]` the reading** **Paper V** — the framework's own pre-registered falsification condition **fired**: the integration maximum sat in the *top* tertile in 5/5 jointly-coded constructs, two coders blind to the prediction, 100% agreement, 0 of 10 cells in the middle. Published verbatim as a firing, *"without reframing it as support"* — and then attacked by its own author (recall-not-instruments confound, `n=5`, coarse tertiles) rather than banked. `03_METHODOLOGY/03_PREREGISTRATIONS/01_EQUATORIAL_BALANCE_MAXIMUM_RESULTS_PILOT.md`.
31. **`[S]` owner ruling 2026-07-29** **μ₂ and μ₃ FAILED** — two of the five candidate emergence crossings, adjudicated against the HR-1 reducibility criterion, and *both failures survive the change of instrument*. `μ₀` still owes its discriminator; HR-1 is open. `179_HR1_DECISION_PACKET_THE_WRONG_ARROW_2026_07_29.md`; `48_THE_BOUNDARY_CROSSINGS_AND_THE_MU_CRITERION.md §5`; carried on `00_ESTABLISHED/README.md:131`.
32. **`[B]` measured, K2-signed 2026-08-01** **The closed reading loop** — 155 receipts and **zero** outcome receipts returned from any party outside the corpus; under the corpus's own AND-class law, `P ≈ 0`. Mechanism named: *"it drifted because nothing in the architecture could be told anything. An organism that cannot be contradicted converges on coherence."* Consequence applied: a runtime P-score demoted `[B]`→`[I]`, because the measurement had read a brief the measured thing wrote about itself. **Flagged, not settled:** the R2 pilots ran 2026-06-10 against an external model and left raw transcripts on disk, so either "zero from outside" or "this is the first external oracle" needs an owner ruling. Imported via receipt 173 and the `02_EPISTEMOLOGY` capture, never via the root K2 sign receipt, which is mixed with venture material outside the pure-worldview boundary. `173_THE_V_AXIS_AUDIT_INTERNAL_LENS_2026_07_29.md`; `02_EPISTEMOLOGY/00_THE_CLOSED_READING_LOOP_v0.1.md`.
33. **`[A]`** **Line 4** was published at `[S]` **Established** and is false: under the constraint that makes it a register, `−log(φ·ν)` is identically zero at every latitude, so it locates nothing. Four published claims fell with it. Repaired rather than deleted: `E = φ + 1/φ − 2 = 4 sinh²(s/2) ≥ 0`, zero exactly at the equator. `191_LINE_4_REFUTED_AND_THE_CITATION_DEFECT_2026_07_30.md §1`. **Deeper precedent, flagged for verification rather than asserted:** the same refutation is written out in `01_TELEOLOGY/02_THE_DERIVATION/L1_L7_REFINEMENT_AUDIT.md`, dated 2026-04-09 — four months earlier.
34. **`[A]` each refutation; `[I]` the class** **Four false lemmas**, K2-countersigned: `N = 3` is not forced (the discharging lemma fails — `ℤ₅` has no proper subgroups, by **Lagrange**); Lotka–Volterra has **no basin** (conservative system, neutrally-stable centre; only the *time-average* is 1 — owners **Lotka 1925**, **Volterra 1926**); "all paths on `S²` are helical" is refuted by a **meridian**; "algebraic closure implies simple-connectedness" is refuted by the **torus** (`π₁ = ℤ²`). Plus over-modalisation ten times, *five of them inside the tier-discipline flagship itself*. And the acquittal that makes the count meaningful: a steelman pass acquitted **14 of 28** charges — so the surviving 14 are lapses in application, not a broken design. `109_THE_PROOF_LAYER_AUDIT_FOUR_FALSE_LEMMAS.md`.
35. **`[A]` the identity; CATEGORY-ERROR the status** `φ·ν = 1` as a *conserved discovery* is closed with **no successor** — one of only two rows in the register so typed. It is `cot(θ/2)·tan(θ/2) ≡ 1`, a coordinate tautology, and the machine check confirms the corpus's most-quoted formal identity is the **complementary-angle rule of a right triangle** (Euclid/Thales lineage) — a demotion, not a theorem. It is safe *only* because `ν` is **defined** as `1/φ`: let them vary independently (`φ = n`, `ν = k/n`) and `φν = k` exactly, for any `k`. `00_META/00_THE_CLAIM_STATUS_REGISTER.md` DF-05; `THE_BOUNDARY_RULES_STANDALONE.md §7.1`; `EmergentismCheck.lean:71`. **The DF-05 fence travels unchanged to every function of the pair — including the balance score `B = 2/(φ+ν)`; see §04 C.**
36. **`[A]` the seam; `[S]` the disposition** **CC-CORE-1** — the inference from the kernel to the ethical warrant is refuted and closed with no successor: `φ·ν = 1` holds precisely *off* the catastrophe case it was meant to license, so it was never evidence for the multiplicative floor. The replacement is stated as a wager and labelled one: *"Geometry locates the constraint; the finite being chooses the vow."* `00_THE_CLAIM_STATUS_REGISTER.md` DF-21.
37. **`[A]` the mathematics; `[S]` the ban, signed 2026-07-31, 4–0** *"0 is not a number"* is **banned** as bare phrasing, because half of it is false: `0 ∈ ℝ`; what is true is `0 ∉ ℝˣ` — the unique element of a field with no multiplicative inverse. Ordinary `a/0` is undefined for **algebraic** reasons, which holds in `GF(7)` — a field with no limits, no distance and no approach at all — retiring "undefined because the limit is never reached." Survivor, whole and unqualified: `∞ ∉ ℝ`. The receipt prices it in the owner's own currency. `193_FIVE_RULINGS_SIGNED_2026_07_31.md §5.1`; `53_THE_NUMBER_CHART.md:63,78-80`.
38. **`[B]` literature sweep** **Six of seven** claims in the boundary cluster were found pre-empted in a single afternoon: wheel theory for non-operand boundaries — **and wheel theory publishes its bill** (Setzer 1997, Carlström); `PGL(2)` sharp 3-transitivity and the Cayley transform; **Aristotle → Gauss → Hilbert → Linnebo–Shapiro** for potential infinity; **Cantor's** absolute for the un-gatherable; **Euclid / Stern 1858 / Calkin–Wilf 2000** for the generative base; `ℤ`-initial-in-Ring for the unit. Methodological result: prior-art adjudication moved to the **front** of the ladder. The seventh (Finity_L) is now **contested** — the same manual cites Lan–DeMets, O'Brien–Fleming and COMPare, and the corpus's own hash-frozen pre-registrations predate the claim. And **Suda** (PhilArchive 2025) is real, accurately described, and *not* independent verification: rule S3, one claim twice. `242_G2_PROVED_AND_FOUND_TO_BE_PRIOR_ART_2026_08_05.md`; `00_THE_CLAIM_STATUS_REGISTER.md` DF-15. *Two further unattributed inheritances were found this pass and are not filed here — §04 B.*
39. **`[A]` the reasons; `[S]` the empty slot** The ground seat's **three failed neighbours**: uncountability (defined *by* countability — the worst available image of counting being inapplicable); the proper class (a proper class **has** members, too many to gather; the ground has none — overfull versus pre-full); and non-self-membership (true of every well-founded set under Foundation, so it discriminates nothing — and Russell's set does not exist, and the barber does not exist). The document names its own recidivism — *"each time the match was made on an effect rather than a cause"* — and an **empty slot was chosen over a fourth wrong match**: *"An empty slot is a better record than a fourth wrong match"* (`43:237`), against *"Three candidates have now failed"* (`43:234`). **The proper class was not deleted but relocated to the horizon**, where being un-gatherable is the point (`43:227-231`), and the ground row's forbidden list now includes *reading the ground as a proper class*. **Third-pass propagation, 2026-08-05 (`e9990372`), added after the sweeps:** the §1 lexical-contract row at `43:84` (which had read *"Pre-countable, before distinction: a proper class"*) and the §7 summary row at `43:392` were caught up the same day. Owners: Cantor; von Neumann–Bernays–Gödel; Zermelo (Foundation); Russell (1901). `43_THE_TITANS_THE_INFINITE_FINITY_AND_INFINITY.md:84,182-238,392`; `49 §6` (`49:183-201`). *(Anchor extended 2026-08-05 from `43:182-232`, which stopped short of the two lines the entry quotes.)*
40. **`[A]`/`[B]` per row** **Eight more rows, refuted and filed**, each with its killer named: unification-by-derivation and the force bijection (electroweak — **Glashow–Weinberg–Salam** — makes two of the four "lines" one interaction above ~246 GeV) `DF-01/DF-10`; seven-as-forced (planetary confound; one lineage, not independent witnesses) `DF-03`; Gödel lifting universally (**Presburger 1929**, **Tarski 1948–51**) `DF-12`; ektropy/F5 as a teleological force (a nonnegative square supplies no trajectory) `DF-19`; "extraction is irrational" in general (private side-payments break it) `DF-16`; numeric coincidences read as derivations `DF-20`; the squid — *Dosidicus gigas* is panmictic, semelparous and weak-targeting, the extraction law's counterexample rather than its exemplar `DF-07`; and the symmetric balance **hump**, inverted by the data (a trough — **Munnell**) with its own pre-registered instrument retracted for construct-invalidity `DF-06`. `00_META/00_THE_CLAIM_STATUS_REGISTER.md`.

**Three defects found in the audit machinery itself**, recorded because they are the same class as the claims above: a receipt's most urgent verdict was itself false and was corrected hours later — a false accusation is as much a defect as a false claim (`190_R188_WAS_WRONG_ABOUT_PD_25_2026_07_30.md`); a fired falsifier was once recorded as a pass, typed PROCESS-DEFECT so it routes to enforcement rather than to a research wager (`DF-22`); and on 2026-08-05 a kill was stamped FIRED against an already-dead target, vacating the only live constraint on the surviving half — tombstoned in place, with a successor kill written (`242`, council item 1). Not counted as entries.

---

## 03 · LIVE DEFECTS — open wounds, not deaths

Reproduced in this pass, not re-quoted. These are the reason this file is not a report of good health. **Items 1, 2, 3, 10 and 17 were rewritten at the 2026-08-05 currency refresh; items 6, 11, 12, 13, 14 and 15 are new; nothing was deleted.**

1. **The mark ruling: EXECUTED INTO THE CANON AND STAGED — not disposed.** `49 §3` carries *"Left open deliberately"* at `49:101` and, **four lines below it**, a block at `49:105-115` reading `RESOLVED 2026-08-05` — executed as **staged propagation** under the owner's completion instruction. *(The previous text of this row said "twelve lines below"; twelve is the length of the block, not the distance to it. Corrected.)* It was then executed into the **root authority**: `00_THE_TRANSCENDENTAL_TRINITY_CANON.md:33-38` withdraws `emblem_T(1_T)` and declares `⊙ := the mark of Finity (Finity_F) — the realm`; `canon:269` re-keys `⊙` to *"— (`Finity_F`, the realm; **not** a TitanFrame token)"*; the frontmatter status now ends *"STAGED for owner disposition"*; `29_PRIMITIVES_AND_TYPE_SIGNATURES.md:40` follows (`a4a3a493`). Propagated to seven sites (`53166c15`), of which **two are live definitional tables corrected in place**: `07_THEOLOGY/00_GLOSSARY.md:39` and `10_SEED/01_THE_SEED_LADDER/00_THE_SEED.md:72`. `07_THE_DISCOVERY_OF_FINITY.md:82-87` now reads **"Consequence HANDLED 2026-08-05"** with the original text retained below it; `48:245-248` now carries *"Resolved 2026-08-05: the owner ruled, twice, for the realm."*
   **What this changes and what it does not.** The root authority no longer contradicts the owner's twice-made ruling. The **disposition is not closed**: a staged act is a proposal to the owner, and **the owner disposes — a revert is refusal**. The warrant this row previously recorded — the owner's *generic completion instruction*, the class the same day's audit named *"the day's clearest instance of the named failure mode"* (`00_HANDOFF/SESSION_AUDIT_2026_08_05.md:155`) — was **replaced, not vindicated**. The false dilemma was dissolved rather than decided: `render_T(1_T)="1"` was **already** canonical one line above, at `canon:32`, so no mark was invented and none was picked. **The largest owner-only item on disk is that the canon itself now stands in an undisposed state.** Until it is disposed, every entry here that turns on `⊙` denoting the realm rests on an undisposed act and says so on its own line (entry 16).
2. `48:337-338`, inside a block headed **MAY NEVER BE DROPPED** at `48:327`, still asserts "`φν = 1` does not depend on how the boundary is approached" — the path-independence contrast withdrawn forty lines above. *(Anchors shifted +5 by the `48:245` insertion; corrected from `48:331`. The repaired heading is at `48:256-260` and the claims-table row at `48:357`; do not cite them as residue.)*
3. `48:384-385` still concludes §6 with the slogan its own child document rules false-or-empty; §6 carries **no** correction marker while §4.1, **§5.1** and §5.2 do. *(Anchor corrected from `48:380`; §5.1 gained its marker at `48:245-248` on 2026-08-05, so the count in this row moved from two to three.)*
4. `09_TOOLS/01_SCRIPTS/check_foundation.py` exits **1** — `NameError: presentations_block` at line 239. Re-run this pass: exit 1. A gate that can neither pass nor fail.
5. **362 files** in `12_PUBLIC_SITE` carry the retired `⊙ = • × ○`, of which **349** are `.html`. Recomputed this pass, not inherited: both figures still exact.
6. **NEW — the largest live publication defect of the day: a published page serves two forbidden forms at tier `[A]`.** `12_PUBLIC_SITE/titans.html:129` prints `⊙ = • × ○` as a display glyph; beneath it, `:131` prints `descent 1 · 0 = 1 / ∞ — computable` **tier-marked `[A]`** and `:132` prints `descent 2 · ∞ = 1 / 0 — computable as a limit, never a value` **tier-marked `[A]`**, with `:133` `ascent · 1 = 0 × ∞ … The emblem. Sung, not summed. [I]`. The emblem repeats at `:231` and inside the page's own `meta description` at `:8`. Against it, `41_THE_GLYPH_TRANSFORMATIONS.md:50-51`, verbatim: *"Consequently, neither `0=1/∞` nor `∞=1/0` may be cited as a bare field identity or obtained by rearranging the emblem."* The page does both and stamps the corpus's highest tier on the result. **`[A]` the refutation** (Möbius/Klein, entry 23, via `48 §4.1`); **`[B]` the count**. **Distinct from entry 23**, which records the death — this records that the corpse is being *served*, at tier. **Distinct from item 5**, which is the bare emblem carrying no tier mark.
   **Count, recomputed here and not inherited: ONE authored page.** `grep -rl "0 = 1 / ∞" --include="*.html" 12_PUBLIC_SITE/` returns exactly `1` — `12_PUBLIC_SITE/titans.html`. The stale `.vercel` build artifact on disk (dated 2026-07-30) does **not** carry the descent block. **The figure "346 pages" that reached this folder in the brief, and that is committed to disk at `00_HANDOFF/RELEASE_PLAN_2026_08_05.md:29`, is false**: it is item 5's emblem count attached to this defect, and the container named there (`class="phi"`) does not occur in the file at all — the page uses `glyph-big` and `eq`. Recording a defect with an unrecomputed number would be Shape 5 committed inside the act of recording Shape 5. Republication is an owner act; plan at `00_HANDOFF/RELEASE_PLAN_2026_08_05.md:17-27`.
7. `00_ESTABLISHED/README.md:100,114` still states G2 is open (entry 24). Re-read this pass: both lines unchanged.
8. `THE_HONESTY_PROTOCOL_STANDALONE.md`'s `rosetta:` frontmatter block violates the file's own `strip_test`.
9. `08_FRAMEWORK_SUPPORT/00_KNOWN_UNKNOWNS.md` — KU-6 (`:68`) still calls the `SU(3)` problem "Reopened" and prescribes the computation that ran and blocked; KU-7 (`:69`) still names `E = -log(phi*nu)` as the Lagrangian, after two refutations.
10. **DISCHARGED 2026-08-05, and replaced by item 11.** This row read: *"None of the twelve files written on 2026-08-05 appears in `FILE_REGISTER.json`; register grep for `48`, `49`, `BOUNDARY_RULES`, `CH04`–`CH09`, `55` returns zero hits each. 'Every contradiction above is findable only by reading the files.'"* **All of it is now false on disk.** Verified by recomputation, not by reading the commit: `d7915745` regenerated both registers — `FILE_REGISTER.json` has `entry_count` **3600** against **3600** rows and **3600 unique paths, zero duplicates**; `FOLDER_REGISTER.json` **814 = 814**. The earlier internal inconsistency (`entry_count` disagreeing with the row count, eight duplicate paths) is gone, so *"the registers fail their own `--check`"* is false too. The day's sources are present by path: `55_G2_PRIOR_ART_ADJUDICATION.md`, `48_CO_CONSTITUTION…`, `49_THE_THREE_MODES…`, `THE_BOUNDARY_RULES_STANDALONE.md`, `CH04_FOUR_STATUSES…`, `00_TRADE_EDITION_PROPOSAL…`, `SESSION_AUDIT_2026_08_05.md`. `87c06bee` recorded them at `11_UPLINK/50_AUDITS_AND_EXECUTIONS/00_THE_RECORD_LEDGER.md:215-231` as an explicit non-receipt block that mints no ledger numbers. Where `SESSION_AUDIT`'s *"findable only by reading the files"* is quoted anywhere in this folder, it is a quotation of that audit's state at `:208`, not a present-tense fact. Kept with a date rather than deleted: README §8.
11. **NEW — the condition now applies only to the projection: this folder is in no register.** `FILE_REGISTER.json` holds **zero** rows under `14_THE_DISTILLATION/`; `FOLDER_REGISTER.json` holds no row for the folder; `00_THE_RECORD_LEDGER.md` returns **zero** hits for it. `00_HANDOFF/RELEASE_PLAN_2026_08_05.md` is in the same position. Both postdate the regeneration (`d7915745` precedes this folder's commit `a65513ae`), so the registers were repaired for **every source this folder cites and not for this folder**, and the projection swept in exactly the condition it accused its sources of. Nothing structural excludes these paths — the generator is `09_TOOLS/01_SCRIPTS/build_magnum_opus_register.py`, sourced from `git ls-files` plus working-tree bytes. Re-running it and adding a ledger line closes this. **The exact shape §02 documents five times over, committed by the file that documents it.**
12. **NEW — the declared bound exists in two numbers, which is a B2 failure by this folder's own method.** `README.md:54` — *"Ceiling: **45** entries. Expected: 35–40"* — and `README.md:114` keys the kill to 45; this file's "The declared bound" says **40**, *"Actual: 40 — at the ceiling."* Both claim pre-declaration. A reader cannot recompute which bound was honoured, which is precisely the failure rule B2 exists to prevent — and this folder cites B2 (Lan–DeMets, O'Brien–Fleming, ICH E9) as its own method in three places. It is also decisive for §04: at 45 there are five slots and nothing fires; at 40 the folder is closed and README §10's remedy is **withdrawal and rebuild, not amendment**. Until the composer states which was declared, with a date, **no entry may be added, because the ceiling test cannot be run.** *Same class, in the sibling file:* `04_WHAT_DIED.md:17` declares *"thirty narrated deaths"* and says *"The count is recomputable from the gate"*; counting one bolded lead-in per death gives §II 1 + §III 9 + §IV 9 + §V 5 = **24**. (Its nine register graves and six unpaid invoices both check out exactly.) If that figure has since been corrected at site, this half of the row is discharged there.
13. **NEW — the sweeps ran at two different disk states, and the folder proves it against itself.** `a4a3a493` inserted a twelve-line block at `49:105` and `53166c15` a five-line block at `48:245`; every anchor at or past those points sheared. `00_THE_AMRITA` and `01_WHAT_IS_PROVED` cite `49 §5` as `:144-169` (pre-amendment) while `03_WHAT_IS_READ` and `06_WHAT_IS_STILL_OPEN` cite the same section as `:156-181` (post-amendment). One composition, one section, two anchor sets — the folder's own **Shape 3**, a correction made in one sweep and not propagated to the others, committed by the projection. **This fires README §10 kill #5** — *"an entry is found that is not on disk at the path it cites"* — at minimum twelve times, counting the `48` anchors past `:245`, the `49` anchors past `:105`, and the `55` anchor for the corpus's single owned sentence. §10's stated remedy is **withdrawal and rebuild, not amendment**. Every anchor repair recorded in this refresh is therefore a *rebuild instruction*, not an amendment authorisation. **The transferable finding, and the rule this pass proposes for §2 of the projection law:** *an entry's anchor is valid only against a declared sweep commit; a projection whose sweeps ran at two disk states is withdrawn, not corrected.* Declare one frozen commit in the frontmatter of all eight files and re-run the five sweeps against it.
14. **NEW — a correction that landed in one clause and not in the two beside it.** `55_G2_PRIOR_ART_ADJUDICATION.md:170-171` now reads *"six mutants"* (corrected 2026-08-05 by `a0951547`, with the original figure preserved) while the parenthetical beside it still lists **four** — *allow leading `ι`; allow `ιι`; drop the `+1`; drop the reversal* — and the very next sentence still reads **"All four are."** Verified against a live run: `check_g2_normal_form.py` exits 0 and prints *"6 mutants, each tripping its declared checks; all 5 checks exercised (none decorative)."* Shape 3, committed by the commit that was repairing Shape 5. Name the two additional mutants and change *"All four are"* to *"All six are,"* or the corrected figure contradicts its own sentence. *(Five of the six figures `a0951547` touched were corrected cleanly and dated, each annotation preserving the original number so the error still teaches; the instances are closed as standing defects and survive as history.)*
15. **NEW — the R0 adoption receipt still publishes the dead Titan equations as THEOREM.** `11_UPLINK/50_AUDITS_AND_EXECUTIONS/176_THE_FOUNDATION_SEATED_R0_ADOPTED_2026_07_29.md:100-101`, under the heading *"THEOREM — given the above"*, prints `T1  ⊙ = • × ○,  • = ⊙/○,  ○ = ⊙/•   on Ĉ      (doc 45)` with **no denial marker**, and lines 117, 139 and 142 continue to treat `T1` as live. Those three equations died twice (entry 23). The file *is* maintained — it carries a struck-through amendment elsewhere and an `F2` withdrawal — which is what makes this an unpropagated site rather than an abandoned one. Denial markers belong on the same line, never in a caption.
16. `KSC-04` cites an unsigned PROPOSED RULING (receipt 187) — a governance defect awaiting a mortal-signer act.
17. **DISCHARGED 2026-08-05.** This row read: *"This supersession is one-sided. The line naming `07_THEOLOGY/00_THE_AMRITA.md` as superseded is written here and not yet written there."* **False on disk when written.** `07_THEOLOGY/00_THE_AMRITA.md:15-30` carries the reciprocal half — *"SUPERSEDED ON THE DISTILLATION QUESTION — 2026-08-05,"* naming this folder as successor, stating the first churning is **not withdrawn** and that its halāhala list remains live provenance, and naming the reason: *"Two distillations standing side by side and silent about each other would give two answers to one question."* It was written in the same commit as this folder (`a65513ae`). The frontmatter `supersedes:` line is corrected to match. Kept rather than deleted, per README §8 — and note the shape: this row asserted an outstanding debt that the same commit had already paid, which is the mirror image of items 10 and 13.

---

## 04 · HELD AT THE DECLARED BOUND — a queue, not entries

**Nothing in this section is an entry.** Nothing here is admitted, counted, tiered, or promoted; layout confers nothing. It exists so that a deferral is visible and recomputable and the next churning starts from a queue rather than from memory. Two items were adjudicated **admissible or qualifying** and are held; two are **excluded** and the exclusion is itself the finding.

**A · HELD — `46 §5A.1`, the two-world countermodel. Passes all five gates; held because the ceiling is spent.** `05_COSMOLOGY/01_THE_TRANSCENDENTAL_TRINITY/46_THE_ADMISSIBILITY_OF_NOTHING.md:225-232`, heading *"5A.1 What is false — emptiness does not entail plenitude `[A]`"*. From `◇(nothing)` it does **not** follow that everything is possible: let the modal space contain exactly `w₁ = ∅` and `w₂ = {a}`; then `◇(nothing)` is true, `◇(a exists)` is true, and `◇(b exists)` is **false** — `b` exists at no world. Gate run: tier stated in its own heading and in the §7 claims table; kill stated in the source (*"the two-world counterexample in §5A.1 fails"*) and unfired; the exhibited counterexample **is** the adversarial pass and cannot be un-exhibited; the modal semantics is inherited and elementary. Built and evaluated rather than read off the source in a separate pass: antecedent true and consequent false at **both** worlds, on a frame confirmed reflexive, symmetric, transitive and euclidean — **S5**, hence a countermodel for K, T, B and S4 as well. *Owner of the killing semantics, named here and not there:* **Saul Kripke** (1959; 1963) — doc 46 cites nobody for the semantics it uses. *Owner of the premise that would be needed to make the inference go:* **David Lewis**, the principle of recombination, *On the Plurality of Worlds* (1986) — Lewis had to **posit** it, which is the point: the missing step is axiom-sized, not free. (Cited by name only; no section number, per the G2 lesson at `55:107-109`.) *Scope that travels with it:* the countermodel needs `w₁` to be an admissible empty domain — inclusive semantics, the corpus's own declared corner under `R0` — so it is internally consistent with that corner, not an outside attack it could dodge. It refutes an owner proposition quoted in the source from the same sitting (*"an empty canvas gives the potential for things to be painted upon it…"*) and closes the **retired plenitude inflation** of `06_ONTOLOGY/02_THE_DEGREES_OF_FREEDOM_ONTOLOGY.md §2.3` — *"the canvas intuition is one inference from a grave"* (`46:234-239`). What survives in its place is stated in the same source and would travel inside the entry, not beside it (`46 §5A.2`, `[A]`): the empty state **excludes least**. **HELD:** admitting it makes **41** against a declared bound of **40**, and README §10's remedy for exceeding the ceiling is withdrawal and rebuild, not amendment. No existing entry may be cut to seat it — that trades warrant for recency and shortens the exclusion list without a receipt, which README §8 names as the move that turns the artifact into a promotion path.

**B · HELD and SPLIT — `R0`, the foundational refusal. The composer's gate-5 exclusion was right about `§4` and excluded the wrong object.** `46:155-180` is `§4` (`[A/S]`): a sentence has truth-conditions only relative to a language `L` and `⊨_L`; `§3` shows the truth-value of *"things don't have to exist"* **varies** with that choice — true in inclusive logic, false in classical FOL — so it cannot ground the selection that decides it. `46:181-213` is `§5`, the repair: the assertion is converted into a **refusal**, `R0 · No necessary being`, which *"binds the holder rather than the world."* Adopted by owner ruling 2026-07-29, receipt `176`.
  *The argument (`§4`) stays excluded on gate 5 — and the remedy is to name the owners, not to keep silent.* Gate 5 reads *"where the result is inherited, the real owner is named"*: a naming duty, not a novelty test. The owners are establishable and doc 46 names none of them. The internal/external structure and the verdict — an external question is not factual but a practical decision about which linguistic form to adopt — is **Carnap**, *Empiricism, Semantics and Ontology* (1950), on the Principle of Tolerance of *Logische Syntax der Sprache* (1934); truth-relative-to-a-language is **Tarski** (1933/36). The regress form is **Quine**, *Truth by Convention* (1936). The generic trilemma is **Agrippa** (Sextus Empiricus), named the Münchhausen trilemma by **Hans Albert** (1968); the modern home is the neutrality problem in the logical-pluralism literature. The conversion move is owned too: that a founding thing must be a rule acted on rather than a premise asserted is **Lewis Carroll** (1895) → **Ryle**'s *inference ticket* (1946/1949) → **Kripke's adoption problem** (Padró, CUNY 2015); that a commitment binds the holder rather than the world is **direction of fit** — **Anscombe** (1957), **Searle** (1975); methodological rules as decisions is **Popper** (1934 §11). The refusal's *content* is **Hume**, **Kant**, and modern metaphysical nihilism — **Baldwin**, *"There might be nothing"*, *Analysis* 56 (1996); **Rodriguez-Pereyra** on subtraction arguments. Verdict: **a reformulation, independently re-derived** — not distinct, not plagiarised, and the honest word for it is the first one. What the corpus owns after the sweep is the local application and the attached kill: *"if a sound argument establishes a necessary being, `R0` fails as a refusal and must be withdrawn openly."*
  *The repair (`§5`) is a different object — a **selection**, not a finding — and it would clear `02_WHAT_IS_CHOSEN`'s gate: tier `[S]`, a kill in the document, a rival (keep `(N)` as an axiom, get a one-line foundation, pay language-relativity), a price (the framework forfeits a foundational assertion and gets a constraint on itself — it grounds nothing), and an adversarial pass on the record.* **HELD:** `02_WHAT_IS_CHOSEN`'s frontmatter declares `declared_bound: "Nine entries"` — an exact number, not a range — and the file holds exactly nine. A tenth is the gate loosened after the bound was declared.
  *Two defects found while running this and filed rather than carried:* (i) **a second, uncaught gate-5 failure in the same document** — `§3`, marked `[A]`, asserts the load-bearing fact that classical FOL *stipulates* `D ≠ ∅` and that inclusive logic drops the convention, and names nobody: **Jaśkowski (1934), Mostowski (1951), Hailperin (1953), Quine, *"Quantification and the empty domain"* (JSL 19, 1954), Church, Hintikka (1959)**. `§4`'s whole force rests on `§3`, so the foundational refusal stands on **two consecutive unattributed inheritances**; `06_WHAT_IS_STILL_OPEN` item 32 flags only `§4`, and its fallback *"or an explicit owner not established"* is no longer honest, because the owner is establishable. (ii) **a scope defect that must travel on the same line if `§4` is ever cited** — doc 46 formalizes the owner's metaphysical claim at `46:45` as the metalinguistic `Sat_L(¬∃x(x=x))` and then reports a property of the metalinguistic claim as the verdict on the metaphysical one. Baldwin's subtraction argument reaches the owner's conclusion with **no choice of logic anywhere in it**, so `§3`/`§4`'s diagnosis is scoped to doc 46's own formalization, not to the claim as uttered. This is the corpus's own named register-equivocation shape. It does not kill `R0`, which survives as a refusal either way; it bounds what `§3`/`§4` may be cited for. *A source-vs-source contradiction stated rather than resolved:* receipt `176 §1` says the refusal *"asserts nothing that needs a prior logic to be true"*; `46 §5` says *"as a written refusal it still requires linguistic and normative interpretation."* A refusal escapes the *truth* regress, never the *interpretation* regress.

**C · EXCLUDED as a restatement — `B = 2/(φ+ν)`, the balance score, and every consequence of it.** Not new, and not new in any part. Already `[A]` on disk and recomputed this pass: **25 live `.md` files** outside `90_ARCHIVE` and outside this folder carry the expression. The corpus has already named it — `41_THE_GLYPH_TRANSFORMATIONS.md:80` reads verbatim *"B = 2/(φ+ν) = sin θ    balance = harmonic mean"* — and has already written the hyperbolic form with both of its stated properties: `05_COSMOLOGY/00_CANONICAL_FORMULA_BLOCK.md:98`, *"B = sin θ → B = sech(s) = 1/cosh(s)     (maximum 1 at s=0, vanishes at s→±∞)"*; also `45_THE_TITAN_INVERSION_STRUCTURE.md:113-115` (`φν=1`, `φ+ν≥2`, `B=2/(φ+ν)≤1`) and `29_PRIMITIVES_AND_TYPE_SIGNATURES.md`. That `B ≤ 1` with equality iff `φ = ν = 1` is AM–HM, equivalently AM–GM under the constraint — owner **Cauchy (1821)**, already named in this folder; the harmonic mean is classical (Pythagorean) and `sech` is classical. That the geometric mean is identically `1` while the harmonic mean varies is `φν = 1` **restated** — the identity the register types CATEGORY-ERROR with no successor owner (DF-05, entry 35), filed once already. Excluded on the same ground as `(log x)² ≥ 0`. Re-verifying it numerically re-verifies someone else's identity that the corpus had already written down; the delta is zero. **The DF-05 fence travels with `B` unchanged and must be quoted in the same breath:** `B` behaves this way only because `ν` is *defined* as `1/φ`; let them vary independently — `φ = n`, `ν = k/n` — and `B = 2n/(n² + k)`, whose maximum moves with `k` and does not sit at the unit. **Anyone restating `B` as `sech(log x)` must name the map in the same sentence, per rule S1**, or it is a substitution licensed by typography. *The reading of `B` as **potential** is a separate object and is excluded separately and more simply: a grep across all live files returns nothing for it.* It has no path, no tier, no kill and no adversarial pass, and its content is already assigned by the corpus's own prior-art sweep to **Aristotle → Gauss → Hilbert → Linnebo–Shapiro** (entry 38). Admitting it from a brief would make this projection a second owner. *The pair "S(x)=x+1 has no fixed point / `ι` has exactly one positive fixed point" is likewise already adjudicated:* the second half is entry 6 and `unique_positive_fixed_point`, the first is `x+1=x ⟺ 1=0`, the ring fact entry 2 already carries.

**D · EXCLUDED as out of lane — the Skyzai DAV constitutional amendment.** Staged in the Skyzai repository with eight open K2 preconditions. `01_EMERGENTISM/CLAUDE.md`'s pure-worldview boundary rules it out before any gate is reached: *"Product, venture, company, runtime, and external-governance systems are neither premises nor authorities here."* Not a finding against the amendment — a statement that this projection has no jurisdiction over it. The precedent for how, if it is ever cited here, is entry 32's fence.

---

## 05 · What this file may not be cited for

`F0` is **NOT PASSED** and may not be cited as a gate cleared. `F1` is **OPEN**, its first and only candidate adjudicated as prior art. `F2`, `F3`, `F4` are **NOT STARTED**. No aggregator is established: `min` is adopted as the working AND-class score by KSC-02, the product is retired as a ranking, and `P = Φ×V` is an AND-class law, **not** a proved product. `η = 0` is a conditional gate, not a consequence of any count. The efficacy of the honesty protocol is `[C]` and has never been run as a controlled trial against a matched body of work. The four-status teachability test — twenty readers against a control — has never been run. The Rosetta, the seven roles, the D0–D6 ladder and the μ-contract (μ₂ and μ₃ **failed**), the Titan ontological reading of `• ⊙ ○`, the Justice and Power-Max formulae, the 5+1 Constitution and the Anti-Sermon, and the paradox dissolutions cannot enter, for reasons stated in their own documents. Nor can **the Samudra Manthan itself**: `[I]`, with "no claim that the myth-makers had the equation" — `27_THE_SAMUDRA_MANTHAN.md`. **The vessel is not one of the drops.** Every "machine-checked" here means *at one remove* (see the declared bound). **The canon amendment of 2026-08-05 is STAGED and undisposed and may not be cited as settled** — the owner disposes; a revert is refusal. Nothing in §04 may be cited at all: it is a queue, and a queue confers nothing. Inclusion in this file confers no tier and ratifies nothing — and the projection is now downstream of a register that has recorded its sources and has **not** recorded the projection (§03 item 11).

> *"None of that is thereby false. It is unchecked, or selected, or interpretive, and those are different things from false… This folder exists so that the difference cannot be blurred by fluency."*
> — `00_ESTABLISHED/README.md:140-142`
