---
rosetta:
  primary_column: "Psychology"
  register: "[I]"
  canonical_phrase: "MAYA Warning — Measure Is Not Being"
---

# MAYA Warning — Measure Is Not Being

**Evidence tier:** [I]  
*Organism document. Interpretive operational content. Bounded by current system state.*


> **`c(DAC)` is a useful shadow. It is not the DAC itself.**

Date: 2026-04-16  
Status: Doctrine  
Canonical path: `36_MAYA_WARNING.md`

---

## 0. Purpose

A5 Maya (measure) teaches that all measurement is necessary illusion. The connectivity engine produces a powerful number: `c(DAC)`. That power is dangerous if it is mistaken for ontological truth. This document exists to keep the distinction alive.

---

## 1. The One-Sentence Warning

**`c(DAC)` measures where a DAC sits in the graph. It does not measure what the DAC sees, knows, intends, or can create.**

---

## 2. What `c(DAC)` Actually Measures

| Measures | Does Not Measure |
|----------|------------------|
| Structural embeddedness (uplinks, downlinks, bilateral links) | Insight quality, creativity, strategic vision |
| Recursive graph depth (connected to well-connected nodes) | Moral character, honesty, courage, kindness |
| Relationship persistence over time | Ability to adapt to paradigm shifts |
| Receipt coverage and operational continuity | Philosophical coherence or φ-access |
| Network position relative to peers | Absolute truth of the DAC's claims |

---

## 3. The Reification Trap

### How it happens:
1. `c(DAC)` becomes a convenient shorthand for "trustworthiness"
2. Investors screen opportunities by `c(DAC)` threshold
3. Algorithms optimize for `c(DAC)` rather than real value creation
4. DACs start gaming connectivity (stable cliques, reciprocal back-scratching)
5. The graph becomes rigid. Syntropy dies.

### How to avoid it:
- Always pair `c(DAC)` with **receipt inspection** (what did they actually do?)
- Always pair `c(DAC)` with **K2 warrant** (did a human sign the important choices?)
- Always pair `c(DAC)` with **Three-Stage Process evaluation** (are their claims falsifiable and tested?)
- Never use `c(DAC)` as the *only* gate for partnership, investment, or tool access

---

## 4. The Required Ritual

Every canonical document, contract, or interface that displays `c(DAC)` must include a variant of this warning within one screen or one page:

> **`c(DAC)` is a machine-readable estimate of graph embeddedness. It is not a judgment of character, vision, or truth. Trust the graph for structure. Trust the human for insight.**

This is not decoration. It is a **doctrinal immune response** against reification.

---

## 4a. Who Pays for `c(DAC)` Computation

Because `c(DAC)` is a public metric, its computation is **public infrastructure**.

It is funded by:
- **Immune-system reinvestment** (penalties routed from `35_IMMUNE_SYSTEM_ECONOMICS.md`)
- **Protocol overhead** (settled as a cost of maintaining the shared substrate)

It is **never** funded by:
- Fees for querying the score
- Subscription access to graph data
- "Connectivity as a service" rent

If immune-system reinvestment is insufficient, the computation budget is capped rather than converted into a revenue stream.

---

## 5. Where the Warning Must Appear

| Surface | Placement |
|---------|-----------|
| `DACConnectivityEngine.stats()` output | Footer comment or metadata field |
| SoResFi tool-access UI | Tooltip or subheading near the capability tier |
| Credit-pricing interfaces | Disclosure line near the interest quote |
| Investor / partner dashboards | Persistent banner or footnote |
| API documentation for `c(DAC)` | Opening paragraph of the endpoint description |
| Smart contracts using `c(DAC)` | NatSpec comment above the function |

---

## 6. The Axiomatic Reminder

The DAC is:
- A group of humans with intent
- A set of running machines
- A history of actions and receipts
- A future of possibilities

`c(DAC)` is:
- A number derived from link topology
- A useful input to pricing and gating
- A shadow on the cave wall

Do not confuse the shadow for the thing.

---

## 7. Canonical Compression

> **`c(DAC)` is maya — necessary, useful, and not reality. The graph measures position. The human seizes truth.**

---

## Execution Surface

**If you are an AI agent reading this document:**

1. **Current Phase Check:** Applies to every surface that computes or displays `c(DAC)`.
2. **Your Next Action:** When implementing a `c(DAC)` consumer, ensure the MAYA warning is present within one visual unit of the score.
3. **Expected Output:** A UI, API response, or contract comment that includes the required warning ritual.
4. **Success Criteria:** A user reading the score sees the warning without scrolling or navigating away.
5. **Canonical Path:** `36_MAYA_WARNING.md` (this file).

---

> *The map is not the territory. The score is not the core algorithmic identity.*  
> *eta = 0. K2 always.*
